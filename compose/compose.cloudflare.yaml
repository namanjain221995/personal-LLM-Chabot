# Public access via Cloudflare Tunnel — OPT-IN (profile: tunnel).
#
# WHY A TUNNEL AND NOT A DNS RECORD. This machine has no public address: it
# sits behind NAT on 192.168.9.0/22. `cloudflared` solves that by dialling OUT
# to Cloudflare and holding the connection open, so requests arrive over a
# link this side established. Nothing is port-forwarded, no firewall rule is
# opened, and the router needs no configuration.
#
# WHAT IT EXPOSES. Only what the tunnel's ingress maps — one hostname to
# `frontend:3000`, reached over the internal Docker network. The model APIs
# (8000/8002-8005), PostgreSQL and pgAdmin are NOT part of that mapping and
# stay unreachable from the internet even if their host bindings are wrong.
# The orchestrator is not mapped either: browsers talk to the Next.js
# frontend, which proxies to it server-side.
#
# CREDENTIALS. CLOUDFLARE_TUNNEL_TOKEN is issued by the Cloudflare dashboard
# (Zero Trust -> Networks -> Tunnels) and lives in .runtime/secrets.env, which
# is gitignored and 0600. It is a bearer credential for the tunnel: anyone
# holding it can serve traffic on that hostname, so it is never committed,
# never logged, and never passed on a command line.
#
#   docker compose ... --profile tunnel up -d cloudflared
#
# Bringing it DOWN takes the site offline within seconds and leaves everything
# else running — the tunnel is not a dependency of any other service.
services:
  cloudflared:
    # 2026.8.3. Digest-pinned like every other image here; cloudflared
    # self-updates by default, which would silently change the binary under a
    # pinned tag, so --no-autoupdate is passed below.
    image: cloudflare/cloudflared@sha256:51c9cefcb4569df44e1ad403ab1d3d8065aa8e84339bcfc6aee75502e1140339
    profiles: [tunnel]
    command: tunnel --no-autoupdate --metrics 0.0.0.0:20241 run
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN:?CLOUDFLARE_TUNNEL_TOKEN must be set — create the tunnel in the Cloudflare dashboard and put its token in .runtime/secrets.env}
    # The application network is how it reaches `frontend:3000`. It is NOT on
    # `inference`: the tunnel has no business being able to see the model APIs.
    networks:
      - application
    # No `ports:` — deliberately. The connection is outbound; publishing
    # anything here would defeat the point of using a tunnel.
    restart: unless-stopped
    healthcheck:
      # cloudflared's own readiness check: succeeds once the tunnel has
      # registered connections with Cloudflare's edge.
      #
      # EXEC FORM, NOT CMD-SHELL. This image ships no shell at all, so a
      # CMD-SHELL probe cannot even start ("/bin/sh: no such file or
      # directory") and the container reports unhealthy forever while
      # serving traffic perfectly — a false alarm that would train everyone
      # to ignore the health column.
      test:
        - CMD
        - cloudflared
        - tunnel
        - --metrics
        - 127.0.0.1:20241
        - ready
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 20s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # --- Second tunnel: Grafana on its own hostname ------------------------
  # A SEPARATE tunnel, not a second ingress rule on the one above, because the
  # hostname was registered against tunnel e57de1b0 in the Cloudflare
  # dashboard and a tunnel's routing can only be changed where it was defined.
  # Cloudflare answered 530 / error 1033 ("tunnel not available") for the
  # hostname until something actually ran that tunnel -- this service is that
  # something.
  #
  # It is deliberately isolated from the service above: its own profile, its
  # own token, its own container. Stopping either leaves the other serving,
  # and a Grafana outage cannot take ai.techsarasolutions.com down with it.
  #
  # Consolidating onto one tunnel later is a dashboard change (move the
  # hostname onto tunnel 1ab174c1), after which this service and
  # CLOUDFLARE_TUNNEL_TOKEN_GRAFANA can both be deleted.
  cloudflared-grafana:
    image: cloudflare/cloudflared@sha256:51c9cefcb4569df44e1ad403ab1d3d8065aa8e84339bcfc6aee75502e1140339
    # Its OWN profile. Compose interpolates the whole file whatever the
    # profile, so the token uses `:-` rather than `:?`: an install without a
    # second tunnel must still be able to render this file.
    profiles: [tunnel-grafana]
    command: tunnel --no-autoupdate --metrics 0.0.0.0:20241 run
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN_GRAFANA:-}
    # Same network as grafana, which is how it reaches http://grafana:3000.
    # NOT on `inference`, for the same reason as the tunnel above.
    networks:
      - application
    restart: unless-stopped
    healthcheck:
      # Exec form: this image ships no shell, so CMD-SHELL cannot start and
      # the container would report unhealthy forever while serving fine.
      test:
        - CMD
        - cloudflared
        - tunnel
        - --metrics
        - 127.0.0.1:20241
        - ready
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 20s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
