# Review findings — round 2 (v2 code)

## orchestrator / contract-v2 (CONTRACT.md §4, §6.7, §7.2, §8; one-model invariant; exact sentences; metric names/labels; MODEL_RECOVERING frame). Read in the worktree /home/techsphere/Documents/project/personal-LLM-Chabot-ha: orchestrator/app/{breaker,engine_state,continuity,admission,resilience,llm,metrics,main,db,health,config}.py plus deep_research.py and the six test files. One-model invariant: every answer-producing call (chat_completion, chat_completion_with_reasoning, stream_chat_completion, stream_chat_events, chat_with_tools, json_completion) goes through _primary_send on settings.openai_base_url; resolve_model_choice returns the main model for every picker value; stream_chat_events runs assert_answer_engine before opening the stream; router_chat_completion is used only by engines/router.py, engines/orchestrate.py, engines/search.py (query generation), freshness.py, facts.py (fact extraction), titling.py (conversation titles) and video/screen.py (frame captions, direct client, bypasses resilient) - none of them writes a person's answer; agent_base_url has no caller outside health.py; vision_chat_stream (vision_base_url, = main by the launcher) has no caller at all. No path answers a user from another model - no blocker in that class. Verbatim checks: QUEUED_LINE and EXPIRED_LINE match §8.3 exactly; LONG_LINE matches §6.7; NORMAL_LINE does NOT match §8.3 (see finding). Metrics: all 11 §7.2 names exist with the contract's labels; reasons/outcomes/lanes/to are bounded per metric in metrics.py (llm_breaker_failures_total reasons = the nine §4 classes; llm_resumed_generations_total resumed|expired|duplicate_suppressed; llm_admission_rejections_total capacity|timeout; lane normal|long; to CLOSED|OPEN|HALF_OPEN; engine main). llm_engine_state_code = -1 when unreachable/stale; MONITORING_UNKNOWN never opens the breaker (engine_state.external_open); STARTING opens only when the document says the head is really starting (a documented qualification of §8.2). Terminal frame: main.py:3241-3243 publishes SSE event `error` with {"message": EXPIRED_LINE, "code": "MODEL_RECOVERING", "resumable": true} - matches §8.3 step 5. Three findings were reproduced with scratch scripts against the worktree modules (sibling-call expire, stream dying before first token during RECOVERING, READY-edge sweep dropped at start-up).
- contract: §8.3 admission sentence: NORMAL lane says 'Waiting for the model to finish current work (N ahead).' (admission.py:71) instead of the contract's verbatim 'Waiting for a free slot on the main model (N ahead).' (CONTRACT.md line 452).
- contract: §8.3 'never fails with a generic error' during STARTING/WEDGED/RECOVERING/DOWN: a streamed answer whose body dies before its first token (head restarted under an open stream) ends the turn with MODEL_UNAVAILABLE instead of being queued/resumed (resilience.py:663 GuardedStream._fail; llm.py _open_stream returns before the first chunk).
- contract: §7.2 llm_queued_generations / llm_queue_wait_seconds: admission (lane) waits are counted as generations waiting for the primary (continuity.py:193, 226); the contract gives lane waits their own llm_admission_* series.
- contract: §8.4 'after the first token ... the partial text is kept as partial, and no second answer is appended' vs the READY sweep: interrupted rows that streamed tokens are re-run and the viewer's partial (meta.error) is deleted (continuity.py:425); the browser's own re-attach of an interrupted row appends a second answer beside the kept partial (main.py _retry_chat_request discards records only for `failed`). The contract's §8.4 v2 sweep sentence and its retry rule need reconciling.
- contract: §8.4 'exactly once (and at orchestrator start-up)': the start-up sweep lists only `queued` rows and the READY-edge sweep that would cover `interrupted` rows is dropped while the start-up sweep is in flight (continuity.py:486); a hold cancelled by orderly shutdown is rewritten `interrupted` (main.py _settle_chat_request) and so misses the start-up sweep.
- contract: §8.3 step 1 'the logical generation_id ... fixed for its whole life': the in-process resume keeps the generation_id, but the sweep/re-attach path mints a new generation_id per attempt (test_continuity.py:391 pins `row['generation_id'] != first_generation`); intent_id is the stable id in practice - a wording deviation the contract should state.
- contract: §8.2 'immediately when the controller reports ... STARTING': engine_state.external_open opens on STARTING only when the document also shows an incident, a non-running/dead head, or a head started within 900 s (STARTING_HEAD_AGE_S); a documented, defensible qualification (controller restarts would otherwise queue every chat) but not the contract's text.
- contract: §8.2 'while OPEN, no caller polls the dead port': context.fit_request POSTs /tokenize to the main port before every main call regardless of breaker state (llm.py:406/457/503; context.py count_tokens).

### [major] orchestrator/app/resilience.py:549
_give_up expires the turn's shared Hold from ANY main-URL call in the task tree, so a window-0 sidecar call (router on the main URL) that gives up while the answer task is legitimately waiting at the breaker gate falsely parks the turn: EXPIRED_LINE is said, gen.parked is set, the queued gauge drops, and the answer then completes with the row left `queued` and the ledger saying `interrupted`.

*Scenario:* ROUTER_BASE_URL == OPENAI_BASE_URL (launcher router_fallback after a failed router probe - the RestartCount incident produced exactly this; router_shared profiles; the external profile in .env.example:747). Controller reports RECOVERING -> breaker held OPEN. A chat turn's answer call is in _admit with hold.waiting=True and QUEUED_LINE shown. The concurrent fact_task (facts.remember_after_route -> llm.router_chat_completion, recovery_s=sidecar_recovery_s()=0) hits _admit on the MAIN breaker, remaining<=0, _give_up(hold) sees hold.waiting -> hold.expire(): user reads EXPIRED_LINE (false), llm_resumed_generations_total{expired}++, gen.parked=True, hold cleared; QueuedForRecovery is swallowed by facts. The answer task re-enters the hold (QUEUED_LINE said a second time; park_chat_request returns None because the row is already `queued`), later resumes and streams the answer with `done`, but _settle_chat_request returns early on gen.parked so the row stays `queued` with a durable answer until a later sweep heals it as duplicate_suppressed, and _attempt_record writes terminal_state=interrupted for a completed answer. Reproduced: scratchpad/repro_sibling_expire.py prints lines [QUEUED_LINE, EXPIRED_LINE, QUEUED_LINE], gen.parked=True while the answer task is still waiting.

*Fix:* Make hold ownership explicit: Hold.enter() returns a token / records the owning task, and only the call that entered may expire(); in resilient(), a call with an explicit recovery_s of 0 (sidecar) must get hold=None (never enter or expire the turn's hold) - e.g. `hold = _hold_for(brk.engine) if brk is not None and window > 0 else None` and have _give_up raise ModelUnavailable when the caller did not enter the hold. Add a test with two sibling tasks on the MAIN breaker (one queued, one window-0) asserting the queued one is untouched.

### [major] orchestrator/app/resilience.py:663
A streamed answer whose body dies BEFORE its first token (the head restarted under an open stream, or the dying-stream error chunk) is neither retried nor queued: GuardedStream._fail only records the failure and re-raises, resilient() has already returned, so the chat turn ends with the generic MODEL_UNAVAILABLE failure while the controller reports RECOVERING - contrary to §8.3 (never a generic error during a recovery) and §8.4 (retry allowed before the first token).

*Scenario:* 2026-09-11 shape: N chats open streams against a head whose worker rank just died; vLLM sends headers, no token. The controller confirms WEDGED/RECOVERING and restarts the pair; every open stream raises httpx.ReadError/RemoteProtocolError from its first __anext__. main.py's `except Exception` -> _failure_sentence -> MODEL_UNAVAILABLE, gen.failed, row `failed` with a failure record and a Retry button; nothing resumes these requests automatically when READY arrives (the sweep lists only queued/interrupted). Reproduced: scratchpad/repro_stream_dies_before_token.py - breaker OPEN (controller_RECOVERING), the turn sees ReadError, hold.waiting=False, gen.parked=False, no sentence shown.

*Fix:* Extend the 'retry the open' rule to the first chunk: in llm._open_stream / _primary_send(stream=True), pull the first chunk inside the resilient attempt (op opens the stream and awaits its first chunk, returning a stream that replays it), so a recoverable failure before any token goes back through _resilient's exception path -> breaker gate -> hold.enter (queued, QUEUED_LINE) -> same-generation resume. A failure after the first token stays a terminal, non-retried error as today. Add a test: stream dies before first chunk while the verdict is RECOVERING -> the turn is queued and resumed on READY, one answer.

### [major] orchestrator/app/continuity.py:486
_schedule_sweep drops the READY-edge sweep whenever the start-up sweep is still in flight, and the start-up sweep lists `queued` rows only, so after an orchestrator restart with a healthy engine the `interrupted` rows (including holds that an orderly shutdown re-labelled `interrupted` via _settle_chat_request) are not resumed until the next not-serving -> serving edge, which may be days away.

*Scenario:* Deploy restarts the orchestrator mid-answers. lifespan: interrupt_open_chat_requests marks the running rows `interrupted`; continuity.start() creates the start-up sweep task; engine_state.start() creates the poller. The sweep awaits db.run_in_thread(list_resumable_chat_requests) (a real query, ms-to-seconds with rows to resume); the poller's first GET returns READY during that await -> _publish_event fires _on_ready -> _schedule_sweep('ready') -> `_sweep_task not done` -> return. No sweep with statuses ('queued','interrupted') runs; the lifespan comment ('a healthy engine at start-up resumes the interrupted rows too') does not hold. Rows wait for a browser re-attach (a closed tab never comes). Reproduced: scratchpad/repro_ready_sweep_dropped.py -> sweeps that ran: [('queued',)], READY sweep with 'interrupted' ran: False. Same drop applies to a queued row whose worker was cancelled at shutdown: _settle_chat_request writes `interrupted` (gen.cancelled and _shutting_down), which the start-up sweep does not list.

*Fix:* Never drop a trigger: keep a `_pending` trigger while a sweep is in flight and run it when the current one finishes (or serialise on _sweep_lock instead of returning early); and let the start-up sweep include `interrupted` when engine_state.serving() is True at that moment. Also leave a hold's `queued` row as `queued` on orderly shutdown (skip the interrupted rewrite when gen.request_status == 'queued'). Test: start-up with an in-flight sweep and an immediate READY -> the interrupted row is resumed once.

### [major] orchestrator/app/admission.py:71
NORMAL_LINE is 'Waiting for the model to finish current work ({n} ahead).' but CONTRACT §8.3 (line 452) fixes the NORMAL-lane sentence verbatim as 'Waiting for a free slot on the main model (N ahead).'; the tests pin the wrong sentence (test_admission.py:143,147,283,450; test_continuity.py:564).

*Scenario:* Eleven concurrent chats: the eleventh waits for the NORMAL lane and reads a sentence that the contract, the runbook and the client copy do not know; anyone verifying the contract's sentences against production output (or a client keying on the text) sees a mismatch.

*Fix:* Set NORMAL_LINE = 'Waiting for a free slot on the main model ({n} ahead).' and update the five test assertions; or, if the lead prefers the code's wording, change CONTRACT.md line 452 first (the contract is the source of truth by its own rule).

### [major] orchestrator/app/continuity.py:193
Hold.enter increments `llm_queued_generations` (and Hold.resume observes `llm_queue_wait_seconds`, line 226) for ADMISSION waits as well as RECOVERY waits, so lane waits in front of a healthy engine are reported as generations queued for a recovering primary - §7.2 defines llm_queued_generations as 'accepted generations waiting for the primary' and gives lane waits their own llm_admission_waiting{lane}.

*Scenario:* The engine is READY and ten chats are streaming; an eleventh waits in the NORMAL lane (up to ADMISSION_NORMAL_WAIT_S=600 s). llm_queued_generations=1 for the whole wait: VllmRequestsQueued (llm_queued_generations > 0 for 2 m) fires while nothing is recovering; with the controller in DEGRADED (e.g. router unhealthy, state 4) the derived cluster:vllm_service_state:code becomes 7 QUEUEING ('primary not READY') on a serving engine; llm_queue_wait_seconds (histogram of 'seconds a generation waited for READY') records lane waits.

*Fix:* Count only kind == RECOVERY in _queued / _publish_queued() and observe llm_queue_wait_seconds only for RECOVERY waits; lane waits are already covered by llm_admission_waiting{lane} and llm_admission_wait_seconds{lane}. Adjust test_the_hold_speaks_once_per_wait_and_the_gauge_follows if it asserts the gauge for an admission wait.

### [major] orchestrator/app/continuity.py:425
The READY sweep resumes `interrupted` rows without checking whether the interrupted attempt had already streamed tokens: it deletes the viewer's persisted partial (meta.error) and runs a whole new attempt, contrary to §8.4 'after the first token the attempt is marked interrupted, the partial text is kept as partial, and no second answer is appended' (the contract's own v2 sweep sentence is in tension with that rule; the code resolves it toward re-running).

*Scenario:* A long answer is 2,000 tokens in when the orchestrator is restarted (deploy). The tab's markInterrupted persists the partial with meta.error; the row is `interrupted` (ledger terminal_state=interrupted, streamed). On the next READY edge _resume_row: _is_answer(stored) is False because meta has `error`, delete_failure_record removes the partial, main.chat() re-runs the full generation - the person's partial disappears, GPU time is spent twice, and a different answer replaces the text they had already read. (Via the browser's own re-attach, _retry_chat_request discards the record only for `failed` rows, so there the partial stays AND a second answer is appended - the other half of the same rule.)

*Fix:* Decide the rule once in CONTRACT §8.4 and implement it: either exclude from the sweep any interrupted row whose generation has a persisted assistant row (partial with meta.error) - treat it like a `failed` attempt that only a person may retry - or, if re-running is intended, say so in §8.4 and keep the partial instead of deleting it. Add a durability test for 'interrupted after first token' on the READY sweep.

### [minor] orchestrator/app/main.py:3242
The MODEL_RECOVERING terminal frame always says resumable: true and EXPIRED_LINE always promises 'will resume automatically', even when the server cannot resume the row: no intent_id (bare API caller, hold.enter parks nothing) or a snapshot marked resumable=false (inline image/PDF bytes), which list_resumable_chat_requests never returns.

*Scenario:* A chat with an inline image is queued through a long recovery, expires after 900 s, the person reads 'Your request is kept and will resume automatically', closes the tab; the row stays `queued` forever - the sweep filters `resumable`, and no browser re-sends the bytes.

*Fix:* Set resumable to bool(gen.intent_id) and the row's `resumable` flag (hold.row_parked and the snapshot's resumable), and for a non-resumable row end with the ordinary failure sentence/Retry instead of the parked frame (or keep the frame but with resumable:false so the client offers Retry).

### [minor] orchestrator/app/llm.py:406
context.fit_request / count_tokens runs before _primary_send on every main-model call and POSTs /tokenize to the engine's port regardless of the breaker state, so an OPEN breaker does not stop this process from touching the dead/wedged port (§8.2 'while OPEN, no caller polls the dead port'); the call fails fast to an estimate, so the effect is log noise and a connect attempt per queued turn, not a hang.

*Scenario:* During a recovery every queued chat turn (and the sweep's resumes) issues a /tokenize connect to vllm:8000 before reaching the gate; with the head restarting these are refused quickly, but during a wedge the API answers them, so the process keeps talking to the port the breaker declared dead.

*Fix:* Skip the exact count when breaker.main_allows() is False (use the estimate) or move the sizing inside the admitted attempt as admission.prompt_tokens already does.

### [minor] orchestrator/app/health.py:764
The main model's /health check keeps status 'ok' (from /health + /v1/models 200) while the controller reports WEDGED/RECOVERING/DOWN; the truth is only in the new nested `engine` block, so any reader of checks.vllm.status alone reads a wedge as healthy - the very '/v1/models 200 proves nothing' rule of §2.

*Scenario:* The 2026-09-11 wedge: orchestrator /health returns checks.vllm.status='ok' for five minutes while engine.controller.state='WEDGED' and breakers.main.state='OPEN'; dashboards/blackbox probes keyed on the old field show green.

*Fix:* Add a top-level `engine_state`/`answer_engine_ready` field next to `status` (not changing `status`, which gates the container healthcheck) and document that checks.vllm.status is reachability only; or set checks.vllm.status='degraded' with the reason when the breaker is OPEN/held.

### [minor] orchestrator/app/continuity.py:398
_resume_row checks the conversation for a live generation, then awaits three thread hops before calling main.chat(); a person's new send in that conversation landing in the gap is cancelled by chat()'s 'newest message wins' replacement in favour of the old parked row.

*Scenario:* READY edge; the sweep is resuming a parked row for conversation C; the owner sends a new message in C during the ~ms between the busy check and main.chat(); chat() sees `previous` (the person's live generation, same viewer) and cancels it with 'This answer was replaced by a newer message' while the older parked question runs.

*Fix:* Pass a flag through the resume path (or re-check _live_generations right before the replacement block when the caller is the sweep) so a sweep never replaces a live generation - skip and count the row as skipped instead.

### [minor] orchestrator/app/continuity.py:117
llm_queued_generations is an in-memory count of holds in this process; rows parked in the database (expired holds, a dead process) are not counted, so the derived QUEUEING state and VllmRequestsQueued/VllmQueueDrained lose sight of durably queued requests exactly when the recovery is long (DOWN, budget exhausted) or after an orchestrator restart. The durable count exists as `chat_requests_queued` but is outside the contract's rules.

*Scenario:* Recovery exceeds 900 s: every hold expires, llm_queued_generations drops to 0 and the top-level state flips from QUEUEING to DOWN/RECOVERING with 'no requests queued' although 30 rows sit `queued` in chat_requests; VllmQueueDrained fires with nothing drained.

*Fix:* Either publish llm_queued_generations as max(in-memory holds, durable `queued` rows) from the periodic work gauge, or add `chat_requests_queued` to CONTRACT §7.2/§7.3 and use it in the QUEUEING derivation and the queue alerts.

## controller / correctness
- contract: §2 DEGRADED is defined as 'canary still succeeds but a non-critical signal failed'; controller.py:2247 publishes DEGRADED for a proven engine whose canary has failed with http_error for an unbounded time (finding 1) — the orchestrator treats it as SERVING.
- contract: §6.3 trigger 1(c) requires rank_process_alive==false to count 'only after the rank was seen alive for this head incarnation'; controller.py:1530 has no such gate for the worker rank (finding 3).
- contract: §6.3 rule 5 says a canary timeout is counted 'only if /health answered 200 when that probe started'; controller.py:1375-1386 also counts probes whose /health was silent (None). Deliberate (the hung-API shape, tested at test_controller_recovery.py::test_hung_api_is_confirmed_by_two_canary_timeouts_regardless_of_health) but the contract text was not updated.
- contract: §6.2 / §7.1 ('Change it here first; nowhere else'): the published /state carries keys absent from the contract (readiness, signals.head_memory, cold_start_detail, cold_start_seconds, recovery_duration_seconds, recovery.{blocked,blocked_detail,manual_pending,worker_restart,verify_successes,steps,attempt,incident_id}, single_node, dry_run) and /metrics exposes techsara_vllm_head_mem_available_bytes and techsara_vllm_recovery_worker_restart{outcome} which §7.1 does not list. Additive and parsed fine by the orchestrator, but CONTRACT.md is no longer the source of truth for the document shape.
- contract: §5 canary interval: the fast 10 s interval is also used while DEGRADED with consecutive failures and while unproven (controller.py:1445-1452); a benign extension of '10 s while STARTING/RECOVERING/WEDGED' that the contract does not mention.

### [major] monitoring/engine-controller/controller.py:1590
No detection rule for canary_http_error: a proven engine whose every completion fails fast (HTTP 5xx, error chunk, stream without terminal) while /health, /v1/models and /metrics answer 200 is never confirmed, never DOWN, never recovered; _evaluate (line 2247) publishes DEGRADED 'awaiting confirmation' forever, and DEGRADED is in the orchestrator's SERVING set (orchestrator/app/engine_state.py:87).

*Scenario:* world.make_ready(); world.head.completion_status = 500; 12 probes over 372 s of controller clock -> state DEGRADED, consecutive_failures 12, confirmed None, pending {}, head_restarts 0, last_success_at 372 s old. Orchestrator serving()==True so the breaker is not externally opened and nothing is queued ahead; only the orchestrator's own 5xx failures open it, then callers wait for a READY that never comes (LLM_QUEUE_MAX_WAIT_S expires with 'still recovering'). No alert fires: VllmReadyUnproven only covers states 2/3. Reproduced by /tmp/claude-1000/ctl-review/test_review_canary_http_error_never_confirms.py (1 failed, exit 1). Contract §2 defines DEGRADED as 'canary STILL SUCCEEDS'.

*Fix:* Add a consecutive_http_errors counter (incremented at controller.py:1388 for outcome http_error on a proven engine with /health not 5xx, reset on success) and a _RULES entry ('canary_http_error','canary_http_error',1,0.0) confirmed at >=3 consecutive errors spanning >= 2*canary_interval_s, mapped in CONFIRMED_STATE to DOWN (the §4 category already exists). At minimum, once now - last_success_at > 2*canary_interval_s stop publishing DEGRADED (a SERVING state) for an engine with zero recent completions, and add an alert on techsara_vllm_synthetic_success == 0 for 5m irrespective of state.

### [major] monitoring/engine-controller/controller.py:725
The readiness sequence accepts zero-token completions: run_non_stream (line 535) and run_canary (line 485) return ok=True for any terminal marker even with tokens==0, so expected becomes 0 and step 3 degenerates to tokens_delta >= 0; the head is marked READY without having generated a single token. The routine canary with 0 tokens (line 1357) only warns and refreshes last_success_at.

*Scenario:* An engine that terminates every request without output (finish_reason 'abort'/'stop' with empty content, or a stream carrying only [DONE]) passes all readiness steps. Reproduced with the fixture: world.head.short_by = 32; world.settle() -> state READY, readiness.passed True, tokens_expected 0, tokens_delta 0.0, last_canary tokens 0 ok True (see /tmp/claude-1000/ctl-review/probe_scenarios.py output B). Contract §5 v2 step 3 ('increased by >= the tokens received') is vacuous at 0 and the orchestrator resumes every queued generation against an engine that emits nothing.

*Fix:* In run_canary/run_non_stream return ok=False with outcome http_error and detail 'no tokens' when ignore_eos is set and tokens < max_tokens (or at least when tokens == 0); in run_readiness_sequence require expected > 0 before step 3 and fail the step 'progress' otherwise; treat a 0-token routine canary as a failure rather than a warning.

### [minor] monitoring/engine-controller/controller.py:1530
Trigger 1(c) restarts a proven pair after WORKER_RANK_GRACE_S on any sentinel rank_process_alive:false with no 'seen alive for this head incarnation' gate; the head-side rank process has that gate (lines 1197-1215), the worker side does not, contrary to contract §6.3 (c).

*Scenario:* The pair is proven while the sentinel is unreachable (readiness step 5 records rank_alive None and passes); the sentinel then comes up on a vLLM build whose worker title is no longer 'VLLM::Worker*' (or docker top lists a different column) and reports rank_process_alive:false -> after 120 s worker_rank_dead -> STOP_STALE_PAIR on a healthy engine; the new head can then never be proven because _record_readiness refuses rank_alive False -> cold_start_timeout -> DOWN with one budget slot burned per cycle.

*Fix:* Track worker_rank_seen_alive per head incarnation (set when the sentinel reports rank_process_alive True after head.started_at, reset in _new_head_incarnation) and require it in trigger 1(c) and in the _record_readiness rank_alive refusal; a name never observed alive for this incarnation is 'unknown', not 'absent'.

### [minor] monitoring/engine-controller/controller.py:1525
Trigger 1(a) compares the worker container's started_at (Node 2 Docker clock) with last_success_at (Node 1 clock); cross-host clock skew larger than the head's load time turns every successful recovery into a fresh worker_rank_dead confirmation.

*Scenario:* NTP fails on Node 2 and its clock runs > ~5 min ahead. A recovery restarts the worker at ws (Node 2 time), the head loads for 5 min, readiness passes at P (Node 1 time) < ws -> 'worker container started Ns after the last proven completion' -> RECOVERING again immediately after MARK_READY, three times, then budget_exhausted -> DOWN on a healthy pair (and with Node 2 behind, a real worker restart right after a canary success is missed by (a) and only caught ~130 s later by canary_timeout).

*Fix:* Measure the sentinel skew per read (sentinel observed_at minus the controller's read time) and shift ws by it before the comparison, or decide (a) on sentinel-native facts (restart_count or last_restart_at moved since the proof was harvested) rather than on two clocks.

### [minor] monitoring/engine-controller/controller.py:2394
/state and /metrics serve a snapshot whose generated_at is frozen for the whole duration of the blocking choreography calls (sentinel POST /restart up to 125 s, docker restart up to 70 s plus re-inspects, diagnostics up to 55 s), so §7.3's 30 s freshness rule derives MONITORING_UNKNOWN and §8.1 reads 'unknown' in the middle of RECOVERING.

*Scenario:* During STOP_STALE_PAIR (_step at line 1878 publishes once, then _stop_stale_pair blocks at 1949-1980) a normal recovery leaves generated_at 20-30 s stale and a slow one up to ~190 s; cluster:vllm_service_state:code drops its sample and Grafana shows MONITORING_UNKNOWN for a recovery the controller is actively running; llm_engine_state_code reads -1.

*Fix:* Stamp generated_at at serve time in snapshot()/metrics_text() and carry the tick's time as observed_at (the freshness rule then measures the controller's liveness, not a step's length), or run the blocking calls on a worker thread and keep publishing heartbeats from tick().

### [minor] monitoring/engine-controller/controller.py:1164
A Docker API error other than 404 (e.g. 400 'client version 1.53 is too new' on an older daemon, or 500) is logged at DEBUG only; the controller then sits in MONITORING_UNKNOWN/DEGRADED for good and can never recover anything, with docker_error:'api_400' in /state as the only clue.

*Scenario:* The daemon is downgraded or the pinned API version is raised: every inspect returns 400, docker_ok False, no trigger can fire (c2p/c3 need docker_ok), no restart ever happens, and nothing at INFO/WARNING says why.

*Fix:* Log at WARNING on every change of docker_error kind (and once per minute while it persists), and negotiate the API version from GET /_ping's API-Version header at startup with the pin as a ceiling.

### [minor] monitoring/engine-controller/controller.py:1983
_advance_recovery handles only the wait_load and canary steps and deadline_mono is set only when wait_load begins; an unexpected exception raised inside a tick between _try_lock (line 1662) and _step('wait_load') is swallowed by run_periodically and leaves rec.in_progress True at step capture/stop_pair with the flock held and state RECOVERING with no deadline.

*Scenario:* Any non-Docker/non-Connect exception inside _capture_diagnostics or _stop_stale_pair (none reachable was identified today; e.g. a future non-JSON-serialisable value in the snapshot at line 1911) -> the controller publishes RECOVERING forever, the orchestrator queues every request forever, cluster-recover.sh cannot take the lock, and only a controller restart clears it.

*Fix:* Set deadline_mono at _start_recovery and make _advance_recovery fail the attempt (cold_start_timeout, lock released) from any step once it passes; wrap the step body of _start_recovery in try/except that calls _fail_attempt.

## orchestrator / correctness (skeptical review of continuity/admission/engine_state/breaker + llm/resilience/main/db V33/config/health/deep_research diffs on feat/vllm-availability-2026-09-12 rebased on b2a5ef4)
- contract: §8.3 steps 2-5 ('emits exactly one status line', 'waits … up to LLM_QUEUE_MAX_WAIT_S', 'if the wait expires, the row stays queued'): after Hold.expire()/abandon() the same turn re-enters the hold and waits a second window with the lines repeated (best-of-N, Deep Research); Deep Research completes with an empty report instead of parking (deep_research.py:754/3154/833/1370/1832, best_of.py:74, continuity.py:187/307).
- contract: §8.4 v2 / §6.3 RESUME_QUEUED ('when the controller reports READY … rows in queued/interrupted … are resumed exactly once'): the READY edge is missed after an unknown/MONITORING_UNKNOWN gap (engine_state.py:349-357) and the READY sweep is dropped when the startup sweep is in flight (continuity.py:486-488).
- contract: §2 DOWN / §6.3 budget-exhausted ('accepted requests stay queued (never lost) until an operator acts'): rows parked more than LLM_RESUME_MAX_AGE_S=3600 s before READY are never resumed and never made terminal (continuity.py:84-89, db.py:5934-5945).
- contract: §8.3 v2 ('a user request … never fails with a generic error' while the breaker is OPEN): a stream that dies before its first chunk fails the turn with MODEL_UNAVAILABLE (resilience.py:663-673 → main.py:3272), and post-recovery lane `capacity` refusals fail resumed requests with the TIMEOUT sentence (admission.py:129-132).
- contract: §8.2 text vs code: breaker.py:89 says continuity wakes queued callers through breaker.on_transition — no listener is registered anywhere (grep on_transition( → only the definition).
- contract: §6.7 (LONG lane): the NORMAL lane is held closed for the entire ≤600 s idle wait, not only 'until its first token'; with the documented second tenant at c=10 the idle condition is unreachable and NORMAL waiters are refused with `timeout` (admission.py:296-316, 143-147).

### [blocker] orchestrator/app/engines/deep_research.py:3154
A Deep Research turn started during a recovery never parks: the run's own stage budgets cancel the queued wait first (planning via _bounded/wait_for at :754, the report via wait_for(report_budget_s) at :3154), the run 'completes' with an empty report plus the 'reached its time budget' note, and the row is written `completed` — the person's research request is lost, and the `except QueuedForRecovery` added in caa97e9 (:3286) is unreachable with the default DEEP_RESEARCH_TIMEOUT_S=600 (gather 450 s + report reserve 150 s < LLM_QUEUE_MAX_WAIT_S=900).

*Scenario:* Controller reports RECOVERING (breaker held OPEN). User sends a Deep Research question. _plan → json_completion → resilience._admit → hold.enter (row `queued`, QUEUED_LINE). At ~450 s _bounded's wait_for cancels the coroutine → resilient() sees CancelledError → hold.abandon() (row stays `queued`, hold re-enterable). _gather_bounded is skipped (gather_left<=0), budget_left() false → break. Write stage → stream_long_completion → _admit → hold.enter AGAIN (QUEUED_LINE #2); at 150 s the report wait_for cancels → TimeoutError → report_cut_short → report = '' + '[the run reached its time budget…]' → _close_run(…,'done') → returns → worker completes; _settle_chat_request sees parked=False, request_status 'queued' ≠ 'completed' → row `completed`; _store_answer persists the note. Nothing ever resumes it. Verified by code reading (the shared re-entry mechanism is demonstrated by /tmp/claude-1000/test_review_hold_reenters_after_expiry.py).

*Fix:* Make a held wait un-cancellable-by-budget or make the budgets exclude queued time: e.g. in Hold.enter/abandon record that the turn is parked and have resilience._admit raise QueuedForRecovery immediately on the NEXT call of a turn whose hold was abandoned/expired while the engine is still not serving; in deep_research, check `continuity.current()` state (or catch the abandon) after each _bounded stage and re-raise QueuedForRecovery instead of writing an empty report; alternatively derive QueuedForRecovery from BaseException so `except Exception` stages cannot swallow it.

### [major] orchestrator/app/continuity.py:187
After Hold.expire() the hold is re-enterable (kind=None, `active` still True because the row says `queued`, `expired` never consulted by enter()/current()), and QueuedForRecovery is a plain RuntimeError swallowed by every 'upgrade, never a gate' handler on the chat path (core/best_of.py:74, deep_research.py:833/1370/1832), so a turn waits a SECOND full LLM_QUEUE_MAX_WAIT_S and the person reads QUEUED_LINE and EXPIRED_LINE twice — 30 min and four lines instead of 15 min and two.

*Scenario:* RECOVERING; user sends at effort=max (EXTRA_HIGH_SAMPLES=3 default → best-of-N). Candidates queue on the one hold; at 900 s candidate 1's _give_up → hold.expire() (EXPIRED_LINE, parked=True, _clear) raises QueuedForRecovery → caught by _generate_one's `except Exception` → error candidate; others get ModelUnavailable (hold no longer waiting) → also swallowed. engines/chat.py falls through to stream_long_completion → _open_stream → _admit → continuity.current() returns the same hold (request_status 'queued' ⇒ active) → enter() (kind is None) → QUEUED_LINE #2 → 900 s → EXPIRED_LINE #2 → only now parked. Reproduced: /tmp/claude-1000/test_review_hold_reenters_after_expiry.py fails on the committed tree with said == [QUEUED, EXPIRED, QUEUED, EXPIRED] and expired counter == 2.

*Fix:* In Hold: keep `expired`/`parked` sticky and make current() (or _admit) raise QueuedForRecovery at once for any further main-model call of a parked turn; and either subclass BaseException for QueuedForRecovery or add explicit `except QueuedForRecovery: raise` before the broad handlers in best_of._generate_one and the deep_research stages.

### [major] orchestrator/app/engine_state.py:356
The READY edge that triggers the resume sweep is computed only from fresh SERVING/OPENS_BREAKER snapshots; `_was_serving` is never reset on an unreachable/stale controller or on MONITORING_UNKNOWN, so READY → (controller unreachable or MONITORING_UNKNOWN while the engine is dead, breaker opened by observed failures, rows parked) → READY fires no edge and the parked/interrupted rows are not resumed until the next genuine not-serving→serving transition or an orchestrator restart.

*Scenario:* Engine dies while the controller is being redeployed/crash-looping (or it reports MONITORING_UNKNOWN because the Docker API is unavailable). Orchestrator's breaker opens on 3 connection failures; chat turns park after 900 s (rows `queued`). Controller returns and reports READY: _publish_event computes edge = True and not _was_serving(True) = False → no continuity._on_ready → no sweep. Probe run in this review: after READY, three _record(None,'ConnectError') polls, then READY → listener fired once (expected twice); READY→MONITORING_UNKNOWN→READY → fired once. Rows stay `queued` with `chat_requests_queued`>0 while state is READY; closed tabs never get their answer.

*Fix:* Treat any non-serving observation (unknown/stale/MONITORING_UNKNOWN, and the breaker's own OPEN) as clearing _was_serving so the next fresh SERVING snapshot is an edge; additionally register a breaker.on_transition listener (the hook exists but nobody registers it) that schedules the sweep on OPEN/HALF_OPEN → CLOSED, and/or run a periodic low-frequency sweep while chat_requests_queued > 0 and serving() is True.

### [major] orchestrator/app/continuity.py:486
_schedule_sweep drops a READY-triggered sweep (queued+interrupted) whenever a sweep is already in flight; at start-up the 'startup' sweep (queued only) is scheduled before engine_state.start(), so if it has any row to resume the poller's first READY lands while it runs and the interrupted rows are never swept — contradicting main.py's lifespan comment that the first READY is an edge the sweep sees.

*Scenario:* Orchestrator is redeployed while one turn waits in the NORMAL lane (row `queued` via the admission park) and others are mid-answer (marked `interrupted` at shutdown). New process: continuity.start() schedules the startup sweep; it lists rows and calls main.chat() for the queued row (several DB round trips). engine_state's first poll returns READY meanwhile → _on_ready → _schedule_sweep('ready') → `_sweep_task is not None and not done()` → return. Probe in this review (fake sweep taking 50 ms, verdict READY after the task started): sweeps that ran == ['startup'], 'ready' lost. The interrupted rows wait for the next genuine edge or a browser re-attach.

*Fix:* Coalesce instead of dropping: remember a pending trigger (and its statuses) and run it after the in-flight sweep finishes (e.g. a pending flag checked in a loop under _sweep_lock), or make the startup sweep include 'interrupted' when serving() is already True.

### [major] orchestrator/app/continuity.py:84
LLM_RESUME_MAX_AGE_S=3600 is measured from the row's updated_at, which park_chat_request stamps at the START of the wait, so any outage longer than ~60 min (a DOWN state waiting for an operator, a bad recovery night) leaves parked rows `queued` forever: never resumed by the sweep, never marked terminal, `chat_requests_queued` > 0 permanently and the browser's /chat/requests poll sees `queued` indefinitely — CONTRACT §2 DOWN / §6.3 promise 'never lost'.

*Scenario:* 22:00 recovery budget exhausted → DOWN. Users' turns park at 22:00–22:15 (rows queued, updated_at ≈ 22:00). Operator fixes the pair at 23:30 → READY edge → resume_sweep lists rows with updated_at > now-3600 → none → the requests are silently abandoned; rows stay `queued`; the frontend keeps polling a row that will never move.

*Fix:* Either drop the age filter for `queued` rows (they are explicit promises) or, when a row is too old, move it to a terminal status with a truthful error the client can render (e.g. `failed` + 'not resumed after N hours') so nothing is left in limbo; stamp updated_at at expiry rather than at park if the age must count from when the person last heard.

### [major] orchestrator/app/continuity.py:398
Race: _resume_row checks that no newer live generation exists for the conversation, then awaits several DB round trips and main.chat(); main.chat()'s 'newest message wins' block (main.py:1775-1783) has no such check for a sweep caller, so a user message that arrives in that window is CANCELLED by the sweep's resume of the older parked question ('This answer was replaced by a newer message'), and the old question runs instead.

*Scenario:* READY edge; sweep picks row R (conversation C, parked 10 min ago). After the busy check passes, the user (who just saw the queued state end) sends a new message in C → its generation registers in _live_generations[C]. The sweep's main.chat() wins the CAS on R, then finds previous=user's gen (same user, not done) → previous.replaced=True; previous.task.cancel() → the user's new request ends with the 'replaced' error frame and row `cancelled`; the stale question is answered instead.

*Fix:* Pass a flag (e.g. via the synthetic request state) so main.chat() never replaces a live generation on behalf of the sweep — re-check `_live_generations.get(conv_key)` and return 'skipped' (leave the row queued) if a newer generation is live; or perform the busy check and the CAS atomically right before registering.

### [major] orchestrator/app/admission.py:296
A LONG request closes the NORMAL lane for the whole idle wait (up to ADMISSION_LONG_WAIT_S=600 s) even when idle can never be reached (the second tenant's raw-port traffic at concurrency 10 keeps requests_running ≥ 1), while NORMAL waiters are refused after ADMISSION_NORMAL_WAIT_S=600 s (`timeout`) or at 100 deep (`capacity`) with the TIMEOUT sentence — one large-document upload turns into up to 10 minutes of no chat answers plus failures for everyone.

*Scenario:* Interview-analysis tenant running its batch (requests_running ≈ 10 continuously). A user uploads a 400-page PDF (>131k tokens) → LONG lane → normal.set_closed(True), drain, _wait_for_idle polls engine_load() every second until deadline. All other users' turns enter Lane.acquire with closed=True: told 'Waiting for the model to finish current work (N ahead)', rows `queued`; those that hit 600 s raise AdmissionRejected('timeout') → main.py 'The model is busy… try again' TIMEOUT, row `failed`; the 101st is refused at once. A second LONG request waits in long.acquire for the first's entire decode too.

*Fix:* Bound the idle wait separately and short (e.g. ADMISSION_LONG_IDLE_WAIT_S ≈ 30–60 s, or give up on idle when requests_running is stable > 0 for N samples), keep NORMAL closed only during the drain + prefill (not the idle wait), and never refuse NORMAL waiters with 'timeout' while the closure is caused by a LONG request (reset their clocks or exclude closed time).

### [major] orchestrator/app/resilience.py:663
A stream that dies BEFORE its first chunk (the 2026-09-11 shape: 200 + headers, then an error chunk or transport error when the rank is dead) settles the breaker correctly but is not retried/queued: the exception escapes GuardedStream into the chat worker's generic handler (main.py:3272) → MODEL_UNAVAILABLE, row `failed` — even though nothing was shown to the person and §8.4 allows a retry before the first token; when that stream was the HALF_OPEN canary the breaker re-opens and everyone else stays queued while this one user is failed.

*Scenario:* Controller unreachable (breaker works from observed failures). Breaker HALF_OPEN after cooldown; user U's stream is the canary; open succeeds (headers), first __anext__ raises openai.APIError('… worker … died unexpectedly') → GuardedStream._fail → record_failure('worker_lost', permit) → OPEN. The exception propagates through stream_chat_events/continuation (index 0, nothing produced → re-raised) → worker except Exception → 'The model is temporarily unavailable… try again', row failed, failure record persisted. Other queued users keep waiting and later resume; U must press Retry.

*Fix:* In GuardedStream (or in _resilient for stream=True) treat a recoverable failure before the first chunk as a retryable open: re-enter the retry/gate loop (hold.enter) instead of raising, since no token was forwarded; or have the chat worker convert a pre-first-token recoverable failure into the queued path when the breaker is now OPEN.

### [minor] orchestrator/app/continuity.py:239
Hold.resume() does not honour a lost lease: when resume_queued_chat_request returns None (the row was taken over under another generation_id) it bumps gen.attempt in memory and lets the generation run anyway — with two orchestrator processes (rolling overlap or a future replica) process B's startup sweep can resume a row process A still holds, and A produces a second answer under its own generation_id (different id ⇒ V29 dedupe does not catch it). Unreachable with today's single-worker container; reported because the lead asked about two processes.

*Scenario:* Process A holds intent I (row queued, gen GA) sleeping on READY. Process B starts, its startup sweep CASes I to GB and runs it. READY: A's _admit acquires a permit → hold.resume → UPDATE … WHERE status='queued' AND generation_id=GA → None → gen.attempt += 1 → A continues and streams/persists an answer under GA; B persists one under GB → two assistant rows for one question.

*Fix:* When the CAS returns None and the row now names another generation, raise (e.g. a new LeaseLost → treat like a Stop: end the turn with a 'taken over' frame) instead of continuing.

### [minor] orchestrator/app/continuity.py:441
Outcome accounting in _resume_row: when main.chat() loses the CAS to a browser re-attach it returns `_sse_response(other.follow())` (no HTTPException), the `after['generation_id'] != before` check then counts the row as `resumed` although the browser resumed it; the discarded StreamingResponse/follow() generator is harmless but the metric lies. Also breaker.py:89 claims continuity registers an on_transition listener — nothing does (dead hook).

*Scenario:* READY edge; sweep and the user's tab re-POST /chat for the same intent within the same tick; the tab's _retry_chat_request wins; the sweep's chat() attaches to `other` and returns; _resume_row counts resumed=1 → llm_resumed_generations_total{resumed} over-counts and duplicate_suppressed under-counts.

*Fix:* Have _resume_row compare `after['generation_id']` against the generation main.chat() actually created (return it, or read _live_generations[conv] and check its intent/owner) and count duplicate_suppressed otherwise; delete or use the breaker listener hook.

### [minor] orchestrator/app/continuity.py:208
Sibling main-model calls of one turn (Deep Research gathers, best-of-N candidates, the route+answer pair) share one Hold: when the breaker goes HALF_OPEN the first sibling takes the canary permit and hold.resume() clears the hold, the next sibling's tick re-enters it (row running→queued again, QUEUED_LINE said again) while the canary is in flight — the 'exactly one status line' of §8.3 becomes one line per HALF_OPEN cycle, and the row flip-flops queued/running.

*Scenario:* Max-effort turn with 3 candidates queued behind an OPEN breaker; cooldown → HALF_OPEN; candidate 1 acquires the canary (hold.resume → row running, attempt 2); candidate 2's next _admit tick gets None → hold.enter → park (row queued) + QUEUED_LINE again; canary succeeds → CLOSED → candidate 2 resumes (row running, no new attempt). Person reads the queued line twice for one outage.

*Fix:* Keep the hold in the 'waiting' state until ALL admitted siblings have permits (reference-count waiters) or suppress re-announcement while a canary of the same turn is in flight (e.g. `announced` sticky until a first chunk of the turn arrives).

### [minor] orchestrator/app/admission.py:129
After a large recovery the resumed rows (sweep limit 200) plus the in-process held turns all rush the NORMAL lane at once; beyond 10 active + 100 waiting, Lane.acquire refuses newcomers with `capacity` before telling them anything, and main.py turns that into the TIMEOUT sentence ('could not start your request in time') and a `failed` row — requests that were promised 'will resume automatically' fail without ever waiting.

*Scenario:* Outage with >110 queued requests (sweep + live holds). READY → breaker CLOSED → all callers pass _admit and enter admission.run; the 111th onward raises AdmissionRejected('capacity', 0 s) → _failure_sentence TIMEOUT → row failed → user must retry.

*Fix:* Size ADMISSION_MAX_WAITING ≥ _SWEEP_LIMIT + expected live holds (or make the sweep pace resumes to lane capacity), and give `capacity` its own truthful sentence.

### [minor] orchestrator/app/main.py:1775
A person's new send in a conversation replaces a LIVE older generation (cancelled, row `cancelled`) but not a PARKED one (row `queued`, no live generation), so a parked question is later resumed by the sweep and answered after the newer exchange — an out-of-order second answer to a question the person already re-asked.

*Scenario:* Turn T1 parks (EXPIRED_LINE). Person re-asks the same question as T2 in the same conversation while still RECOVERING (T2 queues). READY: T2 resumes and answers; the sweep also resumes T1 (no live generation for C at that moment) → a second answer to the same question is appended below T2's.

*Fix:* When a new intent is accepted for a conversation whose latest row is `queued` with no live generation, mark that row `cancelled` ('replaced by a newer message') exactly as the live case does, so the sweep never resumes it.

## sre / correctness
- contract: CONTRACT §6 / ARCHITECTURE.md:84-86: 'the Docker healthchecks are report-only except one last-resort tier ... so it can never race a live controller' does not hold on this production after a routine deploy: sf-local-ai-vllm-1 and sf-local-ai-worker-vllm-worker-1 were created from the v0 overlay and keep the immediate kill-on-5xx branch (verified via docker inspect) beside the new controller until a `deploy.sh --full`; no launcher/deploy step detects or warns about it.
- contract: CONTRACT §5 step 4 / §2 READY ('both GPUs observed working'): on this production the launcher cannot supply the worker exporter URL (no CLUSTER_WORKER_MGMT_IP / MONITORING_WORKER_BIND in .env; monitoring.sh derives it by ssh but cluster.py does not) and the head exporter's loopback publish is not applied by any deploy path, so the readiness sequence can never record participation and the controller stays DEGRADED permanently -- .env.example:258-263 claims 'Empty = never a failure'.
- contract: CONTRACT §6.5: 'ulimits core: 1 on every vLLM container' is in the files but, by the routine-deploy design, not in force on the running head, worker or (until this deploy's recreate) router; RUNBOOK §16 documents this for ulimits/volumes but not for the healthcheck tier that shares the same `--full` dependency.

### [major] compose/compose.cluster-dgx-spark.yaml:114
On THIS production the running head and worker were created from the v0 overlay (docker inspect sf-local-ai-vllm-1: healthcheck still contains `case "$code" in 5*) ... kill -9`, Ulimits only memlock, no kernel-cache mounts; the worker container likewise). A routine deploy leaves them as they are (by design) but starts the new engine-controller beside them, so the v0 immediate-kill-on-5xx branch (<=30 s) races the controller's capture -> worker-first -> head choreography (10-20 s) on every head_engine_dead until an operator does a `deploy.sh --full` -- exactly the round-1 double-restart ARCHITECTURE.md:84-86 says the tier removed. Nothing in cli.py's preserve path (1367-1373), deploy.sh or RUNBOOK §16 detects or warns that the head's live healthcheck is the v0 one.

*Scenario:* After the first routine deploy of this branch (the pipeline cannot run --full), the head's Worker_TP0 faults (the GDN class seen on both nodes). /health goes 5xx; the controller confirms in one observation, captures diagnostics, POSTs the sentinel restart and then `docker restart` of the head; inside that 10-20 s window the OLD healthcheck (30 s interval, 15 s timeout) fires `kill -9` on the API process -> the head is restarted twice; the worker (restarted first by the sentinel) joins the FIRST head's rendezvous and is stale for the second; the second head waits 300 s at the rendezvous, crashes, Docker restarts it; the outage stretches from ~5 min to 10-15 min and a recovery budget unit is burned (attempt 2 after cold_start_timeout at 900 s). Two such events in an hour and the controller stands down (DOWN) with requests queued.

*Fix:* In the launcher's preserve path compare the running head container's `.Config.Healthcheck.Test` (or com.docker.compose.config-hash) with the rendered one; if the live check still carries the 5xx kill branch, print a loud step line and record it in state.json/deploy log, and have deploy.sh refuse a routine deploy of this ref until a `--full` has run (or require the first deploy of this branch to be `--full` in the runbook and the pipeline vars). Alternatively have the controller tolerate an externally restarted head by re-sending the worker restart after the head's started_at moves.

### [major] launcher/techsara_cli/cli.py:1169
A routine deploy never restarts the engine controller or the worker sentinel when only their CODE changed: `up -d --no-deps engine-controller` recreates on a definition change only, and the code is a bind mount (`./monitoring/engine-controller:/app:ro` on the head, `./sentinel.py`/`./common.py` on the worker); `cluster-worker.sh start vllm-worker-sentinel` is `up -d` with the same property. The already-running python processes keep the old module in memory until something else restarts the containers, while the deploy record says the commit is live.

*Scenario:* A fix to controller.py (this worktree already carries 269 uncommitted lines in it) merges to main; the pipeline deploys green (`techsara up` routine): generated.env is unchanged, so `docker compose up -d --no-deps engine-controller` prints "Running" and the previous process keeps executing the old trigger/choreography code; the sentinel on Node 2 likewise. The next incident is handled by the code the fix replaced, and nobody can tell from `docker ps` or the deploy log.

*Fix:* In `_start_main_engine_and_controller` start the controller with `force_recreate=True` (it is stateless: lock and evidence are host files) or `docker compose restart engine-controller` when the mounted files' sha256 changed; in the routine cluster branch use `cluster-worker.sh restart vllm-worker-sentinel` (or `up -d --force-recreate vllm-worker-sentinel`) after `cluster-sync.sh --env-only` has verified the shipped sha256; log the container StartedAt so the deploy record proves it.

### [major] launcher/techsara_cli/environment.py:978
On this production `.env` has no CLUSTER_WORKER_MGMT_IP and `MONITORING_WORKER_BIND=` is empty, so TECHSARA_WORKER_GPU_EXPORTER_URL renders empty; the controller (controller.py:770-777 and 2201-2204) then marks participation `unobserved` and holds the top-level state at DEGRADED permanently after every readiness sequence. TECHSARA_HEAD_GPU_EXPORTER_URL=http://127.0.0.1:9835 is generated, but the loopback publish lives only in compose.monitoring.yaml, which no deploy path re-applies (the running sf-local-ai-dgx-gpu-exporter-1 has PortBindings {} -- verified), so the head sample is unreachable as well until `scripts/monitoring.sh up` is re-run by hand. scripts/monitoring.sh already derives the worker's management address by ssh (`ip -4 -br addr show enP7s7` -> 192.168.9.68, verified) but cluster.py:757-762 only reads the two .env keys. .env.example:258-263 promises "Empty = never a failure"; the observable result is a DEGRADED engine forever.

*Scenario:* Operator runs `deploy.sh` (routine) on this box exactly as documented. The controller comes up, runs the readiness sequence, both exporter samples come back unreachable/not configured, `/state` reads `DEGRADED: GPU participation unobserved (...)`, techsara_vllm_state_code=4 and techsara_vllm_participation_ok=0 for the lifetime of the deployment; the Grafana state tile and `monitoring.sh verify` show DEGRADED for a healthy engine, and a real DEGRADED signal (sentinel unreachable, TTFT > 10 s) is indistinguishable from the permanent one. The orchestrator still serves (DEGRADED is in SERVING), so nobody is forced to notice.

*Fix:* In resolve_cluster_settings derive CLUSTER_WORKER_MGMT_IP the way scripts/monitoring.sh does when the keys are empty (ssh CLUSTER_WORKER_SSH `ip -4 -br addr show enP7s7`, or read the dgx-gpu target from monitoring/prometheus/prometheus.yml), and either publish 127.0.0.1:9835 in an overlay the launcher's chain includes or have the launcher call `scripts/monitoring.sh up` / verify the port and print a step line when it is not bound; at minimum add `CLUSTER_WORKER_MGMT_IP=192.168.9.68` and `scripts/monitoring.sh up` to the runbook's rollout steps for this box.

### [major] scripts/cluster-recover.sh:384
`--clear-kernel-cache` recreates the worker from the shipped compose file and worker.env (`worker_compose up -d vllm-worker`) but only `docker start`s the head's EXISTING container (line 386). Every routine deploy now re-ships worker.env from the current .env/generated.env (cluster-sync.sh --env-only, lines 216-250, including CLUSTER_ENGINE_ARGS and CLUSTER_VLLM_IMAGE = the RENDERED main image) while the head keeps its old command/image until a --full, so after any routine deploy that regenerated the engine args or MAIN_MODEL_IMAGE the two ranks are started with different configurations. On this production the head also has no kernel-cache volume yet, so `empty_volume head` reports "absent (nothing to empty)" and the procedure ends "RECOVERED" without having cleared the head's cache (it lives in the container's writable layer).

*Scenario:* Operator sets CLUSTER_GDN_PREFILL_BACKEND=flashinfer (or MAIN_MODEL_IMAGE=<candidate digest>) in .env for the announced candidate-B window; before that window a push to main runs a routine deploy, generated.env and the worker's worker.env now carry the new argument/image, the head still runs the old command. A compile-error signature appears and the operator follows the runbook: `cluster-recover.sh --clear-kernel-cache --yes` -> the worker is recreated with `--gdn-prefill-backend flashinfer` (or on the candidate image), the head is `docker start`ed with the pinned command -> vLLM's mp executor fails the config match / the rendezvous never completes, the script hits the 900 s cold-start budget, and the pair is down until a `--full` deploy.

*Fix:* Before touching anything, compare the running head's `.Config.Cmd`/`.Config.Image` with the head's rendered config and the running worker's config-hash with `worker_compose config --hash vllm-worker`; refuse with a message naming `deploy.sh --full` when either differs. Use `worker_compose restart`/`docker start` for both ranks in the cache-clear path (never `up -d`), and check `docker inspect` that both containers actually mount the kernel-cache volume, refusing (or saying so) when the head does not.

### [major] launcher/techsara_cli/cli.py:1393
In the not-preserve path the launcher stops the previous engine-controller (`compose.stop_service`) before restarting the pair, but no except/finally restarts it: any TechSaraError raised later (cluster-sync.sh ssh/rsync failure, `cluster-worker.sh start` failure, head readiness timeout, the two-node "did not become ready" re-raise at 1127-1133, a failed probe) leaves the controller in the Docker `stopped` state, where `restart: unless-stopped` will never bring it back. Until the next successful `up`, no actor recovers the pair (the legacy watchdog is gone; only the healthcheck's 4-minute last-resort tier remains).

*Scenario:* A hand-run `./techsara up` (the runbook's own instruction after stopping the controller, or an operator's plain restart) hits a transient ssh failure in `cluster-sync.sh` at the model-files step; the command exits with the error, the lock is released, `docker ps` shows sf-local-ai-engine-controller-1 Exited. The head is still serving, so nobody notices; the next rank fault goes undetected for 5 min (vLLM's RPC timeout), then the head is killed by its healthcheck's last-resort tier at 15 min + 4 min, the worker rejoins the wrong group -- the 2026-09-11 timeline, without any controller state or evidence capture.

*Fix:* Wrap the pair restart in try/except: on failure, if the head container exists and is running (or the previous controller was running before the stop), call `compose.up_service("engine-controller")` (or `docker compose start engine-controller`) and print a step line; otherwise print that the controller is stopped and how to start it. Also make `deploy.sh`'s failed-apply path start it before the health gate.

### [minor] scripts/cluster-sync.sh:218
`--env-only` (the routine-deploy path, cli.py:1413) skips the sentinel-image step (lines 137-157, DO_IMAGE=0) and just writes CLUSTER_SENTINEL_IMAGE=<head's digest> into worker.env, so `cluster-worker.sh start vllm-worker-sentinel` makes the worker pull python@sha256:2c941e86... from Docker Hub during the deploy, with no save|load fallback. The worker does not hold that digest today (its python:3.12-slim is sha256:09f7da3b...; verified) and a manifest query from the worker took ~30 s vs ~1 s from the head.

*Scenario:* Docker Hub is flaky (as it has been for the `# syntax=` build line) during a push-to-main deploy that changes only the frontend: the sentinel pull on Node 2 fails, `cluster-worker.sh start vllm-worker-sentinel` exits non-zero, `_run_cluster_script` raises, `techsara up` fails and deploy.sh rolls the whole deploy back to the previous SHA (which, on the first deploy of this branch, also removes the controller and restores the watchdog).

*Fix:* Run the sentinel-image stage in --env-only as well (it is a ~50 MB check with the existing docker save | ssh docker load fallback), or have `cluster-worker.sh start vllm-worker-sentinel` pre-check `docker image inspect` on the worker and stream the image from the head when absent.

### [minor] launcher/techsara_cli/cli.py:1371
The main-model probe that decides `preserve_main` runs BEFORE the engine lock is acquired (1383). A hand-run `TECHSARA_PRESERVE_MAIN_MODEL=1 ./techsara up` during a controller recovery fails the probe, drops preserve, waits for the lock and then restarts the freshly recovered pair (a definition drift, as on this branch, makes that a full 15-25 min reload). deploy.sh and cluster-up.sh mask this by taking the lock before `techsara up`, at the cost of holding it across the whole up (router recreate + sentinel sync ~1-3 min during which the controller cannot act on a fault).

*Scenario:* Operator runs `TECHSARA_PRESERVE_MAIN_MODEL=1 ./techsara up` to recreate the controller while the controller is in WAIT_FOR_MODEL_LOAD of a recovery: probe fails (connection refused), preserve is ignored, the launcher blocks on the lock until READY, then stops the controller, re-syncs the worker, recreates vllm-worker and vllm (drifted definitions) and reloads the model that just came back.

*Fix:* Acquire the lock first, then probe (or read the controller's /state and wait for READY/BUSY before probing); keep deploy.sh's outer lock only as reentrancy, not as the ordering fix.

### [minor] compose/compose.cluster-dgx-spark.yaml:125
The last-resort kill tier's age guard is hard-coded at 900 s on both nodes (worker file line 124) while the controller's cold-start budget is the configurable ENGINE_COLD_START_BUDGET_S (.env.example:630); a raised budget cannot be honoured because the healthcheck kills a head (or worker) whose process is older than 15 min after 8 misses, regardless of the controller's opinion, and start_period does not stop the script's own kill.

*Scenario:* Operator sets ENGINE_COLD_START_BUDGET_S=1500 for the candidate image's FlashInfer JIT warm-up; a cold start that legitimately takes 16 min is killed by the healthcheck at ~15 m + 4 m of misses, the controller sees started_at move, resets, and the head loops every ~19 min without ever finishing a load; the controller never reaches cold_start_timeout so no DOWN/alert names the cause.

*Fix:* Interpolate the same knob into the guard (`-gt ${ENGINE_COLD_START_BUDGET_S:-900}`) on the head, ship it in worker.env (WORKER_PLAIN_KEYS) for the worker, and have the launcher validate it against the healthcheck's start_period.

### [minor] .env.example:256
CLUSTER_SENTINEL_TOKEN in .env is documented as a supported bring-your-own override, but compose.yaml's `x-runtime-env` feeds `.env` wholesale to the orchestrator, so the bring-your-own restart token lands in the orchestrator's process environment -- the exposure prepare_controller_secrets and compose.dgx-spark.yaml:168-177 exist to prevent (prepare_local_secrets only strips it from secrets.env).

*Scenario:* An operator follows the comment and sets CLUSTER_SENTINEL_TOKEN in .env; `docker inspect sf-local-ai-orchestrator-1` (or any process-environment read inside the container that handles untrusted input) now reveals the token that authorises `POST /restart` of the worker rank from any host that can reach 10.100.184.2:9839.

*Fix:* Document that .env is inherited by the orchestrator and point the override at .runtime/controller.env, or have prepare_controller_secrets refuse (or warn on) a token found in .env.

### [minor] scripts/cluster-worker.sh:8
shellcheck v0.10.0 fails on this script (SC2178/SC2128: `cmd` is a local array in cluster-common.sh:head_compose and a string here) -- the only non-clean file among the scripts the launcher and deploy.sh invoke; monitoring.sh renamed its variable to `action` for the same reason in this commit, this file (untouched since b4bda8a) was not.

*Scenario:* Contract §10 requires shellcheck-clean shell; a CI shellcheck gate over scripts/ fails on a file the routine deploy path now depends on (cluster-worker.sh start vllm-worker-sentinel).

*Fix:* Rename `cmd` to `action` as monitoring.sh does.
