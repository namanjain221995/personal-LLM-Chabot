# Review manifest — engine availability programme (2026-09-12)

**Workstream:** manifest (principal reviewer, read-only).
**Reviewed state:** commit `321cf70` on `dev` — the commit production was running
during the incident (`.runtime/incidents/20260911T223140Z-vllm-down/00-time-and-commit.txt`)
and the base of `feat/vllm-availability-2026-09-12`. Line numbers below are
from that commit (`git show 321cf70:<path>`); where another workstream already
has uncommitted edits in the worktree at review time the row says so.
**Live evidence:** `docker inspect` / `docker logs` / `docker compose config`
on the head (read-only), the incident bundle under
`/home/techsphere/Documents/project/personal-LLM-Chabot/.runtime/incidents/20260911T223140Z-vllm-down/`
(referred to as `INC/` below), and `.runtime/generated.env` read for the
`CLUSTER_*`/`MAIN_MODEL*`/`VLLM_PORT` keys only. No secret value was read
or printed. ssh to the worker was **not** available to this review (denied by
policy), so worker-side facts come from the bundle and from the head's local
copy of the shipped env (`.runtime/cluster-worker.env`).
**Binding contract:** [`CONTRACT.md`](CONTRACT.md) (states, signals, failure
categories, ownership). Findings are tagged with the workstream that owns the
fix per CONTRACT §9.

Legend for *reviewed*: **yes** = read in full; **partial** = the sections named
were read; **excluded** = not read, reason in the last column. *Risk* is the
operational risk the file carries today (H/M/L), not the risk of changing it.

---

## 1. Timeline the bundle supports (UTC, 2026-09-11)

| t | event | evidence |
|---|---|---|
| 22:15:45 | 9 requests running, 346 tok/s, healthy | `INC/10-vllm-head-full.log` (loggers.py:310 line at 22:15:45) |
| 22:15:50.297 | rank 1 (`VLLM::Worker_TP1`, pid 291) raises `RuntimeError: Triton Error [CUDA]: misaligned address` in `fused_gdn_prefill_post_conv.py:215` ← `qwen_gdn_linear_attn.py:1348 _forward_core`; `terminate called after throwing c10::AcceleratorError` | `INC/22-worker-full.log:1538-1603,1693-1694` |
| 22:15:50 | kernel on spark-476e: Xid 13 (graphics exception) + Xid 31 (MMU fault @0x0, GPC1) for pid 1415964 | `INC/32-worker-kernel-apport.txt` (03:45:50 IST) |
| 22:16:05 | head logs `Avg generation throughput: 0.0 tokens/s, Running: 9 reqs` — the last throughput line until the restart | `INC/10-vllm-head-full.log:35283` |
| 22:16:00 → 22:20:49 | Prometheus: `vllm:generation_tokens_total` frozen at 2,138,504; `num_requests_running` 9; `up{job="vllm-main"}==1`; `/health`, `/v1/models`, `/metrics` all 200 (10 `/health` hits per minute from 172.18.0.19, 10.100.184.2 and 127.0.0.1) | `INC/40-prometheus-window.txt`; head log grep (`GET /health … 200` 22:15–22:20) |
| 22:16:50, 22:17:50, 22:18:50, 22:19:50 | head EngineCore: `No available shared memory broadcast block found in 60 seconds` (INFO level) | `INC/10-vllm-head-full.log:35307,35336,35365,35395` |
| 22:16:15 → 22:18:15 | watchdog probe times out (120 s) → `(1/2)` | `INC/11-watchdog-full.log` |
| 22:15:50 → 22:20:41 | apport holds the 25 GiB rank process, writes `/var/crash/_usr_bin_python3.12.0.crash` (3,509,336,530 B, 03:49 IST); the worker's headless executor only then sees `Worker proc VllmWorker-1 died unexpectedly` | `INC/32-worker-kernel-apport.txt`; `INC/22-worker-full.log:1734` |
| 22:20:49.655 | head: `VLLM_EXECUTE_MODEL_TIMEOUT_SECONDS` (300 s) → `TimeoutError: RPC call to sample_tokens timed out` → `EngineDeadError`; 10 × `POST /v1/chat/completions … 500` (9 from 10.100.184.2, 1 from 127.0.0.1 = the watchdog probe); API `Shutting down` … `MPClient: complete` at 22:20:49.876, then **nothing until 22:22:56** | `INC/10-vllm-head-full.log:35424-35502` |
| 22:20:49 | watchdog: `fast HTTP error (94s) - not counted` (94 < 100 s threshold) | `INC/11-watchdog-full.log` |
| 22:21:01 → 22:21:22 | worker container restarts (RestartCount 1), new rank 1 joins the **old** head's TCPStore on 10.100.184.1:29501 | `INC/22-worker-full.log:1737-1752` |
| 22:21:00 | Prometheus `up{job="vllm-main"}` = 0 (listener closed) | `INC/40-prometheus-window.txt` |
| ~22:22:47 | head healthcheck: 4 × 30 s connection-refused misses → `kill -9` of `vllm serve`; container `finished 22:22:47.513`, `started 22:22:47.688` (RestartCount 1) | `INC/03-head-healthcheck.json`; `INC/01-container-inspect-summary.txt` |
| 22:22:44 → 22:22:47 | worker rank 1: gloo `Read error [10.100.184.1]:11692: Connection reset by peer` → `WorkerProc failed to start` → exits → second restart (RestartCount 2, started 22:22:47.170) | `INC/22-worker-full.log:1753-1823`; `INC/21-worker-inspect.txt` |
| 22:22:56 | new head API process (pid 56) starts; 22:23:17 NCCL init; 22:24:42 weights loaded (71 s); 22:24:58 KV 1,663,201 tokens; 22:25:48–22:25:53 CUDA graphs; 22:26:19 `Application startup complete`; 22:26:49 first throughput line (12 reqs, 113 tok/s) | `INC/10-vllm-head-full.log:35505,37166,37179,37193-37194,37241,37258` |
| 22:26:49 | watchdog `engine PROVEN ready - armed` | `INC/11-watchdog-full.log` |

Total: **10 min 59 s** from fault to the first real completion (22:15:50 → 22:26:49);
5 min 0 s of it (22:15:50 → 22:20:49) with every existing health signal green.

---

## 2. File-by-file manifest

### 2.1 Documentation

| path | purpose | reviewed | relevant findings | dependencies | risk | exclusions |
|---|---|---|---|---|---|---|
| `README.md` §7 (L347-399) | two-node cluster overview | partial (§7, §8, §11, §20, §21) | L373-375 claims "The head and worker heal themselves after a crash (measured: worker killed → completions back in 584 s with no operator)" — the incident measured 659 s with 300 s invisible to every check; needs the truthful figure and a pointer to `docs/availability/`. L395-398 file list omits the watchdog and the controller. | links `docs/CLUSTER.md`, `compose/compose.cluster-*.yaml`, `launcher/techsara_cli/cluster.py`, `scripts/cluster-*.sh` | L | §1-6, 9-19, 22-24 not re-read (out of scope) |
| `README.md` §8 (L401-431) | services/ports/networks | partial | L426-427 "dual mode: main 0.30 per node + explicit 16 GiB KV" is stale — running `--kv-cache-memory-bytes 8589934592` (8 GiB, `INC/03-head-cmd.json`; `generated.env CLUSTER_KV_CACHE_MEMORY_GIB=8`). L425 correctly states `inference` is `internal: true` and that publishing keeps the ports unauthenticated. Table row `vllm` says "30000 (→ VLLM_PORT 8000 if published)" but in dual mode the head is host-networked on 8000 regardless of the published overlay (cluster overlay L33-46). | — | L | — |
| `README.md` §11 (L637-662) | engine table | partial | No availability content; `chat.py` (plain streamed completion) and every other engine reach the main model through `app/llm.py` → `app/resilience.py`; the fallback engine of CONTRACT §1 (router) is what `router.py`/`agent.py` already use, so the memory cost claim ("costs no extra memory") is consistent with this table. | `orchestrator/app/engines/*` | L | — |
| `README.md` §20 (L860-875) | security model | partial | L863 documents the exposure decision: "In dual cluster mode the main model's head listens on the host (`0.0.0.0:8000` when published …)". Accurate; see §4 below. | — | M | — |
| `README.md` §21 (L877-929) | operations | partial | L881-884 daily commands: `./techsara status`, `scripts/cluster-status.sh --probe` — neither proves a completion unless `--probe` is given. L921 "the head waits 5 minutes for the worker before restarting" — correct (`--distributed-timeout-seconds 300`). No runbook for "engine wedged while healthy" — the docs workstream's `RUNBOOK.md` fills this; README needs only the index line (CONTRACT §9). | — | L | — |
| `CHANGELOG.md` L78-171 (2026-09-11 "MTP off …") | the only entry in 09-01..09-11 mentioning vLLM/watchdog/cluster/monitoring | partial (entries dated 2026-09-01..09-11 scanned; L78-171 read in full) | Accurate record of the 07:05Z fault, the 11 m 47 s unassisted recovery and the worker healthcheck 10→4 change. L166-171 "Not done, specified" already lists an Alertmanager and a completion-based wedge alert — both still absent at `321cf70`. Gap: no entries exist for 09-02..09-10 at all (monitoring stack, OCR relocation, whisper, V29 upload reliability, V30 tracing are undocumented here); the 09-11 Artifact entry (L3-77) and the 09-01 login entry (L173-239) contain no vLLM/cluster/monitoring content. | — | L | entries before 2026-09-01 not read |
| `docs/CLUSTER.md` | operator reference for dual mode | yes | L130-135 "Memory" still says 16 GiB per node / 2.98M tokens while L164-165 and the running engine say 8 GiB / 1,663,201 tokens — internal contradiction. L229-267 (engine-argument runbook) is correct and matches `scripts/deploy.sh:418-421,442-444`. L395-457 "Failure behaviour" is the best description of the two failure classes (RPC timeout vs NCCL spin) but L417-422 says a 5xx kills the process "immediately" — on 09-11 the API returned 500 once and then closed its listener, so the healthcheck saw 000 and needed four misses (`INC/03-head-healthcheck.json`, timeline above). L405-414 records the deliberate decision to keep `VLLM_EXECUTE_MODEL_TIMEOUT_SECONDS` at 300 s — the controller's canary (CONTRACT §5-6) is what removes the 5-minute blindness without touching that clock. L470-477 security notes correctly say `ufw` is disabled (verified: `/etc/ufw/ufw.conf ENABLED=no`). | `compose/compose.cluster-*.yaml`, `scripts/cluster-*.sh`, `launcher/techsara_cli/cluster.py` | M | — |
| `docs/MONITORING.md` | monitoring architecture | yes | L63-65/L234-253: one vLLM endpoint, scraped via the host port 8000 — so the `vllm-main` job depends on the head being host-networked (dual) or `PUBLISH_MODEL_PORTS=true` (single); undocumented coupling. L352-359 intervals match `prometheus.yml`. L456-476 security matches the compose files. L632-649: "Alerts are local … No paging integration, by choice" — confirms there is **no Alertmanager** (no `alerting:` block in `prometheus.yml`); every alert of the incident fired to nobody. Port table (L26-29, L456-467) lacks 9838/9839 — monitoring workstream adds (CONTRACT §9). L106-110 documents the exporter pattern the controller reuses. | `monitoring/**`, `compose/compose.monitoring*.yaml`, `scripts/monitoring.sh` | M | — |
| `docs/ISSUE/gdn-spec-decode-fault-report.md` | 2026-09-10 root-cause analysis | yes | §1-§5 attributed the faults to the MTP `index_select` branch; superseded by the remediation §1.1 (the asynchronous CUDA error surfaced at the next synchronising call). §3 fault chain (5 min to prove a hang + 4-9 min reload) is the pre-programme baseline. §8 "ruled out" table (L175-187) is reused in §6 below. §9.5 "consider lowering `--distributed-timeout-seconds`" was wrong about which timeout (corrected in the remediation). §10 records that the head log had rotated — same evidence gap recurred (see §7). | — | L | — |
| `docs/ISSUE/gdn-spec-decode-remediation-2026-09-11.md` | remediation record | yes | §1.1 proves the fault fires on ordinary mixed prefill+decode batches with MTP off (07:05:33Z, rank 0) — the 22:15:50Z event is the same signature on rank 1: fault class **not closed** on `0.26.1rc1.dev77` (§13.1). §4 documents `resilience.py` windows (120 s / 1200 s). §8 documents the worker healthcheck 10→4 misses (applied; `INC/23-worker-healthcheck-cmd.json` `Retries:4`). §11 explicitly kept the 300 s clock and named the wedge phase as the open gap; §12 lists the same exposures §4 below re-verifies (portainer, iperf3, `zealous_williamson` still present). §13.4 asked for an Alertmanager and a completion-based wedge rule — neither exists at `321cf70`. | — | L | — |

### 2.2 Compose

| path | purpose | reviewed | relevant findings | dependencies | risk | exclusions |
|---|---|---|---|---|---|---|
| `compose.yaml` | base project (postgres, orchestrator, sync-worker, frontend, searxng, pgadmin) | yes | L152-254 orchestrator: healthcheck (L230-250) requires only `status ∈ {ok,healthy,degraded}` and `app_db ok` — correct: a model outage must not restart the orchestrator. L227 `init: true`, L229 `stop_grace_period: 2m`. L223/L301 publish on `${TECHSARA_BIND_ADDRESS}` (= 0.0.0.0 in production, `ss` shows `0.0.0.0:8080`, `0.0.0.0:3000`). L355-358 `inference` network `internal: true` — the model containers are unreachable from outside unless published (but see the cluster overlay: `vllm` leaves that network in dual mode). No `ulimits` anywhere. L6-16 env-file chain: `.env` → `secrets.env` → `generated.env` (later wins). | `compose/*.yaml` overlays, `.runtime/generated.env` | L | — |
| `compose/compose.dgx-spark.yaml` | DGX overlay: main/router/embed/reranker/ocr engines + shell watchdog | yes (SRE has uncommitted edits here at review time) | **Watchdog (L115-170):** arms on `/v1/models` (L133) which stays 200 in a wedge; probe `wget -T 120` (L144); only `EL ≥ 100 s` counts (L153) — on 09-11 the 94 s 500 was discarded (`INC/11-watchdog-full.log`); needs 2 timeouts ≥ 6 min apart (60 s sleep + 120 s probe, twice) so it **cannot** act before vLLM's own 300 s clock in the dead-rank class — it only ever fires for the NCCL-spin class (2026-09-01/02/09 entries in the same log); restart is `docker restart -t 30` of the head only (L166) — worker not restarted first, so the old rank 1 rejoins a dying head (timeline 22:21:22 → 22:22:44). L119-120 Docker socket RW. **Not started by the launcher**: `cli.py` never `up_service("vllm-watchdog")`; the container was created by a manual full-chain `up` on 2026-09-01T22:14:14Z and survives on `restart: unless-stopped`; `./techsara down` (`compose.down()`, `compose.py:155-157`) removes it and the next `up` will not bring it back. Its running config-hash equals the rendered one (no drift). **Single-node `vllm` healthcheck (L69-74)** is `curl /v1/models` — proves nothing (CONTRACT §2); dual mode overrides it. L34/173/299 image pinned `@sha256:24f2f897…`; L217/257 embed/reranker `nvcr.io/nvidia/vllm@sha256:654e563e…`. No `ulimits` (core) on any engine (contract §6.5) — SRE adding. `vllm-ocr` (L298-333) is stopped on the head (`Exited (137) 2 days ago`, OCR runs on the worker). | `compose.yaml`, `compose/compose.cluster-dgx-spark.yaml` (overrides `vllm`), `.runtime/generated.env` | H | — |
| `compose/compose.cluster-dgx-spark.yaml` | head overlay (node-rank 0), layered last | yes (SRE has uncommitted edits: `engine-controller` env, comments) | L33-36 host network, `networks/expose/ports` reset → the head API is **on the host** at `${CLUSTER_API_BIND_ADDRESS}:${VLLM_PORT}` = `0.0.0.0:8000` (L40-41; `generated.env`), unauthenticated. L45 interpolates `CLUSTER_ENGINE_ARGS` verbatim (byte-identical to the worker, verified §3). L71 `stop_grace_period: 2m` vs watchdog `-t 30` vs contract `t=10`. **Healthcheck (L92-108):** `/health` 200 → healthy; 5xx → `kill -9`; 000 → 4 misses (2 min) after 15 min of age. On 09-11 it was the only actor that fired, at +7 min (22:20:49 listener closed → ~22:22:47 kill). It cannot see the wedge (signal 8 stays 200). `start_period: 30m` means an engine that never proves a completion sits "starting" for 30 min. No `ulimits: core` (contract §6.5). L110-116 `extra_hosts vllm:host-gateway` for orchestrator/sync-worker. Rendered vs running: identical (§3). | `launcher/techsara_cli/cli.py:209-238` (chain), `cluster.py:542-586` (args) | H | — |
| `compose/compose.cluster-worker.yaml` | worker (node-rank 1, `--headless`), shipped to Node 2 | yes (SRE has uncommitted edits: `ulimits.core`, `vllm-worker-sentinel`) | L17 image from `CLUSTER_VLLM_IMAGE` (pinned digest, §3). L40-42 host network + `ipc: host` (+ `shm_size: 32g`, ignored under `ipc: host`). L47-48 `ulimits: memlock` only → `RLIMIT_CORE` inherits dockerd's **unlimited** (verified on the head's twin: `/proc/<vllm pid>/limits` "Max core file size unlimited") — the 3.5 GB apport report and the 4 m 51 s hold (timeline) follow from this line. **Healthcheck (L76-93):** L82 `pgrep -f 'VLLM::Worke[r]' \|\| exit 1` marks the container *unhealthy* when the rank is gone but never kills/restarts it (Docker takes no action on unhealthy); during the apport hold the rank process still existed, so even that check passed. L83 probes the head's `/health` (200 during the wedge) → the worker cannot detect its own dead rank in this class. L87-88 5xx / 4-miss kill rules only re-pair after the head has already died. `restart: unless-stopped` + `init: true` is what actually recovered it (executor exit at 22:20:41 → restart at ~22:21:01). L57 `stop_grace_period: 2m` → `scripts/cluster-worker.sh restart` takes up to 2 min on a hung worker (the sentinel's `restart?t=5` in CONTRACT §6.4 is the fix). Worker healthcheck `Retries 4` confirmed live (`INC/23-worker-healthcheck-cmd.json`). | `scripts/cluster-sync.sh:101-112` (ships it + `worker.env`), `scripts/cluster-worker.sh` | H | — |
| `compose/compose.ocr.yaml` | Unlimited-OCR as its own project (either node) | yes (SRE has uncommitted `ulimits.core`) | L37 same vLLM digest (subject to the same GDN build; single-node, `--enforce-eager`). L41 `--host ${OCR_BIND}` — on the worker that is the management IP `192.168.9.68:30004` (Prometheus target `INC/40-prometheus-window.txt`), unauthenticated on the LAN. L44 explicit 3 GiB KV, L46 `--max-num-seqs 8`. L81 host network. L90 healthcheck `/v1/models` (proves nothing). No core ulimit at `321cf70`. Worker `RestartCount 9` for this container (`INC/21-worker-inspect.txt`) — worth a look by SRE, unrelated to this incident. | `scripts/ocr.sh`, `write_ocr_scrape_target` (`scripts/lib/cluster-common.sh:125-140`) | M | — |
| `compose/compose.whisper.yaml` | whisper replicas (either node) | yes | L33 default bind `192.168.9.68` (LAN, unauthenticated), L52 host network, L64 healthcheck checks `ready` (good: a real readiness signal), L71 `start_period 900s`. Not on the incident path; competes for the worker GPU (memory note `whisper-serialises…`). | `scripts/whisper.sh`, `compose/whisper/server.py` | L | `compose/whisper/*` not read |
| `compose/compose.monitoring.yaml` | Prometheus/Grafana/exporters on the head | yes | L36/72/130/205/232/305 images digest-pinned; **L171 and L283 `python:3.12-slim` are floating tags** (contradicts README §21 "the overlay tests assert every image is digest-pinned" — the launcher test only covers the launcher's chain, not this overlay). L55/109 Prometheus/Grafana bind loopback (verified `127.0.0.1:9090`, `127.0.0.1:3300`). L56-59 `host.docker.internal:host-gateway` → the `vllm-main` scrape reaches the head via the host port. L140 node-exporter `0.0.0.0:9100` (verified `*:9100`). L181 GPU exporter `pid: host`. L204-224 cAdvisor: `/var/run` **read-only** (verified). No Alertmanager service. | `monitoring/**`, `scripts/monitoring.sh:54-75` (must use the full chain) | M | — |
| `compose/compose.monitoring-worker.yaml` | node/GPU exporters on the worker | yes | L32/62-63 bind to the management IP (never RoCE) — the sentinel of CONTRACT §6.4 deliberately does the opposite (RoCE only); both are correct for their audience. L55 floating `python:3.12-slim`. L66-79 documents the "published port on a not-yet-assigned address leaves the container dead with RestartCount=0" trap — the sentinel must bind inside the process for the same reason (SRE's in-flight file does). | `scripts/monitoring.sh:88-108` | L | — |
| `compose/compose.published-dgx-spark.yaml` | opt-in port publishing | yes | L28 `vllm` publishes on `${TECHSARA_BIND_ADDRESS}` (0.0.0.0) — in dual mode the cluster overlay resets this, but the same variable drives `CLUSTER_API_BIND_ADDRESS` via `cluster.py:666-669`. L35-56 aux engines on `${TECHSARA_MODEL_BIND_ADDRESS}` (default loopback; verified `127.0.0.1:8002/8003/8005`). Header L5-16 states the exposure honestly. | `launcher/techsara_cli/environment.py:401-443` | M | — |
| `compose/compose.published-nvidia.yaml`, `compose/compose.published-cpu.yaml` | same for other profiles | yes | Not used on this deployment (dgx-spark profile). `published-nvidia` publishes `vllm-embed` on `TECHSARA_BIND_ADDRESS` (not the model bind address) — inconsistent with the dgx overlay's 2026-09-01 fix. | — | L | — |
| `compose/compose.nvidia.yaml` | generic NVIDIA overlay | yes | Not used here; `vllm` healthcheck `/v1/models` (L67-72); `MAIN_MODEL_PREFIX_CACHING_ARGUMENT`/`SPECULATIVE_ARGUMENT` rendered from the same launcher validator (L46-47) so single-node cannot re-enable MTP by itself. | `launcher/techsara_cli/environment.py:764-765` | L | — |
| `compose/compose.cpu.yaml` | llama.cpp | yes | Not used here. | — | L | — |
| `compose/compose.cloudflare.yaml` | public tunnel(s) | yes | L38-39 tunnel is on `application` only, never `inference` → the model ports are not reachable through it; L57-58 no `ports:`. Grafana tunnel L101-140 exposes the Grafana login publicly (docs/MONITORING.md L441-452 recommends Cloudflare Access). Not on the incident path. | `scripts/tunnel.sh` | L | `scripts/tunnel.sh` not read |
| `compose/compose.mac.yaml`, `compose/compose.windows-wsl2.yaml`, `compose/compose.external-development.yaml` | `extra_hosts` shims | yes | Nothing availability-relevant. | — | L | — |

### 2.3 Scripts

| path | purpose | reviewed | relevant findings | dependencies | risk | exclusions |
|---|---|---|---|---|---|---|
| `scripts/lib/cluster-common.sh` | shared settings/ssh/compose helpers | yes | L64-96 `cluster_load_settings` reads `.env` then `generated.env` (generated wins; L83-85 `TECHSARA_CLUSTER_MODE` = what is deployed). L104-110 `api_host` maps `0.0.0.0` → `127.0.0.1` (so every script probes the head over loopback). L144-152 ssh `BatchMode`, `ConnectTimeout=8`. L171-200 `head_compose` replays the chain from `state.json` (guards the subset-chain trap). L203-205 `worker_compose` runs compose on Node 2 over ssh with `--env-file worker.env`. No lock of any kind — `scripts/lib/engine-lock.sh` (CONTRACT §6.3) is new. | `.runtime/state.json`, `.runtime/generated.env` | M | — |
| `scripts/cluster-sync.sh` | image/model/env to the worker | yes | L39-58 enforces the same image ID on both nodes (falls back to the image ID when the worker has no registry digest). L101-112 generates `worker.env` from `generated.env` (`MAIN_MODEL*`, `MODEL_MAX_CONTEXT`, `VLLM_PORT`, `TECHSARA_CLUSTER_MODE`, `CLUSTER_*` only — verified against `.runtime/cluster-worker.env`: 26 keys, no secret). L113 validates the worker compose remotely. Ships **only** `compose.cluster-worker.yaml` + `worker.env` (L111) — the sentinel's `sentinel.py`/`common.py` are not shipped at `321cf70` (SRE's in-flight worker compose expects them next to the file). | `scripts/lib/cluster-common.sh` | M | — |
| `scripts/cluster-worker.sh` | start/stop/restart the worker project | yes | L13 `up -d` (recreates on definition change; waits at the rendezvous). L17 `restart` is a plain compose restart (2 min grace, does not apply a new `worker.env` — `docs/CLUSTER.md:265-267`). L15-16 stop/down `--timeout 120`. | `worker_compose` | M | — |
| `scripts/cluster-up.sh`, `scripts/cluster-down.sh` | wrappers | yes | `cluster-down.sh:13` runs `./techsara down` (removes the watchdog container, see 2.2). No engine lock — CONTRACT §6.3 lists both for `engine-lock.sh`. | `./techsara` | L | — |
| `scripts/cluster-logs.sh` | log tails | yes | L16-17 `nccl` mode greps `NET/(IB\|Socket) : Using` — the only place "NCCL TCP fallback" is observable today (not in Prometheus; CONTRACT §7.4 `NcclTcpFallback` → document as not observable). | — | L | — |
| `scripts/cluster-status.sh` | status report (`--probe` = real completion) | yes | L63-73 "healthy" = compose health (= `/health` 200 in dual mode) — reports `running / healthy` on a wedged pair (`INC/04-cluster-status-passive.txt` at 04:04 IST shows the healthy post-restart state; nothing in the bundle ran it during the wedge). L77-83 `/v1/models` = "API responds". L122 prints `num_requests_running` but does not compare `generation_tokens_total` across two samples. L139-184 `--probe`: streaming completion + GPU sampling on both nodes — this is the only script-level proof of both ranks (signal 13). Used as the deploy health gate (`deploy.sh:498-501`) **without** `--probe`; the gate's completion comes from `deploy.sh:491-495` instead. | `cluster-common.sh` | M | — |
| `scripts/cluster-verify-engine.sh` | prove the engine config from `docker inspect` on both nodes | yes | L27-28 both argvs; L55-61 byte-identical comparison (strip list); L63-84 generated.env vs running (drift); L94-97 fault/Xid counts (fail on any Xid since boot — after 09-11 both kernels have Xids, so it reports FAIL until the next reboot; consider `--since` the container start); L109-129 `--probe` completion + both GPUs ≥30 %. This is the reference implementation for CONTRACT §3 signal 13 and for `VllmConfigDrift`. | ssh to worker | L | — |
| `scripts/cluster-doctor.sh` | preflight (read-only) | yes (SRE has a 2-line uncommitted edit) | L30 `/dev/infiniband`, L70/L77 `ufw` state, L79-81 port collisions on 29501/8000, L85-89 image on both nodes. Nothing about core dumps/apport — a `core_pattern`/`RLIMIT_CORE` check belongs here (SRE). | — | L | — |
| `scripts/cluster-bench.sh`, `scripts/cluster-test.sh` | bench / NCCL test | partial (headers + endpoint logic) | Not on the incident path; `cluster-test.sh` is the only transport proof (`docs/CLUSTER.md:462-464`). | `scripts/lib/nccl_allreduce_bench.py` | L | bodies beyond L30 not read |
| `scripts/cluster-soak.py` | mixed-workload soak + two-node monitor | yes | The reproducer: L71-91 mix (60 % short, 30 % 3-9K, 10 % 24-32K; thinking on for a third). L50-54 `FAULT_RE` and L189 worker grep are the fatal-signature list the sentinel should reuse (CONTRACT §6.2 list lacks `Worker proc`, `WorkerProc hit an exception` is present; add `No available shared memory broadcast block` as a **head** wedge signature). L216-221 wedge = tokens frozen across 3 samples (90 s at 30 s) with running > 0 — same threshold as CONTRACT §2 WEDGED. L235 read timeout 1800 s. Read-only apart from the load. | ssh to worker, `docker logs`, `/metrics` | L | — |
| `scripts/monitoring.sh` | monitoring stack lifecycle | yes | L54-75 full-chain rule (a subset recreates the orchestrator as `:cpu`). L131 renders the OCR file_sd target. L134-136 named services only (never a bare `up -d`). L200-231 `verify` — Prometheus queries, no completion. Adding `engine-controller` to the head's named list is SRE/monitoring's change. | `cluster-common.sh`, `compose/compose.monitoring*.yaml` | M | — |
| `scripts/deploy.sh` | deploy to this machine | yes | L141 `dr_lock_acquire` (deploy lock only — no engine lock). L151-156 refuses a dirty tree (shared checkout). **L351 `git clean -qfd -e .env -e .runtime`** on the production checkout — deletes untracked work of other sessions (the fault report was lost this way per the remediation §2). L410-417 `--full` → `techsara down` → recreates the pair (~10-15 min outage, no lock against the controller/watchdog). L418-421/442-444 routine deploy preserves the main model (`TECHSARA_PRESERVE_MAIN_MODEL`). **Health gate L459-503:** real completion (L491-495, 180 s, `enable_thinking:false`) + `cluster-status.sh` (L498-501) — a good gate; it also runs after a rollback (L567). L483 treats `vllm` in `checks` as fatal (= the orchestrator's `/health` dependency probe, which is `/health` 200 — see 2.5). | `scripts/lib/deploy-common.sh`, `scripts/deploy-{record,preflight,drain}.sh`, `./techsara` | H | — |
| `scripts/lib/deploy-common.sh` | chain/lock/image/schema helpers | yes | L48-53 required four-file chain (the cluster overlay is mandatory — a single-node deploy on this box is a different deploy). L73-139 prefix from `state.json`. L156-211 `flock` on `.runtime/locks/deploy.lock` with holder metadata and re-entrancy by pid — the model `engine-lock.sh` should copy. No destructive action here. | `.runtime/state.json` | L | — |
| `scripts/deploy-lock.sh` | run anything under the deploy lock | yes | L24-27 states the hole: a bare `./techsara up` bypasses the lock; the launcher does not take it. Same hole applies to the new engine lock unless the launcher (or `cluster-up.sh`) takes it. | `deploy-common.sh` | M | — |
| `scripts/deploy-drain.sh` | grace-period audit + quiet-moment wait | partial (header, `check`) | Never stops anything; advisory. `stop_grace_period` audit (L89-…) covers orchestrator/frontend/sync-worker only, not `vllm`. | `deploy-common.sh` | L | `wait`/`uploads` bodies not read |
| `scripts/deploy-preflight.sh`, `scripts/deploy-record.sh`, `scripts/deploy-rollback.sh`, `scripts/deploy-db-rehearsal.sh` | build-once/record/rollback/schema rehearsal | partial (headers + grep for model/restart/socket/lock) | None touches the model containers or the Docker socket beyond `docker rm -f` of throwaway containers (`deploy-rollback.sh:200`, `deploy-db-rehearsal.sh:95`). `deploy-rollback.sh:266` and `deploy-preflight.sh:80` take the deploy lock. | `deploy-common.sh` | L | bodies not read |
| `scripts/deploy-smoke.sh` | post-rollout invariants | yes (L95-157) | L109 `/health says the main model is not answering` keys on the orchestrator's `vllm` check (= `/health` 200). L126-141 MODEL CLOCK asserts the main model did **not** restart during a routine deploy. L143-155 `--with-model` real completion (off by default). | `deploy-common.sh` | L | L1-94 skimmed |
| `scripts/lib/env-load.sh`, `scripts/lib/env_export.py` | safe env loading | yes (`env-load.sh`) | Correct handling of `CLUSTER_SPECULATIVE_CONFIG` JSON quoting (L15). Nothing else. | `launcher/techsara_cli/utils.py` | L | `env_export.py` not read |

### 2.4 Launcher and config

| path | purpose | reviewed | relevant findings | dependencies | risk | exclusions |
|---|---|---|---|---|---|---|
| `launcher/techsara_cli/cli.py` | `techsara up/down/status/doctor` | partial (L209-238, L284-352, L887-1210, L1371-1487; grep of the rest) — SRE has uncommitted edits | L209-238 `_compose_files`: base → profile overlay → published → **cluster overlay last** (matches `state.json`). L1068-1086 preserve-main probes the running engine with a real completion (`record_probe` → `compose.probe_internal_model`, 8 tokens) and leaves it alone; if the engine is not serving the flag is ignored and the pair is restarted (`cluster-sync.sh` + `cluster-worker.sh start` L1088-1096, then `up_service("vllm")` + `wait_service` 2400 s + probe L1107-1109). **Never starts `vllm-watchdog`** (grep: no reference in `launcher/`). L1431-1452 `down` also downs the worker. No lock is taken (`deploy-lock.sh:24-27`). Restart choreography is worker-first (correct) but has no engine lock and no "capture diagnostics first" step. | `compose.py`, `cluster.py`, `environment.py`, `scripts/cluster-*.sh` | H | L1-208, 353-886, 1211-1370, 1488-2198 skimmed by grep only |
| `launcher/techsara_cli/compose.py` | compose command + readiness | yes | L91-102 command chain (project `sf-local-ai`, three env files). L143-153 `up -d --no-deps <svc>`. L155-157 `down --timeout 120` (removes every project container, watchdog included). L192-256 `wait_service`: "ready" = compose `running` + `healthy` — i.e. the container healthcheck, which in dual mode is `/health` 200; L236-245 counts only restarts during **this** wait (the 08-25 RestartCount bug fix). L15-63 `_INTERNAL_PROBE`: an 8-token completion from inside an orchestrator container (`urlopen timeout=90`, outer 180 s) — the launcher's only real-completion gate; matches CONTRACT §2 "READY requires a real completion". L325-374 `reconcile` stops model containers that publish ports unless allowed. | — | M | — |
| `launcher/techsara_cli/cluster.py` | cluster settings, discovery, `CLUSTER_ENGINE_ARGS` | yes | L55/69 MTP opt-in only (default off). L76 default KV 16 GiB (overridden to 8 by `.env`). L82 `DISTRIBUTED_TIMEOUT_SECONDS = 300`. L83 default API bind `0.0.0.0`; L666-669: `publish_model_ports` → `0.0.0.0`, else the Docker bridge gateway — **the exposure decision** (see §4). L542-586 `build_engine_arguments` — deterministic, both nodes interpolate the same string. L291-326 ssh preflight `StrictHostKeyChecking=accept-new`. No `VLLM_EXECUTE_MODEL_TIMEOUT_SECONDS` rendering (vLLM's env default 300 s applies; the decision is recorded in `docs/CLUSTER.md:405-414`). | `environment.py` | M | — |
| `launcher/techsara_cli/environment.py` | generated env | partial (L401-465, L573-600, L636-672; grep) — SRE has uncommitted edits | L401-419 `TECHSARA_BIND_ADDRESS` (app), L421-443 `TECHSARA_MODEL_BIND_ADDRESS` (aux models, loopback default), L591 `PUBLISH_MODEL_PORTS`. L654-660 dual mode: `OPENAI_BASE_URL=http://vllm:${VLLM_PORT}/v1` (→ host-gateway). L764-765 prefix-caching/speculative arguments from one validator. `generated.env` is secret-free by design (0644). | `cluster.py`, `profiles.py` | M | remaining ~600 lines not read |
| `launcher/techsara_cli/runtime.py` | native runtimes, `CapabilityProber` | partial (grep + L474-590) | `CapabilityProber.probe` (L541-590) hits `/v1/models`, `/health` and an 8-token completion — used for native (Mac) runtimes; on DGX the compose probe applies. Nothing else on the incident path. | — | L | L1-473 not read |
| `launcher/techsara_cli/profiles.py` | profile/memory selection | partial (grep) | L143-178 memory budget uses free system memory unless models already run (`_project_has_running_models`, `cli.py:334-350`) — a `techsara up` while the head is swapping (`INC/50-head-system-state.txt`: 97 GB used, 24 GB available, 13 GB swap in use) can select a smaller profile; not this incident's cause. | `config/hardware-profiles.yaml`, `config/model-manifest.yaml` | L | bodies not read |
| `launcher/techsara_cli/hardware.py` | hardware detection | partial (grep) | L255 `_detect_dgx_spark`; L225-253 GPU smoke probe. Nothing availability-relevant. | — | L | bodies not read |
| `launcher/tests/test_compose_overlays.py` | overlay invariants | partial (L405-470) | **L429-439 asserts exactly one service mounts the Docker socket and it must be `vllm-watchdog`** — the `engine-controller` service (CONTRACT §6) fails this test until SRE amends it (they own `launcher/tests/**`). L460-469 requires `@sha256` on every image in the launcher chain (the controller's `python:3.12-slim` must be digest-pinned; the monitoring overlay is outside this test). | — | L | other tests not read |
| `config/hardware-profiles.yaml` | profiles | partial (dgx-spark L80-93) | `dgx-spark` → `compose/compose.dgx-spark.yaml`, main `dgx-qwen36-35b-nvfp4`, router `dgx-qwen3-vl-8b-fp8` (the fallback engine of CONTRACT §1), `concurrency: 4`, `initial_context 262144` (overridden to 1,000,000 by `.env`). | `profiles.py` | L | other profiles not read |
| `config/model-manifest.yaml` | model pins | partial (L185-215) | Main model revision `491c2f1e…` = the running path `…--491c2f1ea524` (no drift). L210 `startup_arguments` `--quantization modelopt --attention-backend flashinfer` present on both ranks. **L211 `health_probe: /v1/models`** — the manifest's notion of health is the one that proves nothing; the launcher's compose probe (2.4 compose.py) is what actually gates. | `profiles.py`, `model_manager.py` | L | other entries not read |

### 2.5 Orchestrator

| path | purpose | reviewed | relevant findings | dependencies | risk | exclusions |
|---|---|---|---|---|---|---|
| `orchestrator/app/resilience.py` | wait-and-retry primitive (the existing outage tolerance; CONTRACT says the breaker composes with it) | yes | L196-248 classification: connection errors and 5xx/429 and a bare `APIError` (dying-stream chunk) are recoverable; 4xx, read timeout, cancellation are not — matches CONTRACT §4 except that `request_timeout` there also covers the **missing TTFT deadline** (see llm.py). L256-275 `engine_answers` = `/health` **and** `/v1/models` 200 — exactly the two signals that stayed green during the wedge, so a retry after `wait_for_engine` lands on a wedged engine again; the breaker's external open on WEDGED/RECOVERING (CONTRACT §8.2) is what closes this. L335-382 `resilient`: on a 5xx it backs off 2-30 s and re-issues **without** consulting `/health` (only `reason == "connection"` waits) — fine for a dead API, wrong for a wedged one. L142-147 interactive window 120 s, L149-173 sidecars get 0 s on interactive turns. L298-308 `llm_engine_wait_seconds` observed into 30 s-capped buckets (see metrics.py). Orchestrator log for the window (`docker logs`, read-only) shows **no** chat call to the head 22:15-22:30Z and `/health` 200 through 22:20 — the orchestrator's users were not on at 03:45 IST; only the second tenant and the watchdog were hit. | `config.py`, `metrics.py`, `context.service_root` | M | — |
| `orchestrator/app/llm.py` | every model call | partial (L36-72, L171-232, L281-330, L652-836; grep of the rest) | L281-316 one `AsyncOpenAI` per (loop, url, key, read): `connect=10 s`, `read=LLM_REQUEST_TIMEOUT` (= `GEN_WALL_CLOCK_S`, 1800 s default), `write=60`, `max_retries=0`. L171-209 `_open_stream` wraps only the **open** in `resilient()`; a streamed request on a wedged engine gets its 200 headers at once, then hangs with **no time-to-first-token deadline** until vLLM's 300 s clock delivers the error chunk (L726 `async for chunk in stream` — the wall clock at L727-748 is 1800 s). Every chat opened during a wedge therefore waits the full 5 min and then fails with `MODEL_UNAVAILABLE` (`main.py:1053-1083`). Fallback eligibility of CONTRACT §8.3 maps onto `chat_completion` (L327), `chat_completion_with_reasoning` (L374), `stream_chat_completion` (L438), `stream_chat_events` (L652), `json_completion` (L950); not `chat_with_tools` (L887), `vision_chat_stream` (L1108). | `resilience.py`, `config.py` | H | L330-651, L837-1269 grep only |
| `orchestrator/app/config.py` | settings | partial (L80-135, L1340-1420; grep) | L88 `OPENAI_BASE_URL` default; L103-107 router = the fallback engine's URL/model (`FALLBACK_BASE_URL`/`FALLBACK_MODEL` defaults per CONTRACT §8.3). L577 `GEN_WALL_CLOCK_S` 1800 (README/memory say 4200 in `.env`; not verified — `.env` not read). L1354-1380 timeouts and the two recovery windows; L1384 `HEALTH_PROBE_TIMEOUT` 2 s. No `LLM_BREAKER_*`, `ENGINE_CONTROLLER_URL`, `FALLBACK_*` keys yet (orchestrator workstream). | — | M | ~1,300 lines not read |
| `orchestrator/app/health.py` | `GET /health` dependency checks | partial (L41-51, L321-410, L462-520, L664-760; grep) | L41-51 `_probe_vllm` = `GET {root}/health` 200 → `ok` — **signal 14 is the same `/health` that stayed 200 in the wedge** (verified in the orchestrator log: 6 × 200 per minute 22:15-22:20Z). L664-703 probes each distinct engine URL once, concurrently, 2 s timeout. L393-409 live generation count, L462-517 work gauges (`chat_requests_interrupted` etc.) published for Prometheus. CONTRACT §7.2 `llm_engine_state_code` and the controller read belong here. | `db.py`, `metrics.py`, `main._live_generations` | M | rest not read |
| `orchestrator/app/metrics.py` | stdlib Prometheus registry | yes | L32 histogram buckets end at **30 s** → `llm_engine_wait_seconds` (120-1200 s) lands only in `+Inf` (remediation §13.4 already noted). L35-119 closed vocabularies exist only for `level, rule, result, purpose, state, job, stage, format, language`; **`what`, `reason`, `outcome`, `engine`, `to`, `category` are unconstrained** (L122-131 `_clean` folds only known keys) — the contract's `llm_breaker_failures_total{engine,reason}`, `llm_fallback_requests_total{outcome}`, `llm_queue_rejections_total{reason}` need their sets added here (orchestrator workstream) or they can mint series. `state` is already reserved with the upload/video vocabulary — a breaker gauge must not reuse that label name. L140-176 `inc/set_gauge/observe` never raise (good). | — | M | — |
| `orchestrator/app/main.py` | API + chat worker | partial (L49-198, L286-460, L973-1335, L1335-1590, L1773-1830; grep) | L104-112 / L162-172 open `chat_requests` rows are marked `interrupted` on start **and** on orderly shutdown (durability invariant holds across deploys). L1053-1083 `_failure_sentence`: `ModelUnavailable`, transport errors and `is_recoverable` → `MODEL_UNAVAILABLE`; read timeouts → `TIMEOUT`. L1095-1120 `_retry_chat_request` / L1544-1587 `/chat` with a known `intent_id`: attach to the live generation, replay the persisted answer, or resume under a **new** `generation_id` with `attempt+1` (`db.resume_chat_request`) — CONTRACT §8.4's "a generation keeps its logical id for its whole life" is satisfied at the `intent_id` level, not the `generation_id` level; per-attempt `engine`, `retry_reason`, `terminal_state` are **not** recorded anywhere today (only `attempt` on the row and in `messages.meta`, L446-450, L1132-1134). L1773-1778 `set_wait_notifier(lambda line: emit("status", {"text": line}))` — the hook for CONTRACT §8.3's two status lines. No automatic pre-first-token retry exists for streams (llm.py). | `db.py`, `llm.py`, `resilience.py`, `core/tracing.py` | M | ~2,000 lines not read |
| `orchestrator/app/db.py` | schema + persistence | partial (L1520-1600 V29, L1810-1819, L4043-4110, L5710-5845) | V29 `chat_requests` (L1574-1593): `intent_id` PK, `generation_id` UNIQUE, `status` CHECK `{accepted,running,completed,failed,cancelled,interrupted}`, `attempt`, `resumable`, `request` (the snapshot to resume from). `add_message` (L4056-4110) is idempotent per `(conversation_id, generation_id)`. `resume_chat_request` (L5763-5798) is CAS on `generation_id`, so two racers cannot both resume. No `chat_request_attempts` table — the durability workstream's V32 (contract §8.4 fields) would go here. | — | M | ~5,700 lines not read |
| `orchestrator/app/core/tracing.py` | `TraceRecorder` (V30) | partial (L1-60) | Keyed by `generation_id`; best-effort; redacts credential-shaped keys. Suitable place for `engine=fallback`/`retry_reason` trace events. | `db.py` | L | rest not read |
| `orchestrator/app/usage.py` | usage_events (V18) | partial (grep) | Records `generation_id` per turn; the `engine_model` of a fallback answer should flow here for analytics. | `db.py` | L | — |

### 2.6 Monitoring

| path | purpose | reviewed | relevant findings | dependencies | risk | exclusions |
|---|---|---|---|---|---|---|
| `monitoring/prometheus/prometheus.yml` | scrape config | yes (monitoring workstream has an uncommitted `engine-controller` job at review time) | L16-18 5 s scrape / 15 s evaluation. L85-95 `vllm-main` via `host.docker.internal:8000` (depends on the host-bound head). L103-127 aux by service name + OCR by `file_sd` (L121-123; `.runtime/prometheus/ocr.json`, present). L157-161 orchestrator `/metrics` public (no session, L153-156). L183-204 blackbox: orchestrator `/health`, frontend, searxng only — **no completion probe of the engine**. `promtool check config`: valid (run with the pinned image). No `alerting:` block → no Alertmanager. | `monitoring/prometheus/rules/*.yml`, `.runtime/prometheus/ocr.json` | M | — |
| `monitoring/prometheus/rules/alerts.yml` | alerts | yes (`promtool check rules`: 30 rules OK) | L44-52 `VllmDown = up{job="vllm-main"}==0 for 1m` — fired only at 22:22 (scrape target gone), i.e. after the outage was already 6 min old; CONTRACT §7.4 renames it `VllmScrapeTargetDown` (warning). L145-171 vllm-health alerts key on waiting/KV/preemptions — none sees frozen tokens with running > 0. L175-181 `ContainerRestartLoop` needs > 3 restarts/30 m. L297-316 `VideoAnalysisStalled` and L318-335 `ChatRequestsLeftInterrupted` are the only "work is stuck" alerts and are application-side. Every alert has fired to nobody (no receiver). | `recording.yml`, exporters | H | — |
| `monitoring/prometheus/rules/recording.yml` | aggregates | yes (`promtool`: 31 rules OK; monitoring workstream has uncommitted additions) | L140-171 `vllm` group: rates and gauges only; no `timestamp()`-based staleness rule and no state derivation at `321cf70` (CONTRACT §7.3 adds `cluster:vllm_metrics_age_seconds`, `cluster:vllm_service_state:code`). No `or vector(0)` anywhere (good). | — | L | — |
| `monitoring/grafana/dashboards/dgx-cluster-overview.json` | live overview | structure only (47 panels, 56 exprs; parsed as JSON) | **The "vLLM DOWN" tile**: stat panel `vLLM`, `expr up{job="vllm-main"}`, value mapping `0→DOWN, 1→UP`, **no `noValue` mapping** → it said UP for the first 5 min of the outage and DOWN from 22:21 to 22:26:30 (`INC/40-prometheus-window.txt`); an absent series renders as the raw "No data" default, not as MONITORING_UNKNOWN. Same pattern on `Cluster`, `Spark 1/2`, `Model services`, `PostgreSQL`, `Application endpoints` (`probe_success`). Every expr references scraped or recorded metrics only. | `recording.yml` | M | panel-by-panel review not done |
| `monitoring/grafana/dashboards/dgx-vllm-performance.json` | vLLM performance | structure only (32 panels, 45 exprs) | `Request rate / success / errors` uses `sum(rate(http_requests_total{service="main",status=~"5.."}[5m])) or vector(0)` — the one `or vector(0)` in the dashboards (CONTRACT §2 forbids it for absence-is-not-zero series; here 5xx=0 when absent is arguably true, but the metric name `http_requests_total` is not one vLLM exports — verify). `Request hopper` plots running/waiting; no "frozen tokens" panel. Spec-decode panels reference `vllm:spec_decode_*` which no longer exist with MTP off (they render empty). | `recording.yml` | L | panel-by-panel review not done |
| `monitoring/grafana/dashboards/{dgx-gpu-thermal-power,dgx-host-containers,dgx-interconnect,dgx-data-stores}.json` | other dashboards | structure only (17/24/13/24 panels) | Parse as JSON; metrics referenced are all scraped or recorded; no `up`-based tiles, no `vector(0)`. `dgx-host-containers` has `container_start_time_seconds` — restart evidence for the head lives here (cAdvisor, Spark 1 only). | — | L | panel-by-panel review not done |
| `monitoring/exporters/dgx-gpu/dgx_gpu_exporter.py` | GB10 GPU exporter | partial (grep + L64-69, L120-123, L173-204, L320-372) | Stdlib `ThreadingHTTPServer`, bind `DGX_GPU_EXPORTER_HOST` (default `0.0.0.0`; the worker overlay passes the management IP), `/metrics` + `/healthz`, `nvidia-smi` via `subprocess.run(timeout=8)`, 1.5 s cache, serves `dgx_gpu_up 0` on failure instead of dying. This is the pattern the controller/sentinel copy (CONTRACT §6 "python:3.12-slim + bind-mounted script, stdlib only, /healthz"). | `nvidia-smi` injected by the runtime | L | metric formatting body not read |
| `monitoring/exporters/data-stores/data_stores_exporter.py` | store sizes/ages | partial (grep) | Same server pattern; read-only volume walks cached 30 s. Not on the incident path. | — | L | — |
| `monitoring/blackbox/blackbox.yml` | `http_2xx` module | yes | GET, 5 s, 200 only, IPv4. A completion probe cannot be expressed here (POST body + streaming) — the controller's canary is the right place, not blackbox. | — | L | — |

---

## 3. Configuration drift check

Method: `docker compose config --format json` with the exact chain recorded in
`.runtime/state.json` (four files, three env files, run from the production
checkout), compared with `docker inspect sf-local-ai-vllm-1` on the head; the
worker's running command taken from `INC/23-worker-healthcheck-cmd.json`;
image pins from `compose/compose.dgx-spark.yaml:34` and
`.runtime/cluster-worker.env`.

| check | rendered / expected | running / observed | verdict |
|---|---|---|---|
| Compose chain | `compose.yaml`, `compose/compose.dgx-spark.yaml`, `compose/compose.published-dgx-spark.yaml`, `compose/compose.cluster-dgx-spark.yaml` (`deploy-common.sh:48-53`) | `state.json compose_files` identical; watchdog container label `config_files` identical | no drift |
| Head argv | 47 tokens rendered from `CLUSTER_ENGINE_ARGS` + overlay L37-46 | 47 tokens (`INC/03-head-cmd.json`, live inspect) — byte-identical | no drift |
| Head `config-hash` | `1ab9234030f36f41…` | `1ab9234030f36f41…` | no drift |
| Head image | `vllm/vllm-openai@sha256:24f2f897…` → local ID `sha256:60d84700…` | container `.Image = sha256:60d84700…` | no drift |
| Head healthcheck | overlay L92-108 (`0.0.0.0:8000/health`, 30 s/15 s/4/30 m) | `INC/03-head-healthcheck.json` identical | no drift |
| Head `init`, network, ulimits, logging | `init: true`, `host`, `memlock -1`, json-file 10m×3 | identical; **`RLIMIT_CORE` unlimited** (no `core` ulimit rendered) | no drift; contract §6.5 unmet at `321cf70` (SRE in flight) |
| Worker argv | head argv minus `--host/--port/--reasoning-parser/--tool-call-parser/--enable-auto-tool-choice`, plus `--node-rank 1 --headless` | `INC/23-worker-healthcheck-cmd.json` — exactly that | no drift |
| Worker image | `CLUSTER_VLLM_IMAGE=vllm/vllm-openai@sha256:24f2f897…` (`.runtime/cluster-worker.env`, generated 2026-09-11 12:58 IST) | `INC/20-worker-docker-ps.txt` shows only the tag `vllm/vllm-openai`; the running image **ID** on the worker is not in the bundle | **not verifiable from the bundle**; `scripts/cluster-verify-engine.sh` or `cluster-sync.sh --image-only` proves it (needs ssh) |
| Worker healthcheck | `compose.cluster-worker.yaml:76-93` (retries 4) | `INC/23-worker-healthcheck-cmd.json` identical, `Retries:4` | no drift |
| MTP / prefix caching | `--no-enable-prefix-caching`, no `--speculative-config` | both ranks | no drift |
| `generated.env` vs running | mtime 2026-09-12 00:40 IST (deploy `deploy-20260912-003953.log`), head container created 07:29Z 09-11 | rendered args equal → no pending engine change | no drift |
| Watchdog | rendered hash `b2655a3a…` | running hash `b2655a3a…`; **created 2026-09-01T22:14:14Z by a manual full-chain `up`**, not by the launcher | no config drift; **lifecycle drift** (not managed by `techsara up`) |
| Docs vs running | `docs/CLUSTER.md:130-135`, `README.md:426-427` say 16 GiB KV | 8 GiB (`--kv-cache-memory-bytes 8589934592`) | **doc drift** |
| Host core pattern | — | `/proc/sys/kernel/core_pattern = \|/usr/share/apport/apport …`, `apport.service active` on both nodes (`INC/32-worker-kernel-apport.txt`, head verified) | the precondition for the 4-minute hold |

---

## 4. Security findings (what is exposed where, and the line that decides it)

Verified on the head with `ss -ltn`, `docker inspect`, `docker ps` (read-only);
worker facts from the bundle and the compose files.

| # | exposure | decided by | severity / note |
|---|---|---|---|
| S1 | **vLLM head API `0.0.0.0:8000`** — unauthenticated chat/completions on the office LAN (192.168.9.54), both RoCE rails and any tailnet; the second tenant uses it from 10.100.184.2 and any LAN host could | `compose/compose.cluster-dgx-spark.yaml:33-41` (host network + `--host ${CLUSTER_API_BIND_ADDRESS}`); `launcher/techsara_cli/cluster.py:666-669` (`publish_model_ports → 0.0.0.0`); `.env PUBLISH_MODEL_PORTS=true` (`generated.env TECHSARA_PUBLISH_MODEL_PORTS=true`) | High. `PUBLISH_MODEL_PORTS=false` would bind the bridge gateway but break the worker healthcheck/watchdog probes (remediation §12). Fix = host firewall rule (`ufw` is installed but `ENABLED=no`) or vLLM `--api-key`; out of this programme's scope, must be recorded in the RUNBOOK/ADR |
| S2 | **torch.distributed rendezvous `*:29501`** (TCPStore) on all interfaces, no authentication | `cluster.py:582-583` (`--master-addr/--master-port`), no bind restriction possible in vLLM; `docs/CLUSTER.md:472-477` | Medium: a LAN host can connect and disturb a rendezvous; only the firewall closes it |
| S3 | **`vllm-watchdog`: Docker socket RW + host network** (root-equivalent) | `compose/compose.dgx-spark.yaml:117-120` | Accepted by design; the controller inherits the same trust (CONTRACT §6) — keep the digest pin and the "only thing it does" rule |
| S4 | **`portainer` on `0.0.0.0:9000` and `[::]:9000` with Docker socket RW** — not from this repo, `Up 31 hours`, image `:latest` | outside the repository (`docker ps`) | High: a root-equivalent web UI on the LAN; flagged in remediation §12, still running |
| S5 | **`zealous_williamson`** — interactive `alpine` with `sf-local-ai_data` mounted RW, `Up 29 hours` | outside the repository | Medium: a stray writer on the production data volume; flagged in remediation §12, still running |
| S6 | **`iperf3 *:5201`** listening on the head (and the worker per remediation §12) | outside the repository | Low |
| S7 | `node-exporter *:9100` on the head (all interfaces) | `compose/compose.monitoring.yaml:140` (`MONITORING_NODE_BIND` default `0.0.0.0`) | Low (read-only telemetry; host details LAN-wide) |
| S8 | Worker-side engines on the management LAN without auth: OCR `192.168.9.68:30004`, whisper `192.168.9.68:30007`, exporters `:9100/:9835` | `compose/compose.ocr.yaml:41`, `compose/compose.whisper.yaml:33`, `compose/compose.monitoring-worker.yaml:32,62` | Medium (OCR/whisper accept arbitrary inference) |
| S9 | Orchestrator `0.0.0.0:8080` (session-authenticated) but **`/health` and `/metrics` are public** (schema version, work gauges, store ages); frontend `0.0.0.0:3000` | `compose.yaml:223,301` (`TECHSARA_BIND_ADDRESS=0.0.0.0`); `monitoring/prometheus/prometheus.yml:153-156` | Low |
| S10 | Prometheus `127.0.0.1:9090`, Grafana `127.0.0.1:3300` (+ public via the Grafana tunnel) | `compose/compose.monitoring.yaml:55,109`; `compose/compose.cloudflare.yaml:101-140` | OK; Grafana needs Cloudflare Access (`docs/MONITORING.md:441-452`) |
| S11 | Aux model ports published on loopback only (`127.0.0.1:8002/8003/8005`) | `compose/compose.published-dgx-spark.yaml:35-56` + `environment.py:421-443` | OK |
| S12 | New surfaces from CONTRACT: controller `0.0.0.0:9838` (GET-only + loopback POST) and sentinel `<RoCE A>:9839` with a **second RW Docker socket on the worker**; `CLUSTER_SENTINEL_TOKEN` optional with source-IP gating | CONTRACT §6.1, §6.4; SRE's in-flight `compose.cluster-worker.yaml` | Accept only with: digest-pinned image, token generated by `cluster-sync.sh` into `secrets.env` (never `worker.env` 0644 — note `cluster-sync.sh:110` writes `worker.env` world-readable; the token must travel in a 0600 file or an env layer the worker compose reads separately), and `launcher/tests/test_compose_overlays.py:429-439` amended deliberately |
| S13 | `scripts/deploy.sh:351` `git clean -qfd -e .env -e .runtime` on the shared production checkout | `scripts/deploy.sh:351` | Medium (data loss of untracked files of other sessions; already bit the fault report) |
| S14 | Worker `worker.env` shipped 0644 (`cluster-sync.sh:110`); contains no secret today (verified key list) | `scripts/cluster-sync.sh:101-112` | OK as long as no token is added to it (see S12) |
| S15 | Head `RLIMIT_CORE` unlimited + apport pipe: a crashing 25 GiB process writes a full core into `/var/crash` (root-owned, 0640) — a memory dump of the model process on disk, and the 4-minute hold | `compose/compose.cluster-worker.yaml:47-48`, `compose/compose.cluster-dgx-spark.yaml:69-70` (no `core`), host `core_pattern` | Medium (availability + a 3.5 GB dump containing prompts on disk) — contract §6.5 |

---

## 5. Exclusions (not read, by decision)

- `node_modules/`, `frontend/.next/`, any built artefact — not part of the availability path.
- `.git/` internals, `.runtime/state.json` beyond `compose_command`/`compose_files`, `.runtime/secrets.env` (never opened), `.env` (never opened; the two cluster keys named in this document come from `generated.env`, which is secret-free by design).
- Model weights (`~/Documents/project/Model`, `/models` in containers), the HF caches, `sf-local-ai_pgdata`, `sf-local-ai_data`, `sf-local-ai_reports`, `sf-local-ai_prometheus`, `sf-local-ai_grafana` volumes — data, not code; not read.
- Uploads, workspaces, brain packs (`brain/packs/**`), report files — private user data; not read.
- Private prompt text: no prompt, conversation, request body or answer was read; the head log's `dump_input.py` line was read only for its request **count** (9 running, 447 scheduled tokens) — request ids and prompts are not reproduced here.
- The second tenant's repository on the worker (`interview-analysis`) — not on this host; only its traffic pattern is cited (9 in-flight requests from 10.100.184.2).
- `orchestrator/app/**` outside the modules named in §2.5 (engines, artifacts, video, knowledge, uploads): not on the engine-availability path except through `llm.py`; `orchestrator/tests/**`: not read.
- `launcher/techsara_cli/{bridge,model_manager,modelshape,utils,errors}.py`, `launcher/tests/**` other than the overlay test: not on the path.
- `.github/workflows/pipeline.yml`, `scripts/{ocr,whisper,tunnel,e2e-stack,backup-knowledge,setup-runner}.sh`, `scripts/*.py` other than `cluster-soak.py`: not on the path.
- `docs/**` other than the files named in §2.1 (upload-reliability, artifact-studio, AUTH, VOICE, 01-codebase): not on the path.

---

## 6. Hypothesis matrix (categories A–L)

Evidence paths are under `INC/` = `/home/techsphere/Documents/project/personal-LLM-Chabot/.runtime/incidents/20260911T223140Z-vllm-down/`
(files: `00-time-and-commit.txt`, `01-container-inspect-summary.txt`, `02-docker-events-24h-raw.txt`, `02-docker-events-24h.txt`, `03-head-cmd.json`, `03-head-healthcheck.json`, `04-cluster-status-passive.txt`, `10-vllm-head-full.log`, `11-watchdog-full.log`, `20-worker-docker-ps.txt`, `21-worker-inspect.txt`, `22-worker-full.log`, `23-worker-healthcheck-cmd.json`, `30-head-kernel-grep.txt`, `31-head-journal-apport.txt`, `32-worker-kernel-apport.txt`, `40-prometheus-window.txt`, `50-head-system-state.txt`, `51-worker-system-state.txt`, `52-swap-timeseries.txt`).

| cat | hypothesis | verdict | evidence |
|---|---|---|---|
| **A** | Worker rank GPU fault in the GDN prefill Triton kernel on a mixed prefill+decode batch (the known, unclosed bug in `0.26.1rc1.dev77`) | **confirmed** (root cause of the fault) | `INC/22-worker-full.log:1538-1603` (`fused_gdn_prefill_post_conv.py:215` ← `qwen_gdn_linear_attn.py:1348`, `misaligned address`), `:1693-1694` (`c10::AcceleratorError`); `INC/32-worker-kernel-apport.txt` (Xid 13 + Xid 31 @0x0 at 03:45:50 IST, pid 1415964); scheduler dump `INC/10-vllm-head-full.log:35425` shows a 439-token prefill chunk scheduled next to 8 single-token decodes (mixed batch); same signature as the 07:05:33Z rank-0 event (`docs/ISSUE/gdn-spec-decode-remediation-2026-09-11.md` §1.1) |
| **B** | Head rank (rank 0) GPU fault | **ruled out** for this event | `INC/30-head-kernel-grep.txt`: the head's only Xid pair is at 12:35:33 IST (= 07:05:33Z, the morning soak fault); nothing at 03:45 IST; `INC/31-head-journal-apport.txt` is empty; head `Worker_TP0` never logged an error, EngineCore died of the RPC timeout only (`INC/10-vllm-head-full.log:35427-35473`) |
| **C** | NCCL / RoCE fabric fault (link flap, TCP fallback, RDMA errors) | **ruled out** as cause; **not fully testable** for the exact minute | Both rails ACTIVE and NCCL `Using network IB` on both nodes after the restart (`INC/04-cluster-status-passive.txt`, `INC/10-vllm-head-full.log:35649` NCCL init 0.26 s); no `NCCL WARN`/`Socket` line in either log in the window (grep); RoCE error counters and `node_infiniband_*` for 22:10-22:30 are **not in the bundle** (Prometheus retains them — `node:roce_errors_per_second:sum` over the window would close this) |
| **D** | Unified-memory pressure / swap on either node caused or prolonged the fault | **ruled out** as cause; **still possible** as a chronic latency risk on the head | Worker: 42.6 GB available, `pswpout` 0 until 22:20 (`INC/52-swap-timeseries.txt`), no `NV_ERR_NO_MEMORY` in the window (`INC/32-worker-kernel-apport.txt` shows one at 09:37 IST only). Head: MemAvailable fell to 17.9 GB at 22:10 with 20.7 pages/s swap-out — the head was swapping *before* the fault but the fault was on the worker; the head's `NV_ERR_NO_MEMORY` lines are the 07:33Z start-up retries (`INC/30-head-kernel-grep.txt`, 13:03 IST). Post-restart head: 97 GB used / 24 GB available / 13 GB swap (`INC/50-head-system-state.txt`) |
| **E** | Thermal or power throttling | **not testable** from the bundle; **ruled out** by the standing analysis | No GPU temperature/throttle samples for 22:10-22:30 in the bundle (`dgx_gpu_temperature_celsius`, `dgx_gpu_throttle_active` are in Prometheus); post-event readings 52 °C / 35 W head, 38 °C / 11 W worker (`INC/50-*`, `INC/51-*`); `docs/ISSUE/gdn-spec-decode-fault-report.md` §8 measured 35-44 °C with no throttle events |
| **F** | apport core dump held the dead rank process, delaying the executor's detection | **confirmed** (4 min 51 s of the outage) | `INC/32-worker-kernel-apport.txt`: `core_pattern` = apport pipe, `apport active/enabled`, `/var/crash/_usr_bin_python3.12.0.crash` 3,509,336,530 B written 03:45 → 03:49 IST; `INC/22-worker-full.log:1734` `died unexpectedly` only at 22:20:41; worker `ulimits` has no `core` (`compose/compose.cluster-worker.yaml:47-48`); head twin verified `RLIMIT_CORE unlimited` |
| **G** | Health signals were structurally blind: `/health`, `/v1/models`, `/metrics`, `up` all 200/1 while no token moved | **confirmed** (5 min 0 s invisible) | `INC/40-prometheus-window.txt`: `up{vllm-main}=1` until 22:21:00, `generation_tokens_total` frozen at 2,138,504 from 22:16:00, `num_requests_running=9`, `scrape_duration` normal; head log: 10 × `GET /health … 200` per minute 22:15-22:20 and `GET /v1/models … 200` throughout; orchestrator log (live, read-only): 6 × `GET http://vllm:8000/health 200` per minute 22:15-22:20Z; the only wedge signatures were `Avg generation throughput: 0.0 tokens/s, Running: 9` (`:35283`, once) and `No available shared memory broadcast block` (`:35307,35336,35365,35395`, INFO level) |
| **H** | The shell watchdog cannot act in this fault class (timing + threshold) | **confirmed** | `INC/11-watchdog-full.log`: `1/2` at 22:18:15, then `fast HTTP error (94s) - not counted` at 22:20:49; `compose/compose.dgx-spark.yaml:144,153,159` (120 s probe, `EL < 100` discarded, 2 strikes) → minimum ~6 min to fire > vLLM's 300 s clock; it acted only in the NCCL-spin events of 09-01/02/09 |
| **I** | Head teardown hang: the API closed its listener but `vllm serve` did not exit, so the container healthcheck needed 4 × 30 s before `kill -9` | **confirmed** (~1 min 58 s) | `INC/10-vllm-head-full.log:35492-35502` (`Shutting down` … `MPClient: complete` 22:20:49.876, then silence until 22:22:56); `INC/03-head-healthcheck.json` (`Retries 4`, `Interval 30 s`, 000 → 4 misses); `INC/01-container-inspect-summary.txt` (`finished 22:22:47.513`, `started 22:22:47.688`, `restarts=1`); `docs/CLUSTER.md:404-407` ("closes its listener without exiting") |
| **J** | Worker rejoined the *old* head's TCPStore and had to fail and restart again (the "old worker can never rejoin" race) | **confirmed** (+1 restart, ~1 min 25 s) | `INC/22-worker-full.log:1752` (rank 1 `distributed_init_method=tcp://10.100.184.1:29501` at 22:21:22 while the old head was still alive), `:1753-1794` (`Connection reset by peer` 22:22:44 when the old head was killed), `:1824-1837` (third start 22:22:51); `INC/21-worker-inspect.txt` `restarts=2`; CONTRACT §6.3 orders the worker restart first for this reason |
| **K** | The second tenant's concurrency-10 mixed load (long prefills + many decodes in flight) is the trigger condition | **confirmed** as the trigger; **still possible** that other mixed loads trigger it (the soak did) | `INC/10-vllm-head-full.log:35483-35491` (9 of the 10 HTTP 500s went to 10.100.184.2), `:35425` (9 running requests, 439-token prefill chunk in a decode batch), `:35278` (755 prompt tok/s + 9 running just before); `scripts/cluster-soak.py` reproduced the fault at 27 min at concurrency 10 (remediation §7 E); the fault does not need MTP (§1.1) |
| **L** | Configuration drift or a wrong flag (MTP back on, prefix caching on, mismatched ranks, wrong image) | **ruled out** | §3 above: head rendered == running (47 tokens), worker argv identical modulo `--node-rank 1 --headless`, `--no-enable-prefix-caching` and no `--speculative-config` on both (`INC/03-head-cmd.json`, `INC/23-worker-healthcheck-cmd.json`), same image digest pinned on both (`compose.dgx-spark.yaml:34`, `.runtime/cluster-worker.env`); worker running image ID not in the bundle (see §7) |

Net: A is the fault; F + G + H + I + J are the five independent reasons the
fault cost 10 m 59 s instead of the ~5 m 20 s a cold reload needs; K is the
load shape that reaches A; B, C, D, E, L are excluded for this event.

---

## 7. Evidence gaps in the bundle (for the docs/incident workstream)

1. `INC/02-docker-events-24h.txt` is empty and `02-docker-events-24h-raw.txt` covers only 04:00:14-04:02:15 IST (post-incident `exec_die` noise): the `docker events` capture was started without `--since`. The container start/die/kill events for 22:20-22:23Z are lost; `docker inspect` timestamps (`INC/01-*`, `INC/21-*`) stand in.
2. The worker's running image **ID** is not in the bundle (`20-worker-docker-ps.txt` shows the tag only); `scripts/cluster-verify-engine.sh` proves it.
3. The head healthcheck's own `State.Health.Log` (the four misses and the "restarting the head" line) was not captured before the restart cleared it.
4. RoCE error counters, GPU temperature/throttle and `node_infiniband_*` for the window are in Prometheus but not in the bundle (categories C and E).
5. `31-head-journal-apport.txt` is empty — fine (no apport event on the head), but a note saying so would prevent the file being read as a capture failure.
6. The orchestrator log for the window still exists (container started 19:10Z, json-file 10m×3) and shows no user chat during the outage — worth stating in the incident report so the user-impact statement is evidence-based.
7. No `scripts/cluster-status.sh` or `cluster-verify-engine.sh` run during the wedge exists — only the post-restart `04-cluster-status-passive.txt` (04:04 IST); the controller's `/state` capture step (CONTRACT §6.3 CAPTURE_DIAGNOSTICS) is what will provide this next time.

---

## 8. Cross-workstream notes (changes this review needs from owners; nothing here was edited)

- **controller**: add the head-side signatures `No available shared memory broadcast block` (INFO) and `Avg generation throughput: 0.0 tokens/s, Running: N` to the wedge evidence the controller captures; treat `/health` + `/v1/models` as never sufficient (§6 G); restart order worker→head (§6 J) and `restart?t=5` (worker `stop_grace_period 2m`).
- **sre**: `ulimits.core: 1` on head/worker/router/OCR (§6 F; in flight); start the controller from the launcher (`cli.py`) — the watchdog was never launcher-managed and `techsara down` removes it (§2.2); amend `launcher/tests/test_compose_overlays.py:429-439` and pin the controller image by digest (L460-469); do not put `CLUSTER_SENTINEL_TOKEN` into the 0644 `worker.env` (S12/S14); ship `sentinel.py`/`common.py` in `cluster-sync.sh:111`; take `engine-lock.sh` in `cluster-up.sh`, `cluster-down.sh`, `deploy.sh:410-417` (`--full`) and ideally in `cli.py` before the pair restart; consider a `core_pattern`/`RLIMIT_CORE` check in `cluster-doctor.sh`.
- **orchestrator**: a time-to-first-token deadline on streams (`llm.py:726`) or the breaker's external open is required for the wedge class — today every chat opened during a wedge waits 300 s; `engine_answers` (`resilience.py:256-275`) must not be the readiness gate once the controller state exists; add closed vocabularies for `engine`, `reason`, `outcome`, `category` in `metrics.py:_ALLOWED` and wider buckets for `llm_engine_wait_seconds`; do not reuse the reserved `state` label name for the breaker gauge.
- **durability**: per-attempt `engine`, `retry_reason`, `terminal_state` do not exist (`db.py` V29, `main.py:446-450`); `resume_chat_request` changes `generation_id` per attempt — decide whether CONTRACT §8.4's "logical id" is `intent_id` (today's reality) and say so.
- **monitoring**: `VllmDown` → `VllmScrapeTargetDown` (§2.6); the overview `vLLM` stat needs a `noValue` mapping (MONITORING_UNKNOWN) and the `http_requests_total … or vector(0)` panel needs a check that the metric exists; `NcclTcpFallback` is not observable in Prometheus (only `scripts/cluster-logs.sh nccl` / `cluster-status.sh`) — document as such; no Alertmanager exists, so every alert is silent until a receiver is configured (out of contract scope, must be stated in `docs/MONITORING.md`).
- **docs**: fix the 16 GiB KV figures (`docs/CLUSTER.md:130-135`, `README.md:426-427`) and the "584 s self-heal" claim (`README.md:373-375`); CHANGELOG has no entries for 09-02..09-10.
- **Tooling note**: `shellcheck` is **not installed** on this host (`command not found`); CONTRACT §10 "Shell: shellcheck clean" cannot be evidenced here without installing it or running it in a container. `promtool` (pinned image) and `docker compose config` work.
