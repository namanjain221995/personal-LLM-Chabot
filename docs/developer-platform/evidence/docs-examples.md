# Documentation examples — executed against the isolated stack

Generated by `scripts/docs_examples_run.py`. Do not edit by hand: re-run the script.

* **Run:** 2026-09-13 00:52:59 → 00:54:23 UTC
* **Tree:** branch `feat/developer-platform-security` at `4e28fcc` plus uncommitted changes in the working tree (the worktree the isolated stack was built from)
* **Stack:** the isolated e2e stack — public edge `http://127.0.0.1:3001/v1` (frontend), orchestrator `http://127.0.0.1:8081`. Never production.
* **Identity:** signed in as `e2e-devadmin@test.local`; project `proj_56323ee19b3f24ed7a4e2728` created for this run with one `tsk_test_` key (models.read, responses.read, responses.write, usage.read) and one models.read-only key. Neither key is printed anywhere.
* **Samples found:** 84 fenced blocks across 19 pages (loaded as rendered: the page records bundled by esbuild and evaluated in node).
* **Verdicts:** 48 PASSED, 0 FAILED, 0 NOT RUN, 36 NOT EXECUTABLE (0 of which disagree with the real API).

## How a sample is run

* **bash** — `bash -c`, with `curl` wrapped as a shell function that appends `-w '\n@@HTTP %{http_code}@@\n'`, so every curl call's status is read from the wire.
* **python** — `/home/techsphere/Documents/project/personal-LLM-Chabot/orchestrator/.venv/bin/python`; `httpx.Client.send` is wrapped to print each status to stderr. A sample importing a package that is not installed is NOT RUN, never passed.
* **typescript** — types stripped by the frontend's esbuild (`--loader=ts --format=esm`), run by node v20.20.2; `fetch` is wrapped to print each status to stderr.
* **Changes to a sample** are listed per sample below. The documented base URL becomes the isolated edge; a placeholder key in bash becomes `$TECHSARA_API_KEY`; the example response id becomes a real one; a fragment gets the earlier blocks it calls prepended and, if it only defines a function, a driver that calls it.
* **NOT EXECUTABLE** blocks are still checked where the real API can check them: every documented JSON shape is compared field-by-field (and `object`/`type`/`code`/`param` values) with a real object of the same kind captured in this run, headers and event orders with a real stream, the webhook verifiers with the server's own signer.

## Results

| # | Page | Language | First line | Verdict | Evidence |
| --- | --- | --- | --- | --- | --- |
| 1 | TechSara developer platform (`overview` #1) | bash | `export TECHSARA_API_KEY="tsk_live_…"   # your key, from the console` | **PASSED** | exit 0, HTTP [200]; object=list (0.0s) |
| 2 | Quickstart (`quickstart` #1) | bash | `export TECHSARA_API_KEY="tsk_live_0123456789abcdef_EXAMPLE_KEY_DO_NOT_` | **PASSED** | exit 0, HTTP []; printed 7 chars: KEY_SET (0.0s) |
| 3 | Quickstart (`quickstart` #2) | bash | `curl https://ai.techsarasolutions.com/v1/responses \` | **PASSED** | exit 0, HTTP [200]; object=response status=completed id=resp_6dd7c3fd66a9a373d8c1b2c8; 353 chars of output text (0.7s) |
| 4 | Quickstart (`quickstart` #3) | json | `{` | **NOT EXECUTABLE** | a documented response shape — cross-check: every documented field occurs on a real response completed (HTTP 200) |
| 5 | Quickstart (`quickstart` #4) | python | `import os` | **PASSED** | exit 0, HTTP [200]; printed 264 chars: Retrieval-augmented generation (RAG) is an AI framework that enhances large language models by first retrieving relevant… (0.7s) |
| 6 | Quickstart (`quickstart` #5) | typescript | `const response = await fetch("https://ai.techsarasolutions.com/v1/resp` | **PASSED** | exit 0, HTTP [200]; printed 370 chars: Retrieval-augmented generation (RAG) is an AI framework that enhances large language models by first retrieving relevant… (0.8s) |
| 7 | Authentication (`authentication` #1) | http | `Authorization: Bearer tsk_live_0123456789abcdef_EXAMPLE_KEY_DO_NOT_USE` | **NOT EXECUTABLE** | an HTTP line, not a program — the same line is sent or checked by the executed samples — cross-check: the documented example key sent as this header → HTTP 401 code=invalid_api_key |
| 8 | Authentication (`authentication` #2) | json | `{` | **NOT EXECUTABLE** | a documented response shape — cross-check: every documented field occurs on a real error scope (HTTP 403) |
| 9 | Authentication (`authentication` #3) | http | `WWW-Authenticate: Bearer error="insufficient_scope", scope="responses.` | **NOT EXECUTABLE** | a response header — cross-check: a models.read-only key POSTing /responses through the edge → HTTP 403, header matches; the same request at the origin http://127.0.0.1:8081 → HTTP 403, header matches |
| 10 | Authentication (`authentication` #4) | bash | `export TECHSARA_API_KEY="tsk_live_…"   # your key, from the console` | **PASSED** | exit 0, HTTP [200]; headers present: x-request-id (0.0s) |
| 11 | API-key security (`key-security` #1) | text | `tsk_live_0123456789abcdef_EXAMPLE_KEY_DO_NOT_USE_THIS_IS_NOT_A_SECRETE` | **NOT EXECUTABLE** | an annotated diagram of the key format — cross-check: diagrammed layout (prefix, 16-hex public id, 43-char secret, 6-char checksum) matches the documented example: True; matches the real key minted for this run: True |
| 12 | API-key security (`key-security` #2) | text | `rotate  ──►  new key issued, old key still valid for the overlap` | **NOT EXECUTABLE** | a prose diagram of the rotation window |
| 13 | Model reference (`models` #1) | bash | `export TECHSARA_API_KEY="tsk_live_…"   # your key, from the console` | **PASSED** | exit 0, HTTP [200]; object=list; body names techsara-35b (0.0s) |
| 14 | Model reference (`models` #2) | json | `{` | **NOT EXECUTABLE** | a documented response shape — cross-check: every documented field occurs on a real models (HTTP 200) |
| 15 | Model reference (`models` #3) | bash | `curl https://ai.techsarasolutions.com/v1/models/techsara-35b \` | **PASSED** | exit 0, HTTP [200]; object=model status=available (0.0s) |
| 16 | Model reference (`models` #4) | json | `{ "model": "techsara-35b", "input": "…" }` | **NOT EXECUTABLE** | a request-body fragment whose input is elided ("…") |
| 17 | The Responses API (`responses` #1) | http | `POST /v1/responses` | **NOT EXECUTABLE** | an HTTP line, not a program — the same line is sent or checked by the executed samples |
| 18 | The Responses API (`responses` #2) | json | `{` | **PASSED** | exit 0, HTTP 200; 333 SSE events received, 329 output_text.delta, one terminal response.completed (3.3s) |
| 19 | The Responses API (`responses` #3) | json | `{` | **PASSED** | exit 0, HTTP [200]; object=response status=completed id=resp_ea396a47c3c83cb9c392e286; 161 chars of output text (0.5s) |
| 20 | The Responses API (`responses` #4) | json | `{` | **NOT EXECUTABLE** | a documented response shape — cross-check: every documented field occurs on a real error top p (HTTP 400) |
| 21 | The Responses API (`responses` #5) | json | `{` | **NOT EXECUTABLE** | a documented response shape — cross-check: every documented field occurs on a real response completed (HTTP 200) |
| 22 | The Responses API (`responses` #6) | http | `GET /v1/responses/{id}` | **NOT EXECUTABLE** | an HTTP line, not a program — the same line is sent or checked by the executed samples |
| 23 | The Responses API (`responses` #7) | bash | `export TECHSARA_API_KEY="tsk_live_…"   # your key, from the console` | **PASSED** | exit 0, HTTP [200]; object=response status=completed id=resp_b00ab838c6a70c7bb8e87904; 1840 chars of output text (0.0s) |
| 24 | The Responses API (`responses` #8) | http | `POST /v1/responses/{id}/cancel` | **NOT EXECUTABLE** | an HTTP line, not a program — the same line is sent or checked by the executed samples |
| 25 | The Responses API (`responses` #9) | bash | `curl -X POST https://ai.techsarasolutions.com/v1/responses/resp_4f2b8c` | **PASSED** | exit 0, HTTP [200]; object=response status=in_progress id=resp_fe53ab6063252a510dd78b99 (0.0s) |
| 26 | Chat Completions compatibility (`chat-completions` #1) | http | `POST /v1/chat/completions` | **NOT EXECUTABLE** | an HTTP line, not a program — the same line is sent or checked by the executed samples |
| 27 | Chat Completions compatibility (`chat-completions` #2) | bash | `export TECHSARA_API_KEY="tsk_live_…"   # your key, from the console` | **PASSED** | exit 0, HTTP [200]; object=chat.completion (0.8s) |
| 28 | Chat Completions compatibility (`chat-completions` #3) | json | `{` | **NOT EXECUTABLE** | a documented response shape — cross-check: every documented field occurs on a real chat (HTTP 200) |
| 29 | Chat Completions compatibility (`chat-completions` #4) | text | `data: {"id":"…","object":"chat.completion.chunk","choices":[{"index":0` | **NOT EXECUTABLE** | a stream transcript — cross-check: compared with a real chat stream (3 chunks, HTTP 200): every documented chunk shape occurs |
| 30 | Chat Completions compatibility (`chat-completions` #5) | text | `data: {"id":"…","object":"chat.completion.chunk","choices":[],"usage":` | **NOT EXECUTABLE** | a failure-path transcript: the stream failure cannot be provoked on demand against a healthy engine |
| 31 | Streaming (`streaming` #1) | bash | `export TECHSARA_API_KEY="tsk_live_…"   # your key, from the console` | **PASSED** | exit 0, HTTP 200; 724 SSE events received, 720 output_text.delta, one terminal response.completed (8.7s) |
| 32 | Streaming (`streaming` #2) | http | `Content-Type: text/event-stream` | **NOT EXECUTABLE** | response headers — cross-check: every documented header has the documented value on a real stream through the edge (content-type: text/event-stream; charset=utf-8; cache-control: no-store, no-cache, no-transform; x-accel-buffering: no; connection: keep-alive) |
| 33 | Streaming (`streaming` #3) | text | `response.created` | **NOT EXECUTABLE** | the event order, as a list — cross-check: real stream order (repeats collapsed): response.created → response.in_progress → response.output_text.delta → response.output_text.done → response.completed |
| 34 | Streaming (`streaming` #4) | text | `event: response.created` | **NOT EXECUTABLE** | a stream transcript — cross-check: every documented frame's fields occur on the same event in a real stream |
| 35 | Streaming (`streaming` #5) | text | `: ping` | **NOT EXECUTABLE** | the heartbeat comment line, sent only on a 15 s idle gap |
| 36 | Streaming (`streaming` #6) | text | `event: response.failed` | **NOT EXECUTABLE** | a failure-path transcript: the stream failure cannot be provoked on demand against a healthy engine |
| 37 | Streaming (`streaming` #7) | text | `event: error` | **NOT EXECUTABLE** | a failure-path transcript: the stream failure cannot be provoked on demand against a healthy engine |
| 38 | Streaming (`streaming` #8) | python | `import json` | **PASSED** | exit 0, HTTP [200]; printed 3682 chars: **RAG** stands for **Retrieval-Augmented Generation**. It is a technique used in artificial intelligence, specifically w… (9.9s) |
| 39 | Streaming (`streaming` #9) | typescript | `const response = await fetch('${BASE_URL}/responses', {` | **PASSED** | exit 0, HTTP [200]; printed 4173 chars: **RAG** stands for **Retrieval-Augmented Generation**. It is a technique used in artificial intelligence, specifically w… (9.7s) |
| 40 | Background responses (`background` #1) | bash | `export TECHSARA_API_KEY="tsk_live_…"   # your key, from the console` | **PASSED** | exit 0, HTTP [202]; object=response status=queued id=resp_8c5ff86b27d4b77f12643949 (0.0s) |
| 41 | Background responses (`background` #2) | json | `{` | **NOT EXECUTABLE** | a documented response shape — cross-check: every documented field occurs on a real background created (HTTP 202) |
| 42 | Background responses (`background` #3) | bash | `curl https://ai.techsarasolutions.com/v1/responses/resp_4f2b8c1d9e0a7b` | **PASSED** | exit 0, HTTP [200]; object=response status=completed id=resp_b00ab838c6a70c7bb8e87904; 1840 chars of output text (0.0s) |
| 43 | Background responses (`background` #4) | python | `import time` | **PASSED** | exit 0, 2 polls, all HTTP 200; printed 16 chars: STATUS completed (3.2s) |
| 44 | Background responses (`background` #5) | bash | `curl -X POST https://ai.techsarasolutions.com/v1/responses/resp_4f2b8c` | **PASSED** | exit 0, HTTP [200]; object=response status=in_progress id=resp_20c4ba9f02da900aa07be676 (0.0s) |
| 45 | Webhooks (`webhooks` #1) | json | `{` | **NOT EXECUTABLE** | webhook verification needs a delivery; the SSRF guard refuses a loopback receiver and the isolated stack has no public HTTPS endpoint |
| 46 | Webhooks (`webhooks` #2) | http | `TechSara-Signature: t=1789200000,v1=9f86d081884c7d65…` | **NOT EXECUTABLE** | a request header on a delivery — cross-check: the server signer (app/apiplatform/webhooks/signer.py) emits t=<unix>,v1=<64 hex>: True |
| 47 | Webhooks (`webhooks` #3) | python | `import hmac` | **NOT EXECUTABLE** | webhook verification needs a delivery; the SSRF guard refuses a loopback receiver and the isolated stack has no public HTTPS endpoint — verified locally against the server's own signer instead — cross-check: local check against the server signer: {'signed': True, 'rotated_old_secret': True, 'tampered': False, 'wrong_secret': False, 'stale': False} |
| 48 | Webhooks (`webhooks` #4) | typescript | `import { createHmac, timingSafeEqual } from "node:crypto";` | **NOT EXECUTABLE** | webhook verification needs a delivery; the SSRF guard refuses a loopback receiver and the isolated stack has no public HTTPS endpoint — verified locally against the server's own signer instead — cross-check: local check against the server signer: {'signed': True, 'rotated_old_secret': True, 'tampered': False, 'wrong_secret': False, 'stale': False} |
| 49 | Errors (`errors` #1) | json | `{` | **NOT EXECUTABLE** | a documented response shape — cross-check: every documented field occurs on a real error 401 (HTTP 401) |
| 50 | Errors (`errors` #2) | python | `import random` | **PASSED** | exit 0, HTTP [200]; printed 16 chars: STATUS completed (0.3s) |
| 51 | Rate limits (`rate-limits` #1) | http | `RateLimit: "requests";r=41;t=23` | **NOT EXECUTABLE** | example header values — cross-check: real headers on GET /models: RateLimit: '"requests";r=59;t=1'; RateLimit-Policy: '"requests";q=60;w=60, "concurrency";q=4;qu="concurrent-requests"' |
| 52 | Idempotency (`idempotency` #1) | bash | `export TECHSARA_API_KEY="tsk_live_…"   # your key, from the console` | **PASSED** | exit 0, HTTP [200]; object=response status=completed id=resp_47b9446d89eb61a64f339c46; 3096 chars of output text (8.9s) |
| 53 | Idempotency (`idempotency` #2) | python | `import uuid` | **PASSED** | exit 0, HTTP [200]; printed 16 chars: STATUS completed (0.8s) |
| 54 | Usage reporting (`usage` #1) | http | `GET /v1/usage` | **NOT EXECUTABLE** | an HTTP line, not a program — the same line is sent or checked by the executed samples |
| 55 | Usage reporting (`usage` #2) | bash | `export TECHSARA_API_KEY="tsk_live_…"   # your key, from the console` | **PASSED** | exit 0, HTTP [200]; object=list (0.0s) |
| 56 | Usage reporting (`usage` #3) | json | `{` | **NOT EXECUTABLE** | a documented response shape — cross-check: every documented field occurs on the real /usage (HTTP 200, 1 day rows) |
| 57 | Tool calling (`tools` #1) | json | `{` | **NOT EXECUTABLE** | a documented response shape — cross-check: every documented field occurs on a real model (HTTP 200) |
| 58 | Tool calling (`tools` #2) | json | `{` | **NOT EXECUTABLE** | a documented response shape — cross-check: every documented field occurs on a real error tools (HTTP 400) |
| 59 | Tool calling (`tools` #3) | python | `instructions = (` | **PASSED** | exit 0, HTTP [200]; printed 47 chars: PARSED {'intent': 'refund', 'confidence': 0.95} (0.5s) |
| 60 | Python (`python` #1) | bash | `pip install httpx` | **PASSED** | pip install → the venv's pip with --dry-run: exit 0, HTTP []; printed 1193 chars: Requirement already satisfied: httpx in /home/techsphere/Documents/project/personal-LLM-Chabot/orchestrator/.venv/lib/py… (0.2s) |
| 61 | Python (`python` #2) | python | `"""A minimal TechSara client. One place for the base URL, the key and ` | **PASSED** | exit 0, HTTP [200]; printed 27 chars: MODELS 200 ['techsara-35b'] (0.2s) |
| 62 | Python (`python` #3) | python | `def ask(question: str) -> str:` | **PASSED** | exit 0, HTTP [200]; printed 11 chars: ANSWER Mars (0.3s) |
| 63 | Python (`python` #4) | python | `messages = [` | **PASSED** | exit 0, HTTP [200]; printed 16 chars: STATUS completed (0.7s) |
| 64 | Python (`python` #5) | python | `import json` | **PASSED** | exit 0, HTTP [200]; printed 3872 chars: **Retrieval-Augmented Generation (RAG)** is a technique that combines the capabilities of large language models (LLMs) w… (11.0s) |
| 65 | Python (`python` #6) | python | `import random` | **PASSED** | exit 0, HTTP [200]; printed 16 chars: STATUS completed (0.3s) |
| 66 | Python (`python` #7) | python | `import uuid` | **PASSED** | exit 0, HTTP [202]; printed 32 chars: ID resp_df03400dda5d8e117ad8d523 (0.2s) |
| 67 | JavaScript and TypeScript (`javascript` #1) | typescript | `// NEVER do this. A key in browser code is a key published to everyone` | **NOT EXECUTABLE** | an anti-pattern (a key in browser code) shown so the reader does not do it |
| 68 | JavaScript and TypeScript (`javascript` #2) | typescript | `export interface Usage {` | **NOT EXECUTABLE** | type declarations only — cross-check: esbuild compiles the declarations |
| 69 | JavaScript and TypeScript (`javascript` #3) | typescript | `const BASE_URL = "https://ai.techsarasolutions.com/v1";` | **PASSED** | exit 0, HTTP [200]; printed 11 chars: ANSWER Mars (0.3s) |
| 70 | JavaScript and TypeScript (`javascript` #4) | typescript | `export async function* streamText(question: string): AsyncGenerator<st` | **PASSED** | exit 0, HTTP [200]; printed 13 chars: DELTAS 39 213 (0.6s) |
| 71 | JavaScript and TypeScript (`javascript` #5) | typescript | `const RETRYABLE = new Set([` | **PASSED** | exit 0, HTTP [200]; printed 16 chars: STATUS completed (0.3s) |
| 72 | JavaScript and TypeScript (`javascript` #6) | typescript | `await fetch('${BASE_URL}/responses', {` | **PASSED** | exit 0, HTTP [202]; printed 0 chars:  (0.2s) |
| 73 | cURL (`curl` #1) | bash | `export TECHSARA_API_KEY="tsk_live_…"` | **PASSED** | exit 0, HTTP []; printed 7 chars: ENV_SET (0.0s) |
| 74 | cURL (`curl` #2) | bash | `curl "$TECHSARA_BASE_URL/models" \` | **PASSED** | exit 0, HTTP [200, 200]; object=model status=available (0.0s) |
| 75 | cURL (`curl` #3) | bash | `curl "$TECHSARA_BASE_URL/responses" \` | **PASSED** | exit 0, HTTP [200]; object=response status=completed id=resp_48902d216f47d27f95542be4; 353 chars of output text (0.7s) |
| 76 | cURL (`curl` #4) | bash | `curl "$TECHSARA_BASE_URL/responses" \` | **PASSED** | exit 0, HTTP [200]; object=response status=completed id=resp_3bac32fa08f2518d07ced45a; 273 chars of output text (0.6s) |
| 77 | cURL (`curl` #5) | bash | `curl -N "$TECHSARA_BASE_URL/responses" \` | **PASSED** | exit 0, HTTP 200; 643 SSE events received, 639 output_text.delta, one terminal response.completed (6.1s) |
| 78 | cURL (`curl` #6) | bash | `curl "$TECHSARA_BASE_URL/responses" \` | **PASSED** | exit 0, HTTP [202, 200]; object=response status=completed id=resp_b00ab838c6a70c7bb8e87904; 1840 chars of output text (0.0s) |
| 79 | cURL (`curl` #7) | bash | `curl -X POST "$TECHSARA_BASE_URL/responses/resp_4f2b8c1d9e0a7b6c5d4e3f` | **PASSED** | exit 0, HTTP [200]; object=response status=in_progress id=resp_6040caf487614cb3d7766645 (0.0s) |
| 80 | cURL (`curl` #8) | bash | `curl "$TECHSARA_BASE_URL/usage" \` | **PASSED** | exit 0, HTTP [200]; object=list (0.0s) |
| 81 | cURL (`curl` #9) | bash | `curl "$TECHSARA_BASE_URL/openapi.json"` | **PASSED** | exit 0, HTTP [200]; body names "openapi" (0.0s) |
| 82 | cURL (`curl` #10) | bash | `curl -i "$TECHSARA_BASE_URL/models" \` | **PASSED** | exit 0, HTTP [200]; headers present: x-request-id, ratelimit, ratelimit-policy (0.0s) |
| 83 | cURL (`curl` #11) | bash | `curl -sS -i "$TECHSARA_BASE_URL/responses" \` | **PASSED** | demonstrates the documented 400 — judged by that outcome: exit 0, HTTP [400]; body names "param":"top_p" (0.0s) |
| 84 | API status (`status` #1) | bash | `export TECHSARA_API_KEY="tsk_live_…"   # your key, from the console` | **PASSED** | exit 0, HTTP [200]; headers present: x-request-id (0.0s) |

## What the harness changed, per executed sample

* **#1 overview #1** — https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#2 quickstart #1** — harness driver after the block: test "${#TECHSARA_API_KEY}" -gt 40 && echo KEY_SET; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#3 quickstart #2** — https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#5 quickstart #4** — https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#6 quickstart #5** — https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#10 authentication #4** — https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#13 models #1** — https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#15 models #3** — https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#18 responses #2** — POSTed verbatim as the body of http://127.0.0.1:3001/v1/responses
* **#19 responses #3** — POSTed verbatim as the body of http://127.0.0.1:3001/v1/responses
* **#23 responses #7** — resp_4f2b8c1d9e0a7b6c5d4e3f20 → a completed background response created by this run; https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#25 responses #9** — resp_4f2b8c1d9e0a7b6c5d4e3f20 → a background response created seconds earlier (still running); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#27 chat-completions #2** — https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#31 streaming #1** — https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#38 streaming #8** — https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#39 streaming #9** — harness setup before the block: const BASE_URL = "http://127.0.0.1:3001/v1"; ⏎ const apiKey = process.env.TECHSARA_API_KEY;
* **#40 background #1** — https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#42 background #3** — resp_4f2b8c1d9e0a7b6c5d4e3f20 → a completed background response created by this run; https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#43 background #4** — prepended python block 2 (defines what this block calls); harness driver after the block: with client() as api: ⏎     body = wait_for("<real id>", api) ⏎ print("STATUS", body["status"]); {RID} → a background response created seconds earlier; https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#44 background #5** — resp_4f2b8c1d9e0a7b6c5d4e3f20 → a background response created seconds earlier (still running); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#50 errors #2** — prepended python block 2 (defines what this block calls); harness driver after the block: with client() as api: ⏎     body = send_with_retry(api, {"model": MODEL, "input": "Name one ocean."}) ⏎ print("STATUS", body["status"]); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#52 idempotency #1** — https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#53 idempotency #2** — prepended python block 2 (defines what this block calls); harness driver after the block: with client() as api: ⏎     body = summarise("docs-run-20260913005259-idempotency-page", "The office is closed on Friday for maintenance.", api) ⏎ print("STATUS", body.get("status")); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#55 usage #2** — https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#59 tools #3** — prepended python block 2 (defines what this block calls); harness setup before the block: import json ⏎ api = client() ⏎ customer_message = "I was charged twice for order 1182 and want my money back."; harness driver after the block: print("PARSED", parsed); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#60 python #1** — placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment); pip install httpx → the orchestrator venv's pip, --dry-run (nothing installed or changed)
* **#61 python #2** — harness driver after the block: with client() as api: ⏎     r = api.get("/models") ⏎ print("MODELS", r.status_code, [m["id"] for m in r.json()["data"]]); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#62 python #3** — prepended python block 2 (defines what this block calls); harness driver after the block: print("ANSWER", ask("Name one planet.")); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#63 python #4** — prepended python block 2 (defines what this block calls); harness driver after the block: print("STATUS", body["status"]); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#64 python #5** — prepended python block 2 (defines what this block calls); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#65 python #6** — prepended python block 2 (defines what this block calls); harness driver after the block: with client() as api: ⏎     body = post_with_retry(api, "/responses", {"model": MODEL, "input": "Name one river."}) ⏎ print("STATUS", body["status"]); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#66 python #7** — prepended python block 2 (defines what this block calls); harness driver after the block: with client() as api: ⏎     rid = summarise_in_background(api, "docs-run-20260913005259-python-page", "Summarise: the office is closed on Friday.") ⏎ print("ID", rid); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#69 javascript #3** — prepended javascript block 2 (defines what this block calls); harness driver after the block: console.log("ANSWER", await ask("Name one planet."));; https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#70 javascript #4** — prepended javascript block 2 (defines what this block calls); prepended javascript block 3 (defines what this block calls); harness driver after the block: let n = 0, chars = 0; ⏎ for await (const piece of streamText("Explain RAG in one sentence.")) { n += 1; chars += piece.length; } ⏎ console.log("DELTAS", n, chars);; https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#71 javascript #5** — prepended javascript block 2 (defines what this block calls); prepended javascript block 3 (defines what this block calls); harness driver after the block: const b = await postWithRetry("/responses", { model: MODEL, input: "Name one river." }); ⏎ console.log("STATUS", b.status);; https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#72 javascript #6** — prepended javascript block 2 (defines what this block calls); prepended javascript block 3 (defines what this block calls); harness setup before the block: const ticketId = crypto.randomUUID(); ⏎ const text = "Summarise: the office is closed on Friday.";; https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1
* **#73 curl #1** — harness driver after the block: test "$TECHSARA_BASE_URL" = "http://127.0.0.1:3001/v1" && test "${#TECHSARA_API_KEY}" -gt 40 && echo ENV_SET; https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#74 curl #2** — prepended curl block 1 (defines what this block calls); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#75 curl #3** — prepended curl block 1 (defines what this block calls); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#76 curl #4** — prepended curl block 1 (defines what this block calls); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#77 curl #5** — prepended curl block 1 (defines what this block calls); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#78 curl #6** — prepended curl block 1 (defines what this block calls); resp_4f2b8c1d9e0a7b6c5d4e3f20 → a completed background response created by this run; https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#79 curl #7** — prepended curl block 1 (defines what this block calls); resp_4f2b8c1d9e0a7b6c5d4e3f20 → a background response created seconds earlier (still running); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#80 curl #8** — prepended curl block 1 (defines what this block calls); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#81 curl #9** — prepended curl block 1 (defines what this block calls); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#82 curl #10** — prepended curl block 1 (defines what this block calls); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#83 curl #11** — prepended curl block 1 (defines what this block calls); https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
* **#84 status #1** — https://ai.techsarasolutions.com/v1 → http://127.0.0.1:3001/v1; placeholder key → "$TECHSARA_API_KEY" (the real key, from the environment)
