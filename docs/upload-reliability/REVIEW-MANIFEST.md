# Review manifest — first-party code read for the upload-reliability work (2026-09-10)

Nine agents (eight readers, one completeness critic); 375 file entries; 131 findings. Status is what the reader declared: `reviewed` = every line; `skimmed` = the ranges named; `excluded` = not read, with the reason. Directories inventoried but not read line by line: node_modules, .next, .venv, .git objects, model weights, /data (uploads, analyses), .runtime.

| path | status | slice | purpose / reason |
|---|---|---|---|
| `.env` | skimmed | fe-attach | Deployment value UPLOAD_MAX_MB=102400 — grep only |
| `.env` | skimmed | be-uploads | Deployment values — grep of cap keys only: UPLOAD_MAX_MB=102400 (line 348), WORKSPACE_TTL_HOURS=24 (183), WORKSPACE_QUOTA_GB=20 (184), VIDEO_ANALYSIS_ENABLED=true (372); no secrets |
| `.env` | skimmed | infra | Live settings (non-secret keys only): UPLOAD_MAX_MB=102400, WORKSPACE_QUOTA_GB=20, WORKSPACE_TTL_HOURS=24, GEN_WALL_CLOCK_S=4200, VIDEO_ANALYSIS_ENABLED=true, ASR_ENABLED=true, TEC |
| `.env.example` | skimmed | infra | Documented defaults: UPLOAD_MAX_MB=200, WORKSPACE_TTL_HOURS=24, WORKSPACE_QUOTA_GB=20, ASR_*, VIDEO_* (VIDEO_MAX_UPLOAD_MB=4096), OCR_REMOTE_BASE_URL — Read the requested sections: |
| `.github/workflows/pipeline.yml` | reviewed | infra | Single pipeline: policy/tests/schema/security/images -> ci-ok -> deploy (self-hosted) -> verify -> recovery |
| `.github/workflows/pipeline.yml` | skimmed | tests | ONE pipeline: policy → four test suites (launcher 3.11/3.12 unittest; orchestrator py3.11 + postgres@sha256:9a8afca… PG18.4, TEST_DATABASE_URL=…/techsara_orchestrator_test, `python |
| `.github/workflows/scripts/arm64_gate.py` | reviewed | infra | Base-image arm64 manifest check and x86 smell scan |
| `.github/workflows/scripts/ci_gate.py` | reviewed | infra | Positive assertion that every required job succeeded |
| `.github/workflows/scripts/ruff_gate.py` | reviewed | infra | ruff correctness-rule baseline gate |
| `.github/workflows/scripts/schema_parity.py` | reviewed | infra | Fresh-vs-upgrade schema parity proof |
| `.github/workflows/scripts/secret_gate.py` | reviewed | infra | gitleaks report cross-check and fingerprint baseline |
| `.github/workflows/scripts/workflow_policy.py` | reviewed | infra | Static workflow policy checks P1-P6 |
| `.runtime/logs/deploy-20260909-*.log` | skimmed | infra | Four production deploy logs from 2026-09-09 (20:47, 21:30, 21:58, 22:48 IST) — grep for targets, drain tables, ready-after lines, DEPLOYED; secrets not read |
| `compose.yaml` | reviewed | infra | Base compose: postgres, orchestrator (env, volumes, healthcheck, stop_grace_period 2m, init), frontend (no stop_grace_period), networks, named volumes |
| `compose/compose.cloudflare.yaml` | reviewed | infra | cloudflared tunnel services (frontend:3000 and grafana), no ports, dns override |
| `compose/compose.cluster-dgx-spark.yaml` | reviewed | infra | Two-node head overlay: host-networked vllm, engine-aware healthcheck, extra_hosts for orchestrator |
| `compose/compose.dgx-spark.yaml` | reviewed | infra | DGX overlay: orchestrator :cuda image, vllm main/router/embed/reranker/ocr services, vllm-watchdog |
| `compose/compose.monitoring.yaml` | reviewed | infra | Prometheus/Grafana/exporters profile; data volume mounted read-only into data-stores-exporter |
| `compose/compose.ocr.yaml` | reviewed | infra | Standalone OCR engine project (head or worker), host networking, explicit KV budget |
| `compose/compose.published-dgx-spark.yaml` | reviewed | infra | Opt-in port publishing for model APIs |
| `compose/compose.whisper.yaml` | reviewed | infra | Standalone whisper-large-v3 project, host networking, own cache volume |
| `compose/whisper/server.py` | reviewed | be-video | whisper-large-v3 HTTP engine: decode via ffmpeg stdin, no-speech gate, sequential long-form, verbose_json segments |
| `config/hardware-profiles.yaml` | reviewed | infra | Hardware profile table (dgx-spark selects compose.dgx-spark.yaml, concurrency 4) |
| `config/model-manifest.yaml` | skimmed | infra | Pinned model manifest — Lines 1-120 read in full; remainder covered by grep of ids/backends/startup_arguments only (no upload/video relevance) |
| `docker-compose.yml` | skimmed | be-uploads | Volume layout: one `data` volume mounted at /data (workspaces and video share it) — grep only |
| `docs/00-INVENTORY.md` | skimmed | rest | Docs index: 01-codebase (CRITICAL-PATHS, data-model, frontend, frontend-api-contracts, infra, orchestrator-*, security-model, sync-worker, test-map), 02-diagrams, 03-report (FINDIN |
| `docs/AUTH.md` | skimmed | rest | Auth + tunnel operations; documents the 100 MB Cloudflare body cap and that chunked upload is the fix — lines 290-312 read |
| `docs/deploy*` | excluded | infra | n/a — No docs/deploy* or docs/ops* files exist; docs/AUTH.md:302-311 and docs/video-understanding/README.md were consulted instead |
| `docs/video-understanding/DECISIONS.md` | skimmed | rest | Design decisions: detached hash-keyed job started at upload, step-event progress with fixed ids 1-9, artifacts via meta.report_files copied per user, VIDEO_ANALYSIS_ENABLED folded  |
| `docs/video-understanding/README.md` | skimmed | rest | Video feature narrative, measured stage timings (transcription 453 s, OCR 282 s), operating notes — grep of headings and chunk/reload lines; line 239 quoted |
| `frontend/Dockerfile` | reviewed | infra | Next.js standalone image (node:20-alpine, node server.js, no server timeout overrides) |
| `frontend/app/api/chat/active/route.ts` | reviewed | fe-chat | GET /api/chat/active proxy; returns 200 {active: []} on transport failure |
| `frontend/app/api/chat/active/route.ts` | reviewed | infra | Proxy for GET /chat/active; converts any transport failure into {active: []} with HTTP 200 |
| `frontend/app/api/chat/attach/[id]/route.ts` | reviewed | fe-chat | GET /api/chat/attach/[id] proxy; SAFE_ID check; 404/401 pass through, everything else becomes 502 |
| `frontend/app/api/chat/attach/[id]/route.ts` | skimmed | be-chat | Next.js proxy for GET /chat/attach — grep only: `signal: req.signal` at line 46. |
| `frontend/app/api/chat/compact/route.ts` | reviewed | fe-chat | POST /api/chat/compact proxy |
| `frontend/app/api/chat/route.ts` | reviewed | fe-chat | POST /api/chat proxy to orchestrator /chat; pipes SSE; maps failures to {code} + status |
| `frontend/app/api/chat/route.ts` | reviewed | infra | SSE proxy for POST /chat |
| `frontend/app/api/chat/route.ts` | skimmed | be-chat | Next.js proxy for POST /chat — grep only: `signal: req.signal` at line 302 and a ReadableStream cancel() at 173 that sets a flag. |
| `frontend/app/api/chat/salesforce/[id]/route.ts` | reviewed | fe-chat | GET /api/chat/salesforce/[id] proxy |
| `frontend/app/api/chat/salesforce/cancel/route.ts` | reviewed | fe-chat | POST /api/chat/salesforce/cancel proxy |
| `frontend/app/api/chat/stop/route.ts` | reviewed | fe-chat | POST /api/chat/stop proxy (cookie forwarded, body verbatim) |
| `frontend/app/api/history/[...path]/route.ts` | reviewed | fe-chat | /api/history/* proxy via proxyToOrchestrator with path allowlist (classifyHistoryPath) |
| `frontend/app/api/upload/chunked/[...path]/route.ts` | reviewed | infra | Chunked-rail proxy (init/part/complete); swallows failure cause |
| `frontend/app/api/upload/chunked/[...path]/route.ts` | reviewed | be-uploads | Next.js streaming proxy for the chunked rail; any fetch failure becomes 502 'upload service unreachable' |
| `frontend/app/api/upload/chunked/[...path]/route.ts` | reviewed | fe-attach | Streaming proxy for chunked init/part/complete |
| `frontend/app/api/upload/route.ts` | reviewed | be-uploads | Next.js proxy for the single-shot rail |
| `frontend/app/api/upload/route.ts` | reviewed | infra | Single-shot upload proxy (streams body with duplex half; swallows failure cause) |
| `frontend/app/api/upload/route.ts` | reviewed | fe-attach | Streaming proxy POST /api/upload -> orchestrator /uploads |
| `frontend/app/api/uploads/[conversation]/[upload]/file/route.ts` | reviewed | fe-attach | Proxy: fetch an upload's bytes by id (410 passthrough) |
| `frontend/app/api/uploads/[conversation]/document/route.ts` | reviewed | fe-attach | Proxy: fetch a stored document's bytes by name |
| `frontend/app/api/uploads/[conversation]/route.ts` | reviewed | infra | List-uploads proxy |
| `frontend/app/api/uploads/[conversation]/route.ts` | reviewed | fe-attach | Proxy: list a conversation's stored uploads |
| `frontend/components/ActivityPanel.tsx` | reviewed | fe-chat | Sources drawer incl. the video section (chapters, evidence, status) |
| `frontend/components/AgentTimeline.tsx` | reviewed | fe-chat | Step timeline with client-measured elapsed clock |
| `frontend/components/ChatApp.tsx` | reviewed | fe-chat | Chat shell: send path, upload-then-stream path, regenerate/edit/retry, mount restore (?c=), /chat/active poll, thread render with the unsent notice |
| `frontend/components/ChatApp.tsx` | skimmed | be-uploads | Send path: send_state marker, Promise.all uploads, per-file id linking, startStream — Read 1180-1330 only, for the handoff; not my slice |
| `frontend/components/ChatApp.tsx` | skimmed | tests | Verification only: unsentTurnState (149-160), reload reconcile via fetchServerActive/attachStream (440-530, 600-625, 1855-1880), send path with send_state + Promise.all upload loop |
| `frontend/components/ChatApp.tsx` | skimmed | fe-attach | send(): builds the turn, awaits early uploads (Promise.all), links ids, writes/clears send_state, starts the stream; unsentTurnState; notice condition — Not in my slice but the con |
| `frontend/components/ChatApp.tsx` | skimmed | be-chat | send_state writes and the never-sent notice — Read 1180-1200, 1295-1325, 2495-2535 for handoff evidence only. |
| `frontend/components/Composer.tsx` | reviewed | fe-attach | Draft chips, file classification, early upload (withEarlyUpload), submit -> onSend |
| `frontend/components/Composer.tsx` | skimmed | fe-chat | Attachment state, withEarlyUpload (uploadPromise bound to uploadConversationId, errors swallowed to null), submit gating — Outside the slice; read lines 150-180 and 400-600 plus gr |
| `frontend/components/Composer.tsx` | skimmed | tests | Verification only: withEarlyUpload (490-530) swallows errors to null — Not in slice; range read for verification |
| `frontend/components/Composer.tsx` | skimmed | be-uploads | withEarlyUpload starts the upload at attach time — grep only, lines 499-514: uploadPromise = uploadDocumentFile(...).catch(() => null) |
| `frontend/components/MessageRow.tsx` | reviewed | fe-chat | Message rendering; user-turn branch with attachment chips, uploadStatus indicator and the unsent notice + Send now button |
| `frontend/components/MessageRow.tsx` | skimmed | fe-attach | Upload indicator and the 'never sent' notice text — Read 845-905 and 1243-1250 |
| `frontend/components/MessageRow.tsx` | skimmed | tests | Verification only: unsent notice copy at 873-891 — grep only |
| `frontend/lib/attachments.ts` | reviewed | fe-attach | Resend reconstruction (attachmentsForResend/resendOptionsFor), preview ladder, drag/drop intent |
| `frontend/lib/attachments.ts` | skimmed | fe-chat | rememberAttachments, attachmentsForResend/resendOptionsFor (videos by id only; no id = missing), uploadRefFor — Outside the slice; read lines 36-60, 100-302, 640-700 to trace Send  |
| `frontend/lib/branching.ts` | skimmed | fe-chat | metaWithBranch spreads the original meta (send_state included) onto an edited version — Read lines 58-64 only |
| `frontend/lib/format.ts` | reviewed | fe-attach | Byte/date formatting and file-kind badges |
| `frontend/lib/history.ts` | reviewed | fe-chat | History store: in-memory cache + IndexedDB write-behind + server sync (pushAll/replaceMessages, appendMessage, loadConversation force semantics, 409 adopt-server-truth) |
| `frontend/lib/history.ts` | skimmed | be-uploads | Client history cache + sync (appendMessage vs whole-thread replaceMessages) — Read 880-1000 (pushAll/syncConversation) and 1072-1092 (enqueue) for the PUT semantics handoff |
| `frontend/lib/history.ts` | skimmed | fe-attach | saveMessages -> enqueue -> syncConversation (append vs replaceMessages, 409 adopt); loadConversation sets pdfName from attachments[0] — Read 300-380, 640-720, 880-1060, 1060-1135 t |
| `frontend/lib/historyApi.ts` | reviewed | fe-chat | Typed fetch client for /api/history/*; error classification (409 conflict = shrink refused) |
| `frontend/lib/historyApi.ts` | skimmed | be-uploads | HTTP client for /history — Read 225-300 |
| `frontend/lib/proxy.ts` | excluded | fe-chat | proxyToOrchestrator used by the history route — Outside the slice; not needed for the trace |
| `frontend/lib/sse.ts` | reviewed | fe-chat | SSE parser and typed event mapping (token/reasoning/status/step/research/meta/done/error), mergeStep, foldStreamState |
| `frontend/lib/streams.ts` | reviewed | fe-chat | Per-conversation live stream registry: startStream (POST /api/chat), attachStream (GET /api/chat/attach), consume, finalize/markUnreachable/markInterrupted persistence, fetchServer |
| `frontend/lib/streams.ts` | skimmed | be-chat | Client stream consumer: fetchServerActive, end-of-stream-without-terminal handling — Read 150-215 and 440-530 for handoff evidence only (frontend slice owns it). |
| `frontend/lib/streams.ts` | skimmed | tests | Verification only: fetchServerActive (155-185) returns [] on !ok; attachStream (700-775) — Not in slice; ranges read for verification |
| `frontend/lib/streams.ts` | skimmed | fe-attach | startStream body (video_uploads), fetchServerActive, markUnreachable/markInterrupted persistence — Read 161-180, 236-340, 612-760 |
| `frontend/lib/types.ts` | skimmed | fe-chat | MessageAttachment and Meta.send_state typing — Read lines 155-175 and 276-300 only |
| `frontend/lib/types.ts` | skimmed | fe-attach | Meta.attachments, Meta.send_state, VideoActivity, ChatMessage.pdfName — Read lines 1-60, 150-300, 520-589 (attachment/upload/meta sections) plus grep; unrelated sections not read |
| `frontend/lib/uploadDocument.ts` | reviewed | be-uploads | Browser upload client: single vs chunked (>90 MB), sequential 64 MiB parts, complete |
| `frontend/lib/uploadDocument.ts` | reviewed | fe-chat | Client upload: single POST /api/upload under 90 MB, chunked init/part/complete above; no retry, no part-count/size sent to complete |
| `frontend/lib/uploadDocument.ts` | reviewed | fe-attach | Single-shot and chunked upload client (purpose=document/video) |
| `frontend/lib/uploadDocument.ts` | skimmed | infra | Chunked client: CHUNK_THRESHOLD_BYTES=90 MiB, CHUNK_PART_BYTES=64 MiB, sequential parts then complete — grep of constants and control flow only (lines 11-17, 40-51, 82-113) |
| `frontend/lib/uploadDocument.ts` | skimmed | tests | Verification only: chunked client with no retry, complete result treated as the reference — Not in slice; read whole (116 lines) to verify chunk/complete behaviour the tests assume |
| `frontend/next.config.mjs` | reviewed | infra | Next config: standalone output, security headers; no body-size/timeout settings |
| `frontend/next.config.mjs` | reviewed | fe-attach | Security headers only; no body-size or proxy settings |
| `frontend/node_modules` | excluded | infra | Next.js standalone server internals (SIGTERM handling, http requestTimeout) — Forbidden by the review rules; Next runtime behavior is therefore reported as plausible, not confirmed |
| `frontend/tests/(other 75 test files: accent-palette, accept-invite-form, access-removed, account-*, admin-*, agent-timeline, analytics-console, auth-cache-scoping, auth-me, auth-middleware, auth-proxy-routes, chartOption, chat-error-page, citations, clarification*, compact-verification, composer-menu, composer-prefill, context-meter*, conversation-menu, copy-button-timer, csv-download-*, data-table-*, debug-error-route, engine-badge-removed, error-classification, errors, export-markdown, feature-access, feedback, images, loader, login-*, markdown-segments, mermaid, mode-chip-responsive, new-chat-defaults, pasted, paste-inline, phases, prefs, reasoning-star, reports-proxy, research, salesforce-mode, security-settings, selected-context*, selection-ask, shared-conversation-xss, share-*, shortcuts, sidebar-a11y, slash-command*, summary-panel, title-generation, toast, voice-input, voice-waveform, websearch)` | excluded | tests | Frontend tests outside the upload/document/dataset/stream/history/reload/attach/regenerate name filter — Names do not match the slice filter and grep for reload/regenerate/chat/act |
| `frontend/tests/_wireHarness.tsx` | reviewed | tests | Shared real-wire harness: real ChatApp + real startStream, fetch stubbed (/api/chat → 1 token + done; /api/upload → immediate 32-hex id; everything else → {ok:true, json:{}}); mock |
| `frontend/tests/activity-documents.test.tsx` | skimmed | tests | Activity panel splits multi-document meta — Header + case names only |
| `frontend/tests/attachment-drop.test.tsx` | skimmed | tests | Drag/drop DataTransfer shapes; NEW10A-22 accepts a dropped .mp4 onto the document rail — Read lines 1-40 and 600-780 (video fallback case at 724-744); the remaining drag-shape case |
| `frontend/tests/attachment-identity.test.tsx` | reviewed | tests | Composer chip clientId identity independent of position; remove-by-index bug |
| `frontend/tests/attachment-only-edit.test.tsx` | skimmed | tests | One action row per attachment-only turn; edit keeps attachment metadata — Read lines 1-120 plus the describe/it list; no upload/stream/reload behaviour |
| `frontend/tests/attachment-open.test.tsx` | reviewed | tests | Attachment card preview dialog never downloads; object URL lifecycle; bytes-gone-after-reload dialog copy |
| `frontend/tests/attachment-preview-4c.test.tsx` | reviewed | tests | XLSX/DOCX previews from server profile/document text; abort on close; no refetch on rerender |
| `frontend/tests/attachment-reuse.test.tsx` | reviewed | tests | Re-attach by drag; after simulated reload bytes come from server; expired (410) vs gone (404) messages |
| `frontend/tests/attachments.test.ts` | reviewed | tests | attachmentsForResend: images rebuilt from previews after reload; PDF reports missing |
| `frontend/tests/auth-streams-401.test.ts` | reviewed | tests | 401 on send persists nothing and redirects; fetchServerActive: 401 → redirect, 503 → quiet [] (pins the failure/nothing conflation) |
| `frontend/tests/branching.test.ts` | skimmed | tests | Branch tree reconstruction from server rows (ids renumbered srv-<conv>-<i> on reload) — Lines 295-330 only (reload reconstruction) |
| `frontend/tests/chat-contract.test.ts` | skimmed | tests | toOrchestratorChatRequest field mapping — Header + case names only |
| `frontend/tests/chat-proxy-errors.test.ts` | skimmed | tests | Chat proxy status preservation and server-side logging — Header + case names only |
| `frontend/tests/continuation-notice.test.ts` | reviewed | tests | continuationNotice copy for truncated answers |
| `frontend/tests/conversation-switch-loading.test.tsx` | reviewed | tests | M-10 conversation switching: previous chat never shown under new id; deferLoad/land barrier pattern; latest selection wins |
| `frontend/tests/dataset-chat-request.test.tsx` | reviewed | tests | NEW-14 DATASET_ONLY_PROMPT translation; end-to-end real body capture; failed upload starts no generation (50 ms sleep) |
| `frontend/tests/dataset-upload-feedback.test.tsx` | reviewed | tests | H-01/H-02: dataset turn on screen while /api/upload pending (deferredUpload barrier); failed upload starts no generation; persisted before upload resolves (startStream mocked) |
| `frontend/tests/feedback-server.test.ts` | skimmed | tests | Server-stored thumbs survive reload/regenerate — Header + case names only |
| `frontend/tests/history-cache-engine.test.ts` | skimmed | tests | IndexedDB write-behind cache engine for the server store — Header + case names only; no reload/send_state coverage |
| `frontend/tests/history-proxy-paths.test.ts` | reviewed | tests | /api/history/* allowlist classifier |
| `frontend/tests/history-server.test.ts` | reviewed | tests | Server history store write-through, offline push, lazy load, no-shrink regression, truncate as the only sanctioned shrink |
| `frontend/tests/history.test.ts` | reviewed | tests | Local history store CRUD, pin/archive, quota eviction |
| `frontend/tests/idbCache.test.ts` | skimmed | tests | IndexedDB persister round-trips — Header + case names only |
| `frontend/tests/live-status.test.tsx` | reviewed | tests | LiveStatus ticking clock and long-wait note |
| `frontend/tests/multi-document-resend.test.tsx` | reviewed | tests | Edit/regenerate/unchanged-edit re-send all four documents by id through the real wire; resendOptionsFor identity rules |
| `frontend/tests/multi-document.test.tsx` | reviewed | tests | Documents stack to five; several upload with purpose=document before the chat request; one small doc rides inline plus quiet durability upload (startStream mocked) |
| `frontend/tests/sse-large-event.test.ts` | reviewed | tests | Linear parsing of multi-MB single SSE data line |
| `frontend/tests/sse.test.ts` | reviewed | tests | SSEParser framing, toChatStreamEvent (token/meta/done/error/reasoning/step validation), readChatStream tolerance, mergeStep/foldStreamState |
| `frontend/tests/stream-error-classification.test.ts` | reviewed | tests | Failed send records real status/code; safe sentence persisted |
| `frontend/tests/streaming-frame-coalescing.test.ts` | skimmed | tests | rAF coalescing with a manual frame queue (no sleeps); stale frame from a replaced stream says nothing — Header, case names and lines 280-310 (M-10 reload/re-send case) |
| `frontend/tests/streaming-render-cost.test.tsx` | skimmed | tests | Streaming render cost; stubs /api/chat/active → {active:[]} — grep hit at line 189 only |
| `frontend/tests/streaming-scroll.test.tsx` | skimmed | tests | Streaming scroll behaviour; stubs /api/chat/active → {active:[]} — Lines 140-175 (network stub) only |
| `frontend/tests/streams.test.ts` | reviewed | tests | attachBaseTurns (drop trailing assistant before re-join); withLiveProgressRetired clears live progress on terminal patches |
| `frontend/tests/truncate-failure.test.ts` | skimmed | tests | truncateFailure classification: server-sent 409/404 vs request never landed — Header + case names only |
| `frontend/tests/unchanged-edit.test.tsx` | reviewed | tests | Unchanged edit = regenerate; attachment-only turn cannot resend empty; a resend that cannot be rebuilt errors once |
| `frontend/tests/unsent-turn.test.tsx` | reviewed | fe-attach | Pins the unsent notice; builds turns WITHOUT pdfName (which hides finding F1) |
| `frontend/tests/unsent-turn.test.tsx` | reviewed | tests | unsentTurnState() + MessageRow notice for meta.send_state='uploading' (unit only, no ChatApp, no reload) |
| `frontend/tests/upload-document.test.ts` | reviewed | fe-attach | Pins chunk sizes and error surfacing |
| `frontend/tests/upload-document.test.ts` | reviewed | tests | uploadDocumentFile: single-shot vs chunked init/part/complete sequencing, part failure surfaces detail, chat translator pdf_uploads |
| `frontend/tests/upload-id-persistence.test.ts` | reviewed | tests | Server history store re-pushes meta changes (upload_id) on already-pushed messages; upload_id survives reload from server |
| `frontend/tests/upload-retrieval.test.ts` | reviewed | tests | /api/uploads/[conv]/[upload]/file proxy validation + status passthrough; resolveAttachment ladder; dataset regenerate needs no re-attach |
| `frontend/tests/user-message-actions.test.tsx` | skimmed | tests | Edit/Copy on user turns; row only reports, host truncates/regenerates — Header + case names only |
| `frontend/tests/version-navigator.test.tsx` | skimmed | tests | Version navigator is inert — Header + case names only |
| `frontend/tests/video-attachment.test.tsx` | reviewed | tests | Video chip classification, purpose=video upload on send, video_uploads in chat body, feature gate toast |
| `frontend/tests/video-attachment.test.tsx` | skimmed | fe-attach | Pins purpose=video and video_uploads on send — Read 60-135 |
| `frontend/vitest.config.ts` | reviewed | tests | vitest: node environment default, jsdom via per-file docblock, include tests/**/*.test.ts(x) |
| `launcher/techsara_cli/__init__.py` | skimmed | rest | Package exports — lines 1-6 only |
| `launcher/techsara_cli/__main__.py` | reviewed | rest | python -m entry |
| `launcher/techsara_cli/bridge.py` | skimmed | rest | Authenticated bridge for host-native model servers — lines 1-6 only |
| `launcher/techsara_cli/cli.py` | skimmed | infra | Staged start: orchestrator recreated and health-waited BEFORE the frontend — Read 1180-1200 plus grep of up_service calls |
| `launcher/techsara_cli/cli.py` | skimmed | rest | The launcher command surface — lines 1-6 only |
| `launcher/techsara_cli/cluster.py` | skimmed | rest | Two-node vLLM cluster settings/detection — lines 1-6 only |
| `launcher/techsara_cli/compose.py` | skimmed | rest | Safe compose command construction and readiness checks — lines 1-6 only |
| `launcher/techsara_cli/compose.py` | skimmed | infra | compose command chain, up_service (up -d --no-deps, no --wait), wait_service — Read 86-262 |
| `launcher/techsara_cli/environment.py` | skimmed | rest | Runtime layout, secrets, generated.env (model endpoints, context, KV, ports, search, OCR/rerank capability keys); does NOT emit UPLOAD_MAX_MB, VIDEO_*, SSE_HEARTBEAT, LLM_REQUEST_T |
| `launcher/techsara_cli/errors.py` | skimmed | rest | User-facing launcher errors — lines 1-6 only |
| `launcher/techsara_cli/hardware.py` | skimmed | rest | Host/accelerator detection — lines 1-6 only |
| `launcher/techsara_cli/model_manager.py` | skimmed | rest | Pinned, resumable model installation — lines 1-6 only |
| `launcher/techsara_cli/modelshape.py` | skimmed | rest | Model geometry from HF config.json — lines 1-6 only |
| `launcher/techsara_cli/profiles.py` | skimmed | rest | Hardware profiles and model/memory selection — lines 1-6 only |
| `launcher/techsara_cli/runtime.py` | skimmed | rest | Native runtime process lifecycle and API probes — lines 1-6 only |
| `launcher/techsara_cli/utils.py` | skimmed | rest | Stdlib security-sensitive helpers — lines 1-6 only |
| `monitoring/prometheus/prometheus.yml` | reviewed | infra | Scrape topology: orchestrator /metrics, blackbox probes of /health and /, vllm, node, gpu, postgres, data-stores |
| `monitoring/prometheus/rules/alerts.yml` | reviewed | infra | Alert rules (availability, gpu, host, vllm, containers, databases) — nothing on uploads/video/asr |
| `monitoring/prometheus/rules/recording.yml` | reviewed | infra | Recording rules for dashboards |
| `orchestrator/Dockerfile` | reviewed | infra | Superseded single-target image; CMD uvicorn with no graceful-shutdown timeout |
| `orchestrator/Dockerfile` | skimmed | be-video | Process model: single uvicorn process, no --workers — Only the CMD line (56) was read |
| `orchestrator/Dockerfile.cpu` | reviewed | infra | CPU orchestrator image; ffmpeg present; CMD uvicorn app.main:app (single worker, defaults) |
| `orchestrator/Dockerfile.cuda` | reviewed | infra | Production orchestrator image (NGC vLLM base); same uvicorn CMD |
| `orchestrator/app/__init__.py` | reviewed | rest | Package docstring |
| `orchestrator/app/asr.py` | reviewed | be-video | ASR client: VLLMAudioProvider, RoutedProvider (least-active, 20 s stand-down), POOL/BATCH_POOL admission |
| `orchestrator/app/audio_api.py` | reviewed | be-video | POST /audio/transcribe dictation route and /audio/health |
| `orchestrator/app/auth.py` | reviewed | rest | Compatibility surface (UserRow, current_user, require_user) over authn; resolves ts_session cookie per request |
| `orchestrator/app/auth.py` | skimmed | be-chat | current_user / require_user (used by /chat/active, /attach, /stop) — Read 36-47: current_user delegates to resolve_principal_sync, so it yields the same user_id as /chat's principa |
| `orchestrator/app/authn/__init__.py` | reviewed | rest | Package docstring for enterprise identity |
| `orchestrator/app/authn/admin_api.py` | reviewed | rest | /admin/api: members, roles, status, sessions, invitations, feature access, usage analytics, audited content viewer incl. member upload/report downloads |
| `orchestrator/app/authn/analytics_api.py` | skimmed | rest | SUPER_ADMIN analytics console over usage_events + Prometheus; read-only aggregates, no upload surface — lines 1-120 plus route list (10 GET routes, all behind Cap.ANALYTICS_READ);  |
| `orchestrator/app/authn/api.py` | reviewed | rest | /auth login/logout/me/password/sessions/preferences/invitations; _features_for_ui folds VIDEO_ANALYSIS_ENABLED; /auth/me 401 clears dead cookies |
| `orchestrator/app/authn/bootstrap.py` | reviewed | rest | Identity baseline on start + first SUPER_ADMIN CLI; claims unbound report files |
| `orchestrator/app/authn/features.py` | reviewed | rest | Per-person tool flags (attachments, voice, video_analysis requires attachments, web, research, SF); enforce_chat covers mode/web/research/sf only |
| `orchestrator/app/authn/passwords.py` | reviewed | rest | Argon2id hashing with timing equalisation |
| `orchestrator/app/authn/principal.py` | reviewed | rest | Principal dataclass; resolve_principal_sync (cookie → session → user → membership → caps+features), cached on request.state; require_capability answers 404; audit() |
| `orchestrator/app/authn/principal.py` | skimmed | be-chat | Principal resolution (used by /chat) — Read 40-70 and 100-135 only. |
| `orchestrator/app/authn/rbac.py` | reviewed | rest | Role/Cap tables; SUPER_ADMIN = all caps; ADMIN lacks ANALYTICS_READ/SHARES_MANAGE |
| `orchestrator/app/authn/sessions.py` | reviewed | rest | Opaque sessions: ts_session=<sid>.<secret>, sha256 stored, rolling renewal (>300 s), absolute ceiling, explain() for /auth/me 401 codes, cookie flags |
| `orchestrator/app/authn/shares_api.py` | reviewed | rest | Super-admin share governance (list/revoke/policy); public_id only, never tokens |
| `orchestrator/app/authn/store.py` | reviewed | rest | Identity SQL: users, memberships, feature layers, sessions (create/roll/revoke), invitations, audit, login throttle, report_files bind/owner, admin content queries (uploads via con |
| `orchestrator/app/compaction.py` | reviewed | rest | Per-conversation context budget + rolling-summary compaction (sync and background), per-conversation asyncio locks |
| `orchestrator/app/config.py` | excluded | rest | Settings — owned by another reader; consulted 239-352 (video), 512, 641, 1105-1135 (sessions) for defaults cited in findings |
| `orchestrator/app/config.py` | skimmed | be-uploads | Settings defaults (upload_max_mb=200, video_max_upload_mb=4096, workspace_ttl_hours=24, workspace_quota_gb=20, video_data_dir=/data/video, workspace_dir=/data/workspaces) — grep fo |
| `orchestrator/app/config.py` | skimmed | be-video | Settings; the OCR (125-135), ASR (136-230) and video (231-352) sections — Read lines 125-360 in full plus greps for upload_max_mb (641) and llm_request_timeout (1285); the rest of  |
| `orchestrator/app/config.py` | skimmed | be-chat | Video settings defaults — grep only: VIDEO_ANALYSIS_ENABLED default False, VIDEO_MAX_CONCURRENT_JOBS=1, VIDEO_PACE_MAX_WAIT_S=20, VIDEO_MAINTENANCE_INTERVAL_S=1800. |
| `orchestrator/app/config.py` | skimmed | fe-attach | upload_max_mb (200 default) and video_max_upload_mb (4096) — Read 248-256 and 636-646 |
| `orchestrator/app/context.py` | skimmed | rest | Per-request token budgeting against the serving model's window (/tokenize) — lines 1-12 only; no upload/auth/file surface |
| `orchestrator/app/continuation.py` | skimmed | rest | Stitches multiple bounded model calls into one long answer — lines 1-12 only |
| `orchestrator/app/core/repo.py` | skimmed | be-uploads | Holds the 24-hour workspace sweep enforce_quota_and_ttl (plus GitHub clone code) — Read lines 1-140 (the sweep/quota section and module header); clone/indexing code below not read |
| `orchestrator/app/core/repo.py` | skimmed | infra | enforce_quota_and_ttl: deletes whole top-level workspace dirs by mtime/quota — Read 83-125 only (the sweep and size helper) |
| `orchestrator/app/core/report_render.py` | excluded | rest | Shared pandoc/weasyprint renderer used by dataset_report — core reader; lines 115-160 consulted for subprocess timeout only |
| `orchestrator/app/core/upload_paths.py` | reviewed | be-uploads | Safe resolution of an upload's on-disk path for download |
| `orchestrator/app/db.py` | excluded | rest | App-state PostgreSQL store — owned by another reader; grepped for has_attachment (none) and video_attachments schema (1503-1516) |
| `orchestrator/app/db.py` | skimmed | be-uploads | PostgreSQL schema + accessors — Read: 81-200 (users/conversations/messages/uploads DDL), 213-226 (documents DDL), 950-1010 (usage_events DDL), 1450-1520 (V28 video DDL), 1614-1700  |
| `orchestrator/app/db.py` | skimmed | be-chat | add_message generation_id idempotency, replace_messages (PUT semantics), video row lookups, requeue_interrupted_video_analyses — Read 3651-3700, 3737-3830, 5055-5160 only. |
| `orchestrator/app/db.py` | skimmed | be-video | video_analyses/video_attachments schema (V28) and accessors; V29 lease functions added by commit 845408d during this review — Read 1450-1520 (V28 DDL), 1675-1710 (run_in_thread), 4 |
| `orchestrator/app/embedding_index.py` | skimmed | rest | Read-only compatibility checks for the Salesforce LanceDB index sidecar — lines 1-8 only |
| `orchestrator/app/engines/__init__.py` | reviewed | be-chat | Engine helpers (recent_turns / conversation_turns) and shared prompt fragments; no route registry lives here — routing is the if/elif chain in main.py's worker |
| `orchestrator/app/engines/__init__.py` | reviewed | rest | recent_turns/conversation_turns helpers, NO_DATA_MESSAGE, DIAGRAM/CODE instructions |
| `orchestrator/app/engines/agent.py` | skimmed | rest | PLAN→EXECUTE→SYNTHESIZE agent; merge_step_meta unions report_files by filename — lines 1-4 and 600-660 (report_files merge) only |
| `orchestrator/app/engines/chat.py` | skimmed | rest | Plain streamed completions (assistant mode / salesforce chat class) — lines 1-4; grep found no attachment handling |
| `orchestrator/app/engines/crawl.py` | skimmed | rest | Site crawler into the web store — lines 1-4 only |
| `orchestrator/app/engines/dataset.py` | reviewed | rest | Dataset engine: answers from stored profiles of ALL uploads in the conversation (db.get_uploads(conversation_id)); H-03 branch to dataset_report |
| `orchestrator/app/engines/dataset_report.py` | reviewed | rest | One-page PDF report from upload profiles via core.report_render (pandoc/weasyprint) → meta.report_files |
| `orchestrator/app/engines/deep_research.py` | skimmed | rest | Iterative source-grounded research loop — lines 1-4 only |
| `orchestrator/app/engines/document.py` | reviewed | rest | Document engine: PDF/DOCX/text extraction, OCR on thin pages, upload-time cache extracted/document.json (atomic os.replace), multi-doc answer, meta route 'vision' |
| `orchestrator/app/engines/live_sf.py` | skimmed | rest | Live Salesforce API engine merged with warehouse — lines 1-4 only |
| `orchestrator/app/engines/ocr.py` | reviewed | rest | Unlimited-OCR sidecar client; per-image 120 s timeout, optional deadline, '' on failure |
| `orchestrator/app/engines/ocr.py` | skimmed | be-video | ocr_images: per-image OCR with swallow-all error handling and a whole-batch deadline — Read 135-195 (ocr_images) to settle the failure semantics screen.py depends on |
| `orchestrator/app/engines/orchestrate.py` | skimmed | rest | Auto-orchestration classifier (agent vs direct) — lines 1-4 only |
| `orchestrator/app/engines/rag.py` | skimmed | rest | Salesforce LanceDB RAG engine — lines 1-4 only |
| `orchestrator/app/engines/repo.py` | skimmed | rest | GitHub repo clone/index engine — lines 1-4 only |
| `orchestrator/app/engines/report.py` | reviewed | rest | Salesforce report engine: plan → sql/rag sections → pandoc docx+pdf into REPORTS_DIR → meta.report_files; own _run_pandoc copy |
| `orchestrator/app/engines/router.py` | skimmed | rest | Salesforce-mode router classifier — lines 1-4 only |
| `orchestrator/app/engines/search.py` | skimmed | rest | Web search engine with in-process per-user rate limit (rate_ok, settings.search_rate_per_min) — lines 1-4 and 274-302 (limiter) only |
| `orchestrator/app/engines/sf_intel.py` | skimmed | rest | Salesforce Intelligence Mode engine — lines 1-4 only |
| `orchestrator/app/engines/sql.py` | skimmed | rest | DuckDB SQL engine with sql_guard — lines 1-4 only |
| `orchestrator/app/engines/url.py` | skimmed | rest | URL/website analysis engine — lines 1-4 only |
| `orchestrator/app/engines/video.py` | excluded | rest | Video answer engine; publish_artifacts(user_id) copies artifacts as <slug>-<hash8>-u<user> and emits meta.report_files — owned by the video reader; grepped lines 31-32, 442-449, 51 |
| `orchestrator/app/engines/video.py` | reviewed | be-chat | Video engine: waits on the detached analysis (forwards pipeline progress as step events), renders the attach-turn overview or a retrieval-grounded Q&A answer, publishes artifacts |
| `orchestrator/app/engines/video.py` | skimmed | be-video | Chat-side consumer: _wait_for_analysis subscribes/forwards steps; run_video_engine renders or answers — Read 236-320 and 500-600 plus is_placeholder (118-120) and _STEP_IDS (64) —  |
| `orchestrator/app/engines/video.py` | skimmed | be-uploads | Chat-side wait on the analysis; forwards pipeline events as SSE step events — Read 251-318 (_wait_for_analysis) only, for the handoff |
| `orchestrator/app/engines/vision.py` | reviewed | rest | Image engine on the main model; optional OCR pre-pass at Think/Max; meta route 'vision' |
| `orchestrator/app/facts.py` | skimmed | rest | Background fact extraction into user_facts (memory) — lines 1-12 only |
| `orchestrator/app/freshness.py` | skimmed | rest | STATIC/RECENT/REALTIME freshness classifier (emits metrics) — lines 1-12 only |
| `orchestrator/app/graph.py` | reviewed | rest | LangGraph router→sql/rag/vision/report/chat wiring (Salesforce mode); no video/dataset/document nodes here |
| `orchestrator/app/health.py` | skimmed | rest | /health dependency probes (vLLM services, DuckDB, web corpus); no video/upload probe — lines 1-12 + grep for video/upload/report |
| `orchestrator/app/history.py` | reviewed | be-uploads | Server-side conversation history routes: list/create/get/update, append message, whole-thread PUT replace, truncate, feedback, title, search |
| `orchestrator/app/history.py` | skimmed | be-chat | POST/PUT message routes the frontend uses to persist turns — Read 160-234 only (append and replace routes) to establish PUT semantics for the handoff. |
| `orchestrator/app/identity.py` | reviewed | rest | ContextVar carrying display name/email/workspace into prompts |
| `orchestrator/app/living_knowledge.py` | skimmed | rest | Pre-answer stage: freshness + local corpus + bounded auto-search — lines 1-12 only |
| `orchestrator/app/llm.py` | skimmed | rest | AsyncOpenAI clients per (loop, endpoint, read timeout); stream wrapped in GEN_WALL_CLOCK_S; embeddings with own timeouts — lines 1-12, 240-275, 385, 1046-1062, 1112-1145 (timeout h |
| `orchestrator/app/llm.py` | skimmed | be-video | json_completion signature and the LLM read timeout default — Read 908-950 and line 252 only |
| `orchestrator/app/main.py` | excluded | rest | FastAPI entrypoint; CSRF Origin middleware; feature downgrade for attachments/video at /chat; binds meta.report_files to the viewer — owned by another reader; consulted lines 148-2 |
| `orchestrator/app/main.py` | reviewed | be-chat | FastAPI entrypoint: LiveGeneration registry, POST /chat worker + engine dispatch (incl. video branch), _finalize_generation persistence, /chat/attach, /chat/active, /chat/stop, CSR |
| `orchestrator/app/main.py` | skimmed | be-uploads | /chat, LiveGeneration, _finalize_generation, ref resolution, /chat/active, /chat/stop — Read 355-460, 625-770, 1025-1075, 1890-1930, 2348-2450 for handoffs only; not my slice |
| `orchestrator/app/main.py` | skimmed | infra | Lifespan (no generation persistence on shutdown), LiveGeneration in-process registry, /chat/active — Read 40-140, 195-330, 2398-2440 and greps; the /chat handler body (843-2397) no |
| `orchestrator/app/main.py` | skimmed | be-video | Lifespan wiring of pipeline.start/stop and the busy probe; _resolve_video_refs; the video branch of the chat turn — Read 92-120, 720-745, 1890-1930 only — the rest belongs to anoth |
| `orchestrator/app/main.py` | skimmed | fe-chat | LiveGeneration, _finalize_generation (persist only when subscribers == 0), /chat replacing a running generation, /chat/active membership — Not my slice; read lines 199-380, 1015-10 |
| `orchestrator/app/main.py` | skimmed | tests | Verification only: LiveGeneration.follow subscriber accounting (242-278), _finalize_generation subscribers==0 gate (358-366), POST /chat cancels previous task (1025-1035), /chat/st |
| `orchestrator/app/memory.py` | skimmed | rest | In-process session memory dict (legacy Phase 1) — lines 1-8 only |
| `orchestrator/app/memory_api.py` | reviewed | rest | /memory/facts list/add/delete, user-scoped; validates source_conversation ownership |
| `orchestrator/app/memory_recall.py` | skimmed | rest | Keyword cross-chat recall over the user's other conversations — lines 1-10 only |
| `orchestrator/app/memory_semantic.py` | skimmed | rest | Embedding-based cross-chat recall (packed float32 in PostgreSQL) — lines 1-10 only |
| `orchestrator/app/metrics.py` | reviewed | rest | Stdlib Prometheus registry with closed label vocabularies (incl. video stage set) and fixed 5ms-30s histogram buckets |
| `orchestrator/app/model_capabilities.py` | skimmed | rest | Typed capability declarations per model endpoint (extra_body, output limits) — lines 1-8 only |
| `orchestrator/app/recall.py` | skimmed | rest | Semantic recall over compacted turns (conversation_chunks keyed by conversation_id) — lines 1-10 only |
| `orchestrator/app/rerank.py` | skimmed | rest | Templated Qwen3-Reranker client (ADR-0001 D4) — lines 1-12 only |
| `orchestrator/app/share_api.py` | reviewed | rest | Owner share routes (/conversations/{id}/share*) and the one anonymous route /public/shares/{token}; in-process rate limiters |
| `orchestrator/app/sharing.py` | reviewed | rest | Share token mint/verify, provenance policy (PRIVATE_ROUTES incl. video, PRIVATE_META_KEYS incl. attachments/video/report_files), snapshot allowlist builder, expiry |
| `orchestrator/app/sse.py` | reviewed | be-chat | SSE frame formatting, HEARTBEAT_SECONDS (15s), allowed event names |
| `orchestrator/app/sse.py` | reviewed | rest | The one SSE frame formatter: token/meta/done/error + reasoning/step/status/research; 15 s heartbeat comment |
| `orchestrator/app/summarize.py` | skimmed | rest | Incremental rolling summary / condense prompts — lines 1-8 only |
| `orchestrator/app/titling.py` | skimmed | rest | AI conversation titles on the router model — lines 1-12 only |
| `orchestrator/app/uploads.py` | excluded | rest | Upload routes incl. chunked rail; require_attachments hard 403 — owned by another reader; consulted lines 70-110 for the feature gate only |
| `orchestrator/app/uploads.py` | reviewed | be-uploads | Single-shot and chunked upload rails, purposes (dataset/document/video), finalisers, download, list, ownership claim (_own) |
| `orchestrator/app/uploads.py` | skimmed | fe-attach | Server side of the upload contract (single-shot purpose handling, chunked init/part/complete) — Read 95-160 and 453-625 only to confirm the contract the frontend relies on |
| `orchestrator/app/uploads.py` | skimmed | infra | Single-shot and chunked upload handlers; upload_root under WORKSPACE_DIR/uploads; sweep trigger; synchronous assembly — Read 1-300 and 453-660 (every upload and chunked handler); 3 |
| `orchestrator/app/uploads.py` | skimmed | be-chat | Upload routes; purpose=video hands off to video.api.attach_upload — grep hits only (lines 103-141, 216-221, 465, 516-535, 615-618); belongs to the upload slice. |
| `orchestrator/app/uploads.py` | skimmed | be-video | Upload routes; the purpose=video hand-off to attach_upload from the single and chunked paths — Read 58-76, 95-145, 210-240, 515-580, 600-640 only — the chunked rail itself is anoth |
| `orchestrator/app/uploads.py` | skimmed | tests | Verification only: _finalise_video (217-235), chunked init/part/complete (455-619) — Not in slice; ranges read to confirm chunked-complete findings |
| `orchestrator/app/usage.py` | reviewed | rest | usage_events row per completed turn, idempotent on generation_id (ON CONFLICT DO NOTHING) |
| `orchestrator/app/usage.py` | skimmed | be-uploads | usage_events writer (ON CONFLICT DO NOTHING on generation_id) — Read 54-130 |
| `orchestrator/app/video/__init__.py` | reviewed | be-video | Package docstring (stage overview) |
| `orchestrator/app/video/api.py` | excluded | rest | GET /video/{conversation_id}/{upload_id}/status behind require_user + require_video; no cancel route found — owned by the video reader; grep of decorators/gates only |
| `orchestrator/app/video/api.py` | reviewed | be-chat | attach_upload (upload-time job start, keyed by content hash), GET /video/{conv}/{upload}/status, require_video gate |
| `orchestrator/app/video/api.py` | reviewed | be-uploads | attach_upload hand-off (hash, upsert analysis, adopt bytes, link, upload row, start job), status endpoint, feature gate |
| `orchestrator/app/video/api.py` | reviewed | be-video | attach_upload hand-off (hash, upsert, adopt, link, ensure_running) and GET status |
| `orchestrator/app/video/api.py` | skimmed | tests | Verification only: attach_upload hashes the file in a thread before responding (83-86); GET /video/{conv}/{upload}/status (154-170); no cancel endpoint — grep only |
| `orchestrator/app/video/api.py` | skimmed | infra | attach_upload (hash in thread, 4 GB cap after assembly), /video/{conv}/{upload}/status (owner-checked) — Read 1-120 plus grep of routes and ownership checks |
| `orchestrator/app/video/api.py` | skimmed | fe-attach | attach_upload: video cap, content-hash dedupe, analysis start at upload time — Read 55-110 |
| `orchestrator/app/video/artifacts.py` | reviewed | be-video | transcript/subtitle/summary file writers and timestamp formatting |
| `orchestrator/app/video/frames.py` | reviewed | be-video | pHash dedupe, thinning and cap of extracted frames |
| `orchestrator/app/video/fusion.py` | reviewed | be-video | Evidence pack -> Understanding via main-model JSON completion (direct or map-reduce) |
| `orchestrator/app/video/index.py` | reviewed | be-video | LanceDB video_chunks table: chunking, embed, replace-by-analysis, retrieve |
| `orchestrator/app/video/media.py` | reviewed | be-video | ffprobe/ffmpeg subprocess wrappers: probe, audio extraction, frame extraction, single-frame render |
| `orchestrator/app/video/pipeline.py` | excluded | rest | Detached hash-keyed analysis job; emits metrics video_stage_seconds/video_stage_total — video reader; grep of metrics call sites (lines 110, 338-339, 415) only |
| `orchestrator/app/video/pipeline.py` | reviewed | be-video | The detached analysis job: registry, in-process semaphore, stage runner with cache/timeout, lifecycle (requeue, drain, orphan reaping), pacing |
| `orchestrator/app/video/pipeline.py` | skimmed | be-uploads | Detached analysis job registry, subscribe/publish, ensure_running, _run/_finish, startup requeue, maintenance/reap — Read 170-445 and 725-803; individual stage functions (444-725)  |
| `orchestrator/app/video/pipeline.py` | skimmed | be-chat | Detached analysis job: registry (_tasks/_listeners/_latest), subscribe/ensure_running/wait_for, _run/_finish, stage runner, start (requeue) / maintenance drain — Read 40-115 (busy  |
| `orchestrator/app/video/pipeline.py` | skimmed | infra | Detached job runner; CancelledError leaves row 'running'; start() requeues after restart; in-memory _latest/subscribers — Read 318-340 and 728-766 plus grep of lifecycle functions |
| `orchestrator/app/video/screen.py` | reviewed | be-video | OCR batches and router-VLM captions per kept frame; span building |
| `orchestrator/app/video/store.py` | reviewed | be-uploads | Content-addressed analysis directory under VIDEO_DATA_DIR; hash, adopt_source (hard link/copy), atomic json writes |
| `orchestrator/app/video/store.py` | reviewed | be-video | On-disk layout per content hash; source adoption (hardlink/copy); atomic JSON writes |
| `orchestrator/app/video/store.py` | skimmed | infra | /data/video/<sha256> layout, atomic stage writes, hard-link adoption from workspace — Read 1-140 |
| `orchestrator/app/video/transcribe.py` | reviewed | be-video | WAV memmap -> VAD windows -> ASR clips (concurrent, paced) -> stitched segments |
| `orchestrator/app/video/types.py` | excluded | rest | STAGES tuple (probe…artifacts) and titles — video reader; lines 164-178 checked against metrics._ALLOWED['stage'] |
| `orchestrator/app/video/types.py` | reviewed | be-video | Segment/OcrSpan/Chapter/Understanding dataclasses and STAGES order |
| `orchestrator/app/video/vad.py` | reviewed | be-video | Voice-activity flags -> speech regions -> transcription windows |
| `orchestrator/app/web_index.py` | skimmed | rest | LanceDB chunk index over stored web pages — lines 1-12 only |
| `orchestrator/app/web_memory.py` | skimmed | rest | Living knowledge layer: ranked source-backed evidence — lines 1-12 only |
| `orchestrator/app/web_worker.py` | skimmed | rest | In-process background worker draining the web index / refreshing stale pages (emits metrics.worker_job) — lines 1-12 only |
| `orchestrator/requirements-dev.txt` | reviewed | tests | Offline test deps: psycopg, lancedb 0.37.1, trafilatura, webrtcvad-wheels, pillow, pytest; no ffmpeg (system) |
| `orchestrator/tests/(other ~105 test modules)` | excluded | tests | Engines, knowledge, research, search, salesforce, admin, etc. — grep for LiveGeneration/chat/active/attach/stop/purpose=video/chunked/part_index matched none of them beyond the rev |
| `orchestrator/tests/conftest.py` | reviewed | tests | Session PostgreSQL test DB (TRUNCATE per test, C collation guard), per-test lancedb/video_data_dir under tmp_path, ambient 'local' identity shim, as_user/login_client/anonymous_mod |
| `orchestrator/tests/test_authn_idor.py` | reviewed | tests | Two real sessions: conversations, single-shot uploads, reports, facts, stop/attach/active, salesforce state, POST /chat gate, anonymous 401 |
| `orchestrator/tests/test_chat_modes.py` | reviewed | tests | Router classes, model/effort resolution, POST /chat assistant/salesforce meta contract with generation_id |
| `orchestrator/tests/test_continuation.py` | skimmed | tests | Long-form continuation stopping rules (faked model) — Header + test names only; not LiveGeneration lifecycle |
| `orchestrator/tests/test_conversation_integrity.py` | reviewed | tests | PUT /messages no-shrink (409), same-length/growth allowed, atomic replace, truncate with expected_total, generation_id dedupe incl. threading.Barrier race, startup migration, /heal |
| `orchestrator/tests/test_document_prewarm.py` | skimmed | tests | Document render policy and upload-time cache — Header + test names only |
| `orchestrator/tests/test_document_uploads.py` | reviewed | tests | purpose=document single-shot; chunked init/part/complete reassembly, middle-hole 400, forged id 404, owner scoping, part index bound, oversized part 413, dataset finaliser; referen |
| `orchestrator/tests/test_document_uploads.py` | skimmed | be-uploads | Chunked rail tests — Read 92-200 (chunked tests) to see what is and is not covered |
| `orchestrator/tests/test_endpoints.py` | reviewed | tests | /health probes and /reports ownership smoke tests |
| `orchestrator/tests/test_full_documents.py` | skimmed | tests | docx extraction and document memory — Header + test names only |
| `orchestrator/tests/test_history.py` | reviewed | tests | History CRUD, meta round-trip, ordering, cross-owner 404; 'needs no credentials' test |
| `orchestrator/tests/test_history_search.py` | skimmed | tests | GET /history/search behaviour and isolation — Header + test names only |
| `orchestrator/tests/test_history_v3.py` | reviewed | tests | Pin/archive migration idempotence, PUT subsets, listing order, owner scoping |
| `orchestrator/tests/test_live_generation.py` | reviewed | tests | LiveGeneration follow replay, finalize persists only when subscribers==0 and not cancelled, /chat/active empty + attach 404 after completion, stop no-op, hand-simulated same-conver |
| `orchestrator/tests/test_live_generation.py` | skimmed | be-chat | Pins finalize-persists-only-when-detached and replace-on-resend behaviour — Read 55-170. |
| `orchestrator/tests/test_message_feedback.py` | skimmed | tests | Feedback survives PUT /messages whole-thread rewrite (rows deleted and reinserted with new ids) and truncate — Lines 190-300 only (PUT semantics section) |
| `orchestrator/tests/test_multi_image.py` | skimmed | tests | ChatRequest images list validation — Header + test names only |
| `orchestrator/tests/test_orchestrate.py` | skimmed | tests | Auto-orchestration classifier — Header + test names only |
| `orchestrator/tests/test_sse.py` | reviewed | tests | SSE frame formatting for token/meta/done/error |
| `orchestrator/tests/test_sse_v2.py` | reviewed | tests | reasoning/step/status/research event sets; step status validation |
| `orchestrator/tests/test_video_understanding.py` | reviewed | tests | Offline video pipeline logic: VAD, stitch, frames, artifacts, fusion, index scoping, engine step forwarding, V28 schema (hash dedupe, link, orphan reap, requeue), pipeline version  |
| `orchestrator/tests/test_video_understanding.py` | skimmed | be-video | Video pipeline tests — grep only: it covers requeue (677-679) and wait_for (717); no test exercises gather-sibling orphaning, CancelledError in media._run, BATCH_POOL starvation, o |
| `orchestrator/tests/test_video_understanding.py` | skimmed | be-uploads | Video schema/pipeline tests — Read 600-700 plus grep; no test exercises chunked complete -> attach_upload end to end |
| `scripts/backup-knowledge.sh` | skimmed | rest | pg_dump + knowledge stores backup — header only |
| `scripts/burnin.sh` | skimmed | rest | Multi-hour full-stack burn-in (untracked) — header only |
| `scripts/check-env-files.sh` | skimmed | rest | Env-file lint — header only |
| `scripts/cluster-bench.sh` | skimmed | rest | vllm bench serve against live API — header only |
| `scripts/cluster-doctor.sh` | skimmed | rest | Two-node preflight diagnostics — header only |
| `scripts/cluster-down.sh` | skimmed | rest | Stop cluster — header only |
| `scripts/cluster-logs.sh` | skimmed | rest | Cluster logs — header only |
| `scripts/cluster-status.sh` | skimmed | rest | Live cluster status report — header only |
| `scripts/cluster-sync.sh` | skimmed | rest | Worker host preflight/sync — header only |
| `scripts/cluster-test.sh` | skimmed | rest | NCCL all-reduce test — header only |
| `scripts/cluster-up.sh` | skimmed | rest | Compatibility alias for techsara up — header only |
| `scripts/cluster-worker.sh` | skimmed | rest | Control node-2 worker — header only |
| `scripts/deploy-db-rehearsal.sh` | skimmed | rest | Fresh/upgrade/restore schema rehearsal — header only; infra reader |
| `scripts/deploy-drain.sh` | reviewed | infra | Grace-period audit and established-connection quiet-window wait before recreate (advisory only) |
| `scripts/deploy-drain.sh` | skimmed | rest | Wait for in-flight requests before recreate — header only; infra reader |
| `scripts/deploy-lock.sh` | skimmed | rest | Deploy lock — header only; infra reader |
| `scripts/deploy-preflight.sh` | skimmed | infra | Build once, manifest image ids, verify running containers — Header 1-60 and function greps only |
| `scripts/deploy-preflight.sh` | skimmed | rest | Build once, pin image ids — header only; infra reader |
| `scripts/deploy-record.sh` | skimmed | infra | Pre-deploy state record — Header 1-40 only |
| `scripts/deploy-record.sh` | skimmed | rest | Record running state before replace — header only; infra reader |
| `scripts/deploy-rollback.sh` | skimmed | rest | Restore recorded images — header only; infra reader |
| `scripts/deploy-smoke.sh` | skimmed | rest | Post-rollout invariants — header only; infra reader |
| `scripts/deploy.sh` | reviewed | infra | Deploy: schema gate, land checkout (git clean -fd), record, preflight build, drain, techsara up, digest gate, health gate, rollback |
| `scripts/deploy.sh` | skimmed | rest | Deploy a commit on this machine (ff, rebuild, recreate, verify) — header only; infra reader |
| `scripts/lib/cluster-common.sh` | reviewed | infra | Shared env parsing, ssh helpers, OCR scrape-target writer, head/worker compose wrappers |
| `scripts/lib/cluster-common.sh` | skimmed | rest | Shared cluster helpers — header only |
| `scripts/lib/deploy-common.sh` | reviewed | infra | Compose-chain reader from state.json, deploy flock, image-id and schema-version readers |
| `scripts/lib/deploy-common.sh` | skimmed | rest | Shared deploy plumbing — header only |
| `scripts/lib/env-load.sh` | skimmed | rest | Supported env-file loader — header only |
| `scripts/lib/env_export.py` | skimmed | rest | dotenv → shell export lines (no sourcing) — header only |
| `scripts/lib/nccl_allreduce_bench.py` | skimmed | rest | Two-node NCCL benchmark — header only |
| `scripts/monitoring.sh` | reviewed | infra | Monitoring profile lifecycle on head + worker exporters |
| `scripts/monitoring.sh` | skimmed | rest | Grafana/Prometheus up/down — header only; infra reader |
| `scripts/ocr.sh` | reviewed | infra | Move the OCR engine to the worker; records OCR_REMOTE_BASE_URL in .env |
| `scripts/ocr.sh` | skimmed | rest | OCR engine on the worker node — header only; infra reader |
| `scripts/setup-runner.sh` | skimmed | rest | Register GitHub Actions runner — header only; infra reader |
| `scripts/tunnel.sh` | skimmed | rest | Cloudflare Tunnel up/down — header only; infra reader |
| `scripts/video_edge_cases.sh` | reviewed | rest | Runs video_smoke.py over awkward inputs (short, silent, corrupt, webm, Hinglish, 33 min) |
| `scripts/video_smoke.py` | reviewed | rest | End-to-end video smoke: login (pins Secure cookie as header, no Origin), upload (chunked >90 MB, 64 MB parts: init/part/complete), /chat with video_uploads refs, follows step event |
| `scripts/video_smoke.py` | skimmed | infra | Live end-to-end recipe (states the status route has no frontend proxy) — Lines 1-40 |
| `scripts/whisper.sh` | reviewed | infra | Whisper engine lifecycle; records ASR_BASE_URLS in .env |
| `scripts/whisper.sh` | skimmed | rest | Whisper ASR engine lifecycle — header only; infra reader |
| `src/personal_llm_chabot/__init__.py` | reviewed | rest | Placeholder package (prints hello); unused by the platform |
| `sync-worker/syncworker/__init__.py` | skimmed | rest | Salesforce sync worker package (Bulk/REST extract → Parquet/DuckDB → LanceDB) — lines 1-6; grep shows no psycopg/postgres/uploads/conversations references anywhere in syncworker/ |
| `sync-worker/syncworker/chunking.py` | skimmed | rest | ~800-token chunker — lines 1-6 only |
| `sync-worker/syncworker/config.py` | skimmed | rest | Env + config.yaml loading — lines 1-6 only |
| `sync-worker/syncworker/embedding_index.py` | skimmed | rest | Embedding-index compatibility sidecar writer — lines 1-6 only |
| `sync-worker/syncworker/jsonlog.py` | skimmed | rest | JSON-lines logging — lines 1-6 only |
| `sync-worker/syncworker/main.py` | skimmed | rest | Sync loop entrypoint (interval, full/incremental extract, upsert, RAG index) — lines 1-6 only; no shared app tables |
| `sync-worker/syncworker/objects.py` | skimmed | rest | config.yaml object/field discovery CLI — lines 1-6 only |
| `sync-worker/syncworker/rag_index.py` | skimmed | rest | LanceDB chunk index of long-text SF fields — lines 1-6 only |
| `sync-worker/syncworker/retype.py` | skimmed | rest | One-shot typed-view migration CLI — lines 1-6 only |
| `sync-worker/syncworker/secrets.py` | skimmed | rest | Local SF credentials loading — lines 1-6 only |
| `sync-worker/syncworker/sf_auth.py` | skimmed | rest | JWT bearer OAuth flow — lines 1-6 only |
| `sync-worker/syncworker/sf_client.py` | skimmed | rest | Read-only Salesforce Bulk/REST client — lines 1-6 only |
| `sync-worker/syncworker/storage.py` | skimmed | rest | Parquet + DuckDB upsert + _sync_meta watermarks — lines 1-6 only |
| `sync-worker/syncworker/typemap.py` | skimmed | rest | SF field type → DuckDB type — lines 1-6 only |
| `sync-worker/syncworker/views.py` | skimmed | rest | Typed views over raw VARCHAR warehouse — lines 1-6 only |

## Findings (severity, then confirmed first)

- **F1** [critical/confirmed_by_code] (fe-chat) `frontend/components/ChatApp.tsx:605-631 (poll), 2510-2515 (unsent condition), 2530-2534 (uploadStatus keyed by message id); frontend/lib/history.ts:1027-1059 (force load rewrites ids and replaces the cache)` — While a video/dataset turn is still uploading in THIS tab, the 8-second /chat/active poll force-loads server history and replaces the on-screen user turn with the server's copy whose id is `srv-<conv>-<i>`; the 'Uploading video…' indicator (keyed on datasetUpload.messageId) vanishes and the red 'This message was never sent — the page closed while … still uploading' notice appears on a turn whose upload is running, with the composer still locked busy. When the upload finishes and startStream registers, the notice disappears and live progress shows. This reproduces 'one screenshot live progress, another the red notice' without any reload.
  - evidence: Poll: `if (id && !isStreaming(id) && !active.includes(id) && messagesRef.current.at(-1)?.role === 'user') { const conv = await getHistoryStore().load(id, { force: true }); … setMessages(conv.messages) }` (611-626). During an upload no stream is registered (isStreaming false) and /chat/active lists only _live_generations, which only POST /chat creates (main.py:1035; a purpose=video analysis never registers), so the branch fires every tick. loadConversation(force): `if (force && cached && server.messages.length < cached.messages.length) return cached;` — equal length proceeds — then `id: \`srv-${id}-${i}\`` (1032) and `upsertCached(conv)` (1059). Render: `uploadStatus={datasetUpload?.messageId
  - verify: In an existing chat attach a >150 MB video (throttle the network), press Send, watch the row: 'Uploading video…' for the first tick, then within 8-16 s the red never-sent notice appears; DevTools shows GET /api/history/conversations/<id> every 8 s.
- **T-01** [high/confirmed_by_code] (tests) `frontend/tests/auth-streams-401.test.ts:120-124; frontend/lib/streams.ts:172-183; frontend/components/ChatApp.tsx:509-513, 606-625, 2510-2518` — The suite pins the conflation of '/chat/active failed' with 'nothing running on the server': a 503/network error on reload makes the app load history and, for a turn still carrying meta.send_state, show the red 'never sent' notice even while the generation is running.
  - evidence: auth-streams-401.test.ts:120-124 `respond(503, …)` → `await expect(fetchServerActive()).resolves.toEqual([])` with no redirect ('an ordinary failure stays the quiet empty list'); streams.ts:172 `if (!res.ok) return [];` and 180-182 `catch { return []; }`; ChatApp.tsx:509-513 decides attach vs load with `active.includes(wanted)`; ChatApp.tsx:2510-2518 renders `unsent` whenever `m.meta?.send_state && i === thread.length - 1 && !isStreaming(activeId)`. No test distinguishes 'no stream in this tab', 'nothing on server' and 'unknown'.
  - verify: In the wire harness stub /api/chat/active → 503 after seeding a conversation whose last user turn has send_state:'uploading'; remount ChatApp with ?c=; observe data-testid=unsent-turn rendered and no /api/chat/attach call.
- **T-02** [high/confirmed_by_code] (tests) `frontend/tests/unsent-turn.test.tsx:21-35; frontend/components/ChatApp.tsx:149-160, 1306, 2512; frontend/components/MessageRow.tsx:881-883` — The notice always says 'the page closed while its files were still uploading' regardless of why the turn never went out; the value 'failed' (upload rejected 413/403/network) is written by production but never constructed or asserted by any test.
  - evidence: ChatApp.tsx:1306 `userMessage.meta = { …, send_state: 'failed' }` in the upload catch; unsentTurnState (149-160) reads only `a.id` per attachment, never `send_state`'s value; ChatApp.tsx:2512 gates on truthiness `m.meta?.send_state`; MessageRow.tsx:882-883 fixed copy 'the page closed while …'. unsent-turn.test.tsx:27 builds only `send_state: 'uploading'`.
  - verify: Render MessageRow with a turn whose meta.send_state='failed' and one attachment without id → text says 'page closed while X was still uploading'.
- **T-03** [high/confirmed_by_code] (tests) `frontend/components/ChatApp.tsx:1214-1239, 1293-1308; frontend/tests/_wireHarness.tsx:24-28` — A multi-file send links upload ids by the ORIGINAL array index after the failure branch has replaced meta.attachments with a filtered array, so a sibling that lands after another file fails writes its id onto the wrong entry or drops it; no test can express per-file failure.
  - evidence: ChatApp.tsx:1214 `Promise.all(uploadable.map(async (doc, i) => { … const entry = userMessage.meta?.attachments?.[i]; if (entry && !entry.id) { entry.id = ref.upload_id; persist(…) } }))`; catch at 1300-1305 `const landed = …filter((a) => a.id); if (landed.length) userMessage.meta.attachments = landed; else delete userMessage.meta.attachments;` while sibling promises keep running. _wireHarness.tsx:24-28 `setUploadFails` flips EVERY /api/upload; dataset-upload-feedback.test.tsx:116-126 deferredUpload holds one fetch only; video-attachment.test.tsx attaches one video.
  - verify: Attach [A,B,C]; make B's /api/upload reject immediately and A/C resolve later; assert stored meta.attachments ids — C's id lands on the wrong entry or nowhere; after clearAttachments + regenerate the row reports C 'still uploading'.
- **T-04** [high/confirmed_by_code] (tests) `orchestrator/app/uploads.py:513-540, 552-581, 583-598; orchestrator/tests/test_document_uploads.py:127-138, 174-183; frontend/lib/uploadDocument.ts:72-102` — complete cannot detect a missing FINAL part (contiguity is derived from the parts present; init records no size/part count and the client never sends one), and a part interrupted mid-body stays on disk as a valid-looking part; only a middle hole and an oversized part are tested.
  - evidence: uploads.py:594-598 `expected = [f"{i:05d}" for i in range(len(parts))]; if parts != expected: 400 'parts are not contiguous'`; init 532-537 stores only `{filename, purpose}`; chunked_part 566-581 opens `dest` and streams `request.stream()` with no expected-length check; uploadDocument.ts:78-80 sends conversation_id/filename/purpose only. test_document_uploads.py:127-138 uploads parts 0 and 2 (middle hole); 174-183 covers only `_PART_CAP`.
  - verify: init; PUT part/0 only for a 2-part file; POST complete → 200 and a truncated _original; for the interrupted case, abort a PUT mid-stream and list _parts.
- **ORCH-01** [high/confirmed_by_code] (be-chat) `orchestrator/app/main.py:2206-2210 (worker except), 342-401 (_finalize_generation), 31-35 (root log level INFO)` — A generation that fails leaves no server log and no persisted trace; if nobody is attached at that moment (the tab reloaded), the user sees a bare turn with no answer and no error, and the operator cannot find out why.
  - evidence: `except Exception as exc: gen.failed = True; await gen.publish("error", {"message": str(exc)})` — no log.exception. The `error` event goes only into gen.events, which a reloaded tab never reads (attach 404s once finished). _finalize_generation skips persistence when `gen.failed` (line 348) and pops the registry (345-346), so /chat/active reports nothing. The only log calls in main.py are at lines 86, 387, 394, 1562, 1796, 2270; the 'not persisting' line at 396-401 is DEBUG and invisible at the default INFO level. usage_events records status=error with no message.
  - verify: Force an engine exception (e.g. monkeypatch llm.stream_chat_events to raise) with no client attached; check orchestrator logs (nothing), history (no assistant row), /chat/active (empty).
- **ORCH-02** [high/confirmed_by_code] (be-chat) `orchestrator/app/main.py:1024-1034, 2206-2207, 267-274; frontend/lib/streams.ts:486-490` — Any second POST /chat for the same conversation (double Send, 'Send now' from a reloaded tab while the original tab's request is in flight, a retry, two tabs) silently cancels the running generation; its subscriber's stream just ends with no done/error, which the frontend treats as a completed answer, so a truncated reply is finalized and persisted while the replacement generation writes a second one. Nothing is logged.
  - evidence: `previous = _live_generations.get(conv_key_outer); if previous is not None and not previous.done and previous.task is not None and previous.user_id == viewer: previous.task.cancel()`. The cancelled worker only sets `gen.cancelled = True`; `_finalize_generation` calls `gen.finish()` and `follow()` breaks at `elif self.done: break` without emitting a terminal frame. streams.ts:488: `// Stream ended without a terminal event — treat as complete.` followed by `finalize(s, { status: 'done' })`. No log statement on the cancel path.
  - verify: Open the same conversation in two tabs, send in A, then send (or press 'Send now') in B within a few seconds; A's answer freezes and is saved as complete; B gets a fresh answer; grep logs for the conversation id (nothing).
- **ORCH-03** [high/confirmed_by_code] (be-chat) `orchestrator/app/main.py:280 (_live_generations), 96-115 (lifespan), 2394-2405 (/chat/active); orchestrator/app/video/pipeline.py:728-741, 791-803; orchestrator/app/db.py:5064-5075` — After an orchestrator restart (deploy, crash, OOM) every in-flight chat generation is lost with no record, while the video analysis it was waiting on is requeued and finishes anyway — so the video status endpoint shows progress/done, /chat/active says nothing is running, and no assistant message is ever written. This produces exactly the 'progress in one screenshot, red never-sent notice in the other' split.
  - evidence: `_live_generations: dict = {}` is process memory; lifespan only calls `video_pipeline.start()` which runs `UPDATE video_analyses SET status='queued' WHERE status='running'` and starts `_maintenance_loop` (drain after 5 s). Nothing persists 'a chat turn is pending for upload X'. `/chat/active` iterates the empty dict. The frontend's notice text blames 'the page closed while its files were uploading' (ChatApp.tsx:2508-2510) regardless of cause.
  - verify: Attach a video, wait for steps to stream, restart the orchestrator container, reload: GET /video/{conv}/{upload}/status shows running/done, GET /chat/active returns [], history has no assistant turn. Correlate yesterday's incident with container start time.
- **ORCH-04** [high/confirmed_by_code] (be-chat) `orchestrator/app/main.py:2183-2185 (done published), 347-354 (subscribers==0 gate), 257/274 (subscriber counting), 396-401 (DEBUG log)` — Persistence of a completed answer is delegated to whichever client is attached at finalize time; the terminal `done` is published before any durable write, so a client that receives `done` and then disconnects, reloads, loses network, or hits a 401 before its history save loses the answer permanently — the server never reconciles and does not log it above DEBUG.
  - evidence: `gen.answer = answer; memory.add_exchange(...); await gen.publish("done", ...)` precedes the `finally`. `_finalize_generation` persists only `if not gen.cancelled and not gen.failed and gen.subscribers == 0 and ...`. `subscribers` is +1 at generator start (line 257) and -1 in follow()'s finally (274); a subscriber still draining `done` keeps it at 1 during finalize. The docstring at 208-212 states the design: 'When a reader IS attached, the frontend persists as usual and the server stays out of it.'
  - verify: Attach a client that reads until `done` then closes without calling the history routes (curl -N piped to head); confirm no assistant row is written and the only log is at DEBUG.
- **F-01** [high/confirmed_by_code] (be-uploads) `orchestrator/app/core/repo.py:94-118; orchestrator/app/uploads.py:40-42, 126-131; orchestrator/app/engines/repo.py:32; orchestrator/app/video/store.py:26-29` — The 24-hour/quota sweep deletes the ENTIRE uploads tree of every user and conversation in one rmtree — including in-flight chunked `_parts`, `_chunked.json` markers and just-landed `_original` files — because all uploads live under a single top-level workspace entry.
  - evidence: upload_root() = os.path.join(settings.workspace_dir, "uploads", safe_conv, upload_id) (uploads.py:40-42), so every upload is under /data/workspaces/uploads. enforce_quota_and_ttl() iterates os.listdir(base) (repo.py:101-104), takes getmtime(p) of each TOP-LEVEL entry and does shutil.rmtree(p) when now - mtime > workspace_ttl_hours*3600 (repo.py:108-110); then, if the sum of _dir_size_bytes over the remaining entries exceeds workspace_quota_gb (20 GB in .env:184), it rmtree's whole entries oldest-first (repo.py:112-118). A directory's mtime only changes when a DIRECT child is created/removed, i.e. the first upload of a conversation that never uploaded before; uploads into existing conversatio
  - verify: On the host: stat -c %y /data/workspaces/uploads and compare with the time of the incident; grep the orchestrator log for 'upload not found' 404s on /uploads/chunked/.../part/N or /complete for the 400 MB video, and for a POST /uploads (single-shot, e.g. the 20 MB video) or a repo clone immediately 
- **F-02** [high/confirmed_by_code] (be-uploads) `orchestrator/app/uploads.py:578-619; orchestrator/app/video/api.py:79-89; frontend/app/api/upload/chunked/[...path]/route.ts:36-62; frontend/lib/uploadDocument.ts:97-102` — POST /complete is long-running (synchronous concatenation of the whole file on the event loop, then a full sha256, then DB work and job start), is not idempotent, and the analysis it starts is detached — so any proxy/edge/browser failure of that single response leaves the server with a 'ready' upload row, a link row and a RUNNING analysis while the client records the upload as failed (red 'never sent' notice) with no way to retry the same upload id.
  - evidence: chunked_complete concatenates all parts with blocking reads/writes inside `async def` (uploads.py:596-606), rmtree's _parts (607) and unlinks the marker (608) BEFORE calling _finalise_video; attach_upload then hashes the file (api.py:79), upserts the analysis (81), adopts the bytes (82), links (83-85), writes the uploads row (86-88) and starts the job (89). After line 608 a retried /complete is a 404 (_chunk_state, 496-505) and two concurrent completes race on the same raw_path/`os.unlink` (FileNotFoundError -> 500). The Next proxy turns any fetch failure (abort on reload, undici timeout) into 502 'upload service unreachable' (route.ts:60-62); Cloudflare's origin timeout applies on the publi
  - verify: Time POST /complete for a 400 MB and a 4 GB file on the host (log the request duration in uvicorn access logs); compare with Cloudflare's 100 s origin limit and undici's default 300 s body timeout in the Next proxy. Check video_attachments/uploads rows for the incident conversation: a link row whose
- **F1** [high/confirmed_by_code] (fe-attach) `frontend/lib/attachments.ts:185-203,253; frontend/components/ChatApp.tsx:1088,157; frontend/lib/history.ts:1045; frontend/components/MessageRow.tsx:882-883` — Every video-only turn in production is classified as `missing`, so the 'Send now' button never appears, the notice text is garbled, and Regenerate/Retry refuse with 'Re-attach the file'.
  - evidence: send() sets `pdfName: isPdf || isDataset || first?.kind === 'video' ? first?.name : undefined` (ChatApp.tsx:1088) and the history loader sets `pdfName: (m.meta as Meta).attachments![0].name` for any user turn with attachments (history.ts:1045). attachmentsForResend filters `docs = attachments.filter(kind === 'pdf')` (185); for a video-only turn docs is empty so it falls into `else if (message.pdfName) { if (rememberedPdfs[0]) ... else docsMissing = true; }` (199-202) -> missing=true. unsentTurnState uses `canResend: !resendOptionsFor(m).missing` (ChatApp.tsx:157). Probe run under vitest: WITH pdfName -> missing=true, videoUploads=2; WITHOUT pdfName -> missing=false. The unsent-turn test buil
  - verify: resendOptionsFor({id:'u',pdfName:'a.mp4',meta:{attachments:[{kind:'video',name:'a.mp4',id:'a'.repeat(32)}]}}).missing === true; or reload a chat whose last turn is an all-landed video turn with send_state and observe no Send now button; click Try again on any video answer after reload -> 'Re-attach 
- **F2** [high/confirmed_by_code] (fe-attach) `frontend/components/Composer.tsx:499-509,283; frontend/components/ChatApp.tsx:1060-1066,1216-1242,2585,2617` — A video attached in conversation A and sent after switching to conversation B (or to New chat) is uploaded and analysed under A but linked into B's history and sent as B's video_uploads.
  - evidence: withEarlyUpload captures `const conversationId = uploadConversationId` at attach time and starts uploadDocumentFile(att.file, conversationId, 'video') (Composer.tsx:499-508); uploadConversationId={activeId} (ChatApp.tsx:2617). Composer keeps `attachments` state across activeId changes: it is rendered without a key (ChatApp.tsx:2585), has no effect on uploadConversationId, and ComposerHandle exposes no clear. send() uses `let conversationId = activeId` at send time, minting a new id when null (ChatApp.tsx:1060-1066), then `const early = doc.uploadPromise ? await doc.uploadPromise : null; let ref = early;` (1216-1217) and links `entry.id = ref.upload_id` into the new conversation's turn (1238-
  - verify: Attach an mp4 in chat A, click chat B (or New chat), type and send. Inspect the /api/chat body: video_uploads carries an upload_id whose /api/uploads/<B>/<id>/file returns 404; the server-side outcome for video_uploads of another conversation is the video engine's slice.
- **F3** [high/confirmed_by_code] (fe-attach) `frontend/components/ChatApp.tsx:644-648,1183-1191,2508-2518` — Switching away from and back to a conversation whose upload send is still in flight in this tab shows the red 'never sent — the page closed while … still uploading' notice while the upload is actually running, and re-enables Send.
  - evidence: send() sets datasetUpload{conversationId, messageId, status:'uploading'} and send_state:'uploading' (1183-1191) but never setStreaming(true) on this path. The effect `setDatasetUpload((prev) => prev && prev.conversationId === activeId ? prev : null)` (644-648) nulls it on any switch and nothing restores it on return. The notice condition is `m.meta?.send_state && i === thread.length-1 && !isStreaming(activeId) && datasetUpload?.messageId !== m.id` (2512-2516): after the round trip datasetUpload is null and no stream is registered yet, so unsentTurnState renders. `busy={datasetUpload?.status === 'uploading'}` is also false, so a second send in the same conversation is possible while the first
  - verify: Attach a ~300 MB video, Send, click another chat, click back before the upload finishes: the row shows the 'never sent' notice and the composer accepts another send.
- **F2** [high/confirmed_by_code] (fe-chat) `frontend/components/ChatApp.tsx:2510-2515, 1467-1472 (runRegenerate guard), 2187-2188 (onRetry/onRegenerate); frontend/components/MessageRow.tsx:885-891; orchestrator/app/main.py:1025-1032` — The notice and its Send-now button consult only the in-tab stream registry (`isStreaming`), never `serverActive`, `reconciling` or the attach in progress. A turn carrying a stale `send_state` whose generation is running on the server (another tab; the 409-adopt race in F15; the attachStream's initial force-load round trip; a failed attach per F3) shows 'never sent' and offers Send now; pressing it runs runRegenerate, whose only guard is `if (!id || isStreaming(id)) return;`, and POST /chat makes the orchestrator cancel the running generation (`previous.task.cancel()`).
  - evidence: `!isStreaming(activeId) &&` at 2514 is the only liveness check; `serverActive`/`reconciling` are not read in that expression. runRegenerate: `const id = activeIdRef.current; if (!id || isStreaming(id)) return;` (1471-1472). MessageRow: `{unsent.canResend && (<button … onClick={onRegenerate}>Send now</button>)}`. main.py: `if previous is not None and not previous.done and previous.task is not None and previous.user_id == viewer: previous.task.cancel()`.
  - verify: Open the same chat in two tabs; send a two-video turn in tab A; in tab B (loaded while send_state was 'uploading') the notice shows while A streams; click Send now in B and watch A's stream die.
- **F3** [high/confirmed_by_code] (fe-chat) `frontend/lib/streams.ts:731-735 and 745-749; frontend/components/ChatApp.tsx:513-528 and 1863-1879; frontend/app/api/chat/attach/[id]/route.ts:54-73` — attachStream returns false for ANY non-OK response or thrown fetch (502 from the proxy when the orchestrator is restarting, a 401, a network drop), and both callers treat false as 'Finished during the reload gap — its answer is in history': they setStreaming(false), force-load history and unlock the composer. The generation is still running; the 8 s poll never re-attaches (it only loads when the id is NOT active), so the tab stays permanently detached (sidebar spinner only), a later send replaces the running generation server-side, and with a stale send_state the red notice is shown for a live generation. The attach status is not recorded anywhere (observability gap).
  - evidence: streams.ts: `if (!res.ok || !res.body) { streams.delete(conversationId); // finished already — history has it … return false; }` and `} catch (err) { … streams.delete(conversationId); notifyNow(conversationId); return false; }`. ChatApp: `void attachStream(wanted).then((ok) => { if (!ok && activeIdRef.current === wanted) { // Finished during the reload gap — its answer is in history. setStreaming(false); void store.load(wanted, { force: true })…` ; selectConversation: `if (!ok && activeIdRef.current === id) { setStreaming(isStreaming(id)); …`. Route: everything but 404/401 becomes `{ status: 502 }`. Poll only loads when `!active.includes(id)` (618).
  - verify: Start a long generation, reload while the orchestrator returns 502 for /chat/attach (or block that request in DevTools): the thread shows the user turn with no answer and no stream, the composer is enabled, and the sidebar spinner keeps turning.
- **F4** [high/confirmed_by_code] (fe-chat) `frontend/lib/streams.ts:168-180; frontend/app/api/chat/active/route.ts:16-28; frontend/components/ChatApp.tsx:509-538` — fetchServerActive returns [] on every non-401 failure and the route answers 200 {active: []} when the orchestrator is unreachable, so 'could not ask' is indistinguishable from 'nothing running'. On mount this skips the attach, settles `reconciling` and unlocks the composer while a detached generation runs; the next send cancels it (F2), and the poll then force-loads history every 8 s (F1 conditions).
  - evidence: streams.ts: `if (!res.ok) return []; … } catch { return []; }`. route.ts: `} catch { return Response.json({ active: [] }); }`. ChatApp: `const active = await fetchServerActive(); … if (active.includes(wanted)) { … attachStream … return; }` followed by `finally { settleReconcile(); …}`.
- **F5** [high/confirmed_by_code] (fe-chat) `frontend/components/ChatApp.tsx:440-458` — When /api/auth/me fails with a non-401 status (orchestrator booting, 5xx, offline) the mount effect fetches /chat/active and stores it for the sidebar but never calls attachStream, then settles reconciling and returns. A generation that is running on the server is shown as a bare user turn (with a stale send_state: the red notice), the composer unlocks, and only the eventual poll-driven history load brings the answer.
  - evidence: `if (!me.ok) { if (me.status === 401 || me.status === 403) {…} if (wanted) { const active = await fetchServerActive(); if (!cancelled) setServerActive(active); } settleReconcile(); return; }` — no attachStream on this branch.
- **F6** [high/confirmed_by_code] (fe-chat) `frontend/lib/streams.ts:286-296 (finalize); frontend/lib/history.ts:955-975 (appendMessage), 1013-1032 and 1059 (non-forced load replaces cache); frontend/components/ChatApp.tsx:531-535 (mount uses force=false when the cached thread ends on an assistant); orchestrator/app/main.py:358-372` — The server persists the answer only when `gen.subscribers == 0` at finish; with the browser attached, the client is the sole persister, and it persists asynchronously AFTER the 'done' event (saveMessages -> enqueue -> appendMessage). If the tab is closed/reloaded in that window the server never gets the answer, the sync state is not marked dirty (enqueue's catch never ran), and the next non-forced load compares ids (`sameIds` false), finds no dirty flag, fetches the server's shorter thread and replaces the cache with it — the only copy of the answer is deleted and it visibly disappears on reload.
  - evidence: finalize: `s.status = …; getHistoryStore().saveMessages(s.conversationId, s.messages); notifyNow(…)` — the network append is `enqueue(id, () => syncConversation(id))`. loadConversation(force=false): `if (!force && cached && Array.isArray(pushed) && sameIds(pushed, cached.messages)) return cached;` (lengths differ) → `if (!force && cached && (pushed === undefined || s.dirty.includes(id))) return cached;` (neither) → `const server = await api.get(id);` and the shrink guard is `if (force && …)` only → `upsertCached(conv)`. Mount: `const force = cached?.messages.at(-1)?.role === 'user';` is false when the cached tail is the assistant. main.py: `if (not gen.cancelled and not gen.failed and gen.su
  - verify: Block PUT/POST /api/history/* in DevTools, let an answer finish, reload: the answer is gone from both server and cache.
- **F7** [high/confirmed_by_code] (fe-chat) `frontend/lib/streams.ts:323-335; frontend/components/ChatApp.tsx:615-626 (poll guard), 1885-1895 (non-forced load); frontend/lib/history.ts:1013-1024` — markInterrupted persists an assistant row with status 'error' ('The model is still working on it — reopen this chat to re-join'). The thread now ends on an assistant message, so the poll's `role === 'user'` guard and the non-forced load's `sameIds` short-circuit never fetch the answer the orchestrator persisted when it finished with no subscriber; re-opening only re-joins while /chat/active still lists the id. The user is left with a permanent error row, and a later whole-thread push 409s and adopts [user, error, answer].
  - evidence: `updateAssistant(s, withLiveProgressRetired({ status: 'error', errorMessage: 'The connection to this answer was interrupted. The model is still working on it — reopen this chat to re-join.' })); s.status = 'error'; getHistoryStore().saveMessages(...)`. Poll: `messagesRef.current.at(-1)?.role === 'user'`. selectConversation non-active branch: `const force = cached?.messages.at(-1)?.role === 'user'; void store.load(id, { force })`. loadConversation: `sameIds(pushed, cached.messages)` → `return cached`.
- **F8a** [high/confirmed_by_code] (fe-chat) `frontend/components/ChatApp.tsx:1212-1250 (upload map), 1298-1310 (catch)` — When one upload rejects, Promise.all rejects and the catch filters `meta.attachments` down to entries with an id (or deletes the array). Sibling uploads keep running and, on landing, index `userMessage.meta?.attachments?.[i]` into the filtered/deleted array — the entry is undefined, so the id is never linked. The server holds the bytes and (for video) a running analysis that no turn references; after reload unsentTurnState sees nothing missing for the dropped entry, and Send now re-sends without it.
  - evidence: `const entry = userMessage.meta?.attachments?.[i]; if (entry && !entry.id) { entry.id = ref.upload_id; persist(conversationId, turns); }` inside `Promise.all(uploadable.map(async (doc, i) => …))`; catch: `const landed = userMessage.meta.attachments.filter((a) => a.id); if (landed.length) userMessage.meta.attachments = landed; else delete userMessage.meta.attachments;` then `return;`. Example: [A.pdf, B.mp4 20 MB, C.mp4 400 MB]; A is refused (415) first → attachments deleted → B and C land later and are never linked.
- **F8b** [high/confirmed_by_code] (fe-chat) `frontend/components/ChatApp.tsx:1300-1308; frontend/components/ChatApp.tsx:140-152 (unsentTurnState); frontend/lib/attachments.ts:186-196 (videos: no id = missing); frontend/components/MessageRow.tsx:880-891` — For a two-video turn where the 20 MB file landed and the 400 MB one failed, the catch rewrites `meta.attachments` to [20 MB only] and marks send_state 'failed'. After a reload or a conversation switch (which clears datasetUpload), unsentTurnState reports missing=[] and canResend=true, the notice says 'the page closed while its files were still uploading' and Send now sends the turn with only the 20 MB video — the 400 MB attachment is silently gone, with no record that it was ever attached.
  - evidence: `if (landed.length) userMessage.meta.attachments = landed;` removes the failed entry; `unsentTurnState` = `missing = atts.filter((a) => !a.id)` → empty; `canResend: !resendOptionsFor(m).missing` → true; MessageRow shows the canResend sentence and the button.
- **V1** [high/confirmed_by_code] (be-video) `orchestrator/app/video/transcribe.py:256 (gather), :233-240 (only ASRRejected caught); orchestrator/app/asr.py:543-545 (BATCH_POOL 8-s wait -> ASRBusy), :667 (transcribe_segments under BATCH_POOL)` — When any window raises something other than ASRRejected (ASRUnavailable after both engines are stood down, ASRBusy, or the failure-threshold re-raise at :238-239), transcribe_audio fails the stage while every sibling window task keeps running to the end of the recording, holding the two BATCH_POOL slots; the job is reported failed, the GPU keeps paying for a transcript nobody will read, and a retry (re-attach -> upsert requeues the failed row -> ensure_running) hits BATCH_POOL's 8-s acquire timeout and fails again with 'ASRBusy: every transcription slot is busy' until the orphans drain.
  - evidence: `await asyncio.gather(*(one(i, w) for i, w in enumerate(windows)))` has no return_exceptions and nothing cancels the siblings on the first exception (asyncio.gather propagates the first error and leaves the other awaitables running). Inside `one()` only `except asr.ASRRejected` exists; `asr.transcribe_segments` raises ASRUnavailable/ASRBusy through it. Each orphan still does `async with gate` -> `_pipe.pace()` -> `asr.transcribe_segments` -> `async with BATCH_POOL` (asr.py:667). `_Runner.stage` (pipeline.py:333-335) records the stage failed and `_run` fails the job after the other branch finishes; `stop()` only cancels tasks in `_tasks` (pipeline.py:747-749), not gather children.
  - verify: Stub asr.transcribe_segments to raise ASRUnavailable on the 3rd call and count calls after transcribe_audio has raised; or in prod stop the whisper containers mid-transcript, watch asr_batch_requests_total keep climbing after video_jobs_total{result=fail} increments, then re-attach and observe the A
- **V2** [high/confirmed_by_code] (be-video) `orchestrator/app/video/media.py:109-116 (kills only on asyncio.TimeoutError); orchestrator/app/video/pipeline.py:322-332 (stage-level wait_for -> CancelledError into the stage), :743-749 (stop() cancels tasks); :483 and :552 (per-call deadlines longer than the stage timeout for long videos)` — A stage-level timeout or a shutdown cancels the stage coroutine with CancelledError, which media._run does not catch, so the ffmpeg/ffprobe child is left running (the event loop's child watcher keeps the transport alive until the child exits on its own). The stage is recorded failed and a re-attach retry launches a second ffmpeg writing the same audio.wav.part (media.py:248) or frames/f_%06d.jpg (media.py:313) while the first is still writing them.
  - evidence: `try: out, err = await asyncio.wait_for(proc.communicate(), timeout=timeout_s) except asyncio.TimeoutError: proc.kill()` — no `except BaseException`/`CancelledError`/`finally`. pipeline.py `_Runner.stage` wraps the stage in `asyncio.wait_for(..., timeout=settings.video_stage_timeout_s)` (7200 s) and re-raises CancelledError at :326-330. `_stage_audio` passes `timeout=max(120, duration*0.5+60)` (:483) and `_stage_frames` `max(300, duration*1.5+120)` (:552): for a 4-hour file these are 7260 s and 21720 s, so the outer 7200-s cancel is the path that fires. `upsert_video_analysis` requeues a failed row on re-upload (db.py:5077-5082) and `_run` re-runs the stage with the same output paths.
  - verify: Set VIDEO_STAGE_TIMEOUT_S=10, attach a 30-minute video, wait for 'Extracting audio: did not finish within 10s', then `ps -o pid,etime,cmd -C ffmpeg` inside the orchestrator container: the decoder is still running. Re-attach the file and observe two ffmpeg processes on the same source.
- **V3** [high/confirmed_by_code] (be-video) `orchestrator/app/video/pipeline.py:302-304 (cached = done|skipped + file exists), :612-614 (OCR 'done' with 0 legible), :498-502 (transcript 'skipped' when ASR_ENABLED is false), :593-595 (OCR 'skipped' when disabled), :640 (captions 'skipped'); orchestrator/app/engines/ocr.py:168-170 and :176-181 (every OCR failure and a batch deadline become '')` — OCR engine outages, degenerate OCR output (memory: the production OCR prompt currently loops garbage) and whole-batch deadlines are indistinguishable from 'no text on screen': the stage is stamped done and is served from cache on every later attach of the same bytes, forever, unless PIPELINE_VERSION is bumped. The same permanence applies to stages skipped by configuration: a video attached while `whisper.sh down` has flipped ASR_ENABLED off is permanently 'no transcript' with status done.
  - evidence: screen.ocr_frames calls `ocr.ocr_images(...)` (screen.py:122) which does `except Exception ... return ""` per image and `return ["" for _ in images]` on deadline; `_stage_ocr` then returns `_StageResult("done", f"{legible}/{len(kept)} frames had readable text")` and writes ocr.json. `_Runner.stage`: `cached = state.get("status") in ("done", "skipped") and (output is None or os.path.exists(...))` -> publishes 'from an earlier run' and returns without re-running. `_stage_transcript`: `if not settings.asr_enabled: store.write_json(transcript.json, ...); return _StageResult("skipped", ...)`. `is_current` (pipeline.py:69-71) only compares pipeline_version.
  - verify: Stop vllm-ocr, attach a slide-heavy video (stage reports 'done · 0/N frames had readable text'), start vllm-ocr, re-attach the same file: the OCR step says 'cached'. Repeat with `whisper.sh down` for the transcript.
- **INF-1** [high/confirmed_by_code] (infra) `compose.yaml:291-315; scripts/deploy.sh:437-438; scripts/deploy-drain.sh:43-48,111-115,124-133; .runtime/logs/deploy-20260909-{204736,213033,215857,224832}.log` — The frontend service declares no stop_grace_period, so on every deploy that rebuilds the frontend image Docker SIGKILLs the Next.js proxy 10 s after SIGTERM, cutting every in-flight chunked part PUT, single-shot upload and SSE proxy stream; deploy.sh knows (the drain check fails) and continues anyway. This happened four times on 2026-09-09 between 20:47 and 22:52 IST.
  - evidence: compose.yaml frontend block (291-315) has init/restart/healthcheck/depends_on but no stop_grace_period, while orchestrator has `stop_grace_period: 2m` (229). deploy-drain.sh lists frontend in DR_TRAFFIC_SERVICES (46) and returns 1 when a recreated traffic service is under DR_MIN_GRACE=30 s (113-115, 124-133). deploy.sh:437-438: `"$ROOT/scripts/deploy-drain.sh" check ... || say "  drain: a service being recreated is on Docker's 10s default"` — logged, not fatal. Every 2026-09-09 deploy log contains `frontend         recreate  (unset)      TOO SHORT for live traffic (<30s)` followed by `techsara up` and `DEPLOYED`. The 21:58 and 22:48 runs additionally logged `drain wait: frontend still has 1 
  - verify: `docker inspect sf-local-ai-frontend-1 --format '{{.HostConfig.StopTimeout}}'` (null = 10 s). Correlate the user's incident time with the deploy timestamps above. Fix: add `stop_grace_period: 2m` (or longer) to the frontend service and make the drain check fatal for traffic services.
- **INF-2** [high/confirmed_by_code] (infra) `frontend/app/api/chat/active/route.ts:16-28; orchestrator/app/main.py:2398-2410; launcher/techsara_cli/cli.py:1189-1198` — During the orchestrator's recreate window (SIGTERM -> up to 120 s drain -> image start -> lifespan migrations -> health), GET /api/chat/active turns every transport failure into HTTP 200 `{active: []}`, so a reloaded tab is told 'nothing is running on the server' when the truth is 'the server is unreachable'. Since the launcher recreates the orchestrator before the frontend, the old bundle serves that answer for the whole window.
  - evidence: route.ts:16-28: `try { const upstream = await fetch(`${orchestratorUrl}/chat/active`, ...); return Response.json(await upstream.json(), { status: upstream.status }); } catch { return Response.json({ active: [] }); }` — a refused socket, DNS failure or aborted fetch all become a successful empty list with no log line. main.py:2398-2410 builds `active` only from the in-process `_live_generations` dict. cli.py:1190-1197: `compose.up_service("orchestrator")`, `compose.wait_service("orchestrator", timeout=300.0 ...)`, then `compose.up_service("frontend")`. The deploy logs show the orchestrator was health-waited at 20:47 ('ready after 6s') and 21:58 ('ready after 6s') — i.e. recreated — while any 
  - verify: Stop the orchestrator container and curl http://127.0.0.1:3000/api/chat/active with a valid cookie: expect 200 `{"active":[]}` rather than 502/503. Fix: return 502/503 (or `{active: null, unavailable: true}`) on catch and make the client distinguish 'unknown' from 'none'.
- **INF-3** [high/confirmed_by_code] (infra) `compose.yaml:227-229; orchestrator/Dockerfile.cuda:188; orchestrator/app/main.py:48-121,199-217,290; .env (GEN_WALL_CLOCK_S=4200); orchestrator/app/video/pipeline.py:322-330,728-750` — A recreate SIGKILLs the orchestrator at 120 s while uvicorn (no --timeout-graceful-shutdown) is still waiting for in-flight SSE responses that may legitimately run 4,200 s; every LiveGeneration is in-process only and nothing persists a partial or unattached answer at shutdown, and the lifespan `finally` (pool close, video stop) never runs on SIGKILL. Video jobs DO survive (requeued from the DB row) but their in-memory progress and subscribers do not.
  - evidence: compose.yaml:229 `stop_grace_period: 2m`; Dockerfile.cuda:188 `CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8080"]` (no --timeout-graceful-shutdown, so uvicorn's shutdown waits for open connections indefinitely — an in-flight response is allowed to complete). Live .env: `GEN_WALL_CLOCK_S=4200` (config.py:512 default 1800, llm_request_timeout defaults to it at 1285-1286). main.py:290 `_live_generations: dict = {}`; the lifespan's `finally` (118-121) only calls video_pipeline.stop(), web_worker.stop(), db.close_pool() — no generation persistence. LiveGeneration docstring (199-217): the answer is persisted server-side only 'if the generation finishes while NOBODY is attached'
  - verify: Start a long generation, run `docker compose ... up -d --no-deps --force-recreate orchestrator`, then reload: /chat/active is empty and the assistant turn is absent from history. Fix options: uvicorn `--timeout-graceful-shutdown` shorter than stop_grace_period plus a shutdown hook that persists part
- **INF-4** [high/confirmed_by_code] (infra) `orchestrator/app/core/repo.py:95-120; orchestrator/app/uploads.py:43-45,126-130,498-509,545-580; .env (WORKSPACE_TTL_HOURS=24, WORKSPACE_QUOTA_GB=20, UPLOAD_MAX_MB=102400)` — The 24 h / 20 GB workspace sweep deletes whole TOP-LEVEL directories of WORKSPACE_DIR, and every upload of every conversation lives under the single top-level directory `uploads/`, whose mtime only changes when a brand-new conversation gets its first upload. Any single-shot upload by any user (including the 20 MB sibling video of the reported turn) calls the sweep first; if `uploads/` is older than 24 h, or the tree exceeds 20 GB (trivial with UPLOAD_MAX_MB=100 GB and 4 GB videos), the entire tree — including the 400 MB sibling's in-progress `_parts` and `_chunked.json` — is removed, and its next part PUT fails with 404 'upload not found'.
  - evidence: uploads.py:43-45 `upload_root` = `os.path.join(settings.workspace_dir, "uploads", safe_conv, upload_id)`. repo.py:95-120 (`enforce_quota_and_ttl`): `for name in os.listdir(base): p = os.path.join(base, name) ... mtime = os.path.getmtime(p); if now - mtime > settings.workspace_ttl_hours * 3600: shutil.rmtree(p, ignore_errors=True)` and then, oldest-first, `if total <= quota: break ... shutil.rmtree(p)` — `p` is the top-level entry, i.e. `uploads/`. uploads.py:126-130: `create_upload` runs `enforce_quota_and_ttl()` BEFORE `_stream_to_disk` creates its own directory (58), so the sweep sees the tree as it was. Chunked handlers never call the sweep (511-543, 545-580), so a chunked upload's parts 
  - verify: `stat -c %y` on /data/workspaces/uploads inside the container: if older than 24 h, the next single-shot upload wipes it. Reproduce: touch -d '25 hours ago' the uploads dir, start a chunked upload, then POST a small document from another conversation; the chunked upload's next part returns 404. Fix: 
- **S-01** [medium/confirmed_by_code] (rest) `orchestrator/app/authn/features.py:210-252 (enforce_chat), :183-188 (allowed); orchestrator/app/main.py:947-975 (consulted)` — The server-side chat gate for attachments and video is not part of enforce_chat; main.py bolts it on by nulling request.video_uploads / pdf_uploads and continuing the turn as plain chat with only a status line, so a member whose video/attachments flag is turned off between attach and Send gets an answer that silently ignores the already-uploaded, already-analysing video.
  - evidence: features.py:210-252 `enforce_chat` returns a ChatGate with mode/web_search/deep_research/sf_live and never reads Feature.ATTACHMENTS or Feature.VIDEO_ANALYSIS; main.py:948-975 `attachments_allowed = feature_access.allowed(...)` then `request.video_uploads = None` / `request.pdf_uploads = None` and `access_notice = blocked_notice([... 'Video understanding'])`. uploads.py:80-100 is a hard 403 at upload time, so the two surfaces disagree (refuse vs. downgrade).
  - verify: Upload a video as a member, have an admin set member override video_analysis=false (PUT /admin/api/members/{id}/access), then send the turn: expect a plain chat answer with the notice and no video meta, while GET /video/{conv}/{upload}/status now 403s.
- **S-03** [medium/confirmed_by_code] (rest) `orchestrator/app/sharing.py:317-328 (_is_shareable_message), :241-261 (evaluate provenance), :389-410 (build); orchestrator/app/authn/store.py:231-233,264-265 (usage counts)` — The backend has no notion of meta.send_state: a user message saved to history whose /chat request never happened is treated as a completed, shareable turn by sharing (role user + non-empty content) and as a sent message by workspace analytics, so a 'never sent' video turn can be published into a public snapshot (its private-ness then depends solely on whether meta.attachments survived the history PUT) and is counted as usage.
  - evidence: sharing.py:317-328 `_is_shareable_message` checks only role in (user, assistant), `status == 'streaming'` and non-empty content; evaluate() marks private only via `meta.route in PRIVATE_ROUTES`, `meta.get(key)` for PRIVATE_META_KEYS (incl. 'attachments', 'video') and the never-populated `has_attachment`; store.py:231 `count(*) FILTER (WHERE m.role = 'user') AS messages`. Repo-wide grep for `send_state` in orchestrator/app returns nothing.
  - verify: Save a turn with meta.send_state='uploading' and meta.attachments=[] (or a key other than 'attachments'), then GET /conversations/{id}/share and POST it public: the message appears in the snapshot; check /admin/api/analytics counts it.
- **T-05** [medium/confirmed_by_code] (tests) `orchestrator/app/uploads.py:503-511, 614-618; orchestrator/app/video/api.py:83-86; frontend/lib/uploadDocument.ts:97-102` — For a video, complete hashes and copies the whole file before answering, so it is the slowest request on the tunnel; if the acknowledgement is lost, a repeated complete is a 404 (marker unlinked) and the client, which has no retry, marks the turn failed although the server holds the file and the analysis is running. Neither repeated complete nor a slow complete is tested.
  - evidence: uploads.py:614 `os.unlink(os.path.join(root, _MARKER))` before `_finalise_video` (618); `_chunk_state` 509-511 raises 404 when the marker is absent; video/api.py:83 `content_hash = await asyncio.to_thread(store.hash_file, raw_path)` then 86 adopt_source; uploadDocument.ts:97-102 single POST complete, `jsonOrThrow` → throw. No test in test_document_uploads.py posts complete twice or measures complete latency.
  - verify: After a successful complete, POST complete again → 404 'upload not found'; time complete for a ~400 MB fixture through the public hostname (100 s edge timeout, memory note).
- **T-06** [medium/confirmed_by_code] (tests) `orchestrator/tests/test_live_generation.py:39-60, 81-86; orchestrator/app/main.py:242-278, 358-366` — The test pins 'a subscriber is attached → the client persists; server must not', but nothing covers the attached browser disconnecting after `done` and before its history write, nor subscriber decrement on an abrupt disconnect — the answer can be lost.
  - evidence: test_live_generation.py:81-86 `gen2.subscribers = 1; await _finalize_generation(…); assert len(saved) == 1` (no server write); main.py:366 `and gen.subscribers == 0`; follow() decrements in `finally` (277-278) — tested only for a clean reader exit (test_live_generation.py:52-58 with `asyncio.sleep(0.01)`); no test cancels the reader task mid-stream and asserts persistence.
  - verify: Attach a follow() reader, publish done, cancel the reader before it persists, run _finalize_generation → assert db.add_message called (currently it is not).
- **T-07** [medium/confirmed_by_code] (tests) `frontend/tests/video-attachment.test.tsx:92-118; frontend/components/Composer.tsx:499-510; frontend/components/ChatApp.tsx:1216-1227` — The early-upload path (which is what starts a video's analysis at attach time) is untested: the video test's assertion `forms.length === 1` cannot distinguish early from on-send, and its fresh ChatApp has no conversation id so withEarlyUpload never runs; a swallowed early failure silently causes a second full upload.
  - evidence: Composer.tsx:500 `if (!uploadConversationId || !att.file || att.base64) return att;` and 504-508 `uploadDocumentFile(…).catch(() => null)`; ChatApp.tsx:1216-1227 `const early = doc.uploadPromise ? await doc.uploadPromise : null; … if (!ref) { … ref = await uploadDocumentFile(src, conversationId, doc.kind === 'video' ? 'video' : 'document'); }`; video-attachment.test.tsx:103 `expect(forms.length).toBe(1)`.
  - verify: Seed an existing conversation (window.history ?c= + store.get returning it), attach a video, assert /api/upload was called BEFORE Send; then make the early call reject and assert a second purpose=video upload on Send.
- **T-08** [medium/confirmed_by_code] (tests) `frontend/tests/_wireHarness.tsx:37-74, 117-143, 156-173` — No frontend test reloads with a turn saved as send_state:'uploading' and asserts what appears, and the harness cannot express it: mockHistory's load()/get() return null, /api/upload resolves immediately, there are no /api/upload/chunked/* or controllable /api/chat/active stubs (catch-all returns {} → fetchServerActive → []).
  - evidence: _wireHarness.tsx:44-59 `get: () => null … load: async () => null`; 125-140 `/api/upload` returns synchronously-resolved json; 141 catch-all `{ ok: true, status: 200, json: async () => ({}) }`; a 400 MB file takes uploadChunked (uploadDocument.ts:113) which the harness cannot serve (upload_id would be ''). unsent-turn.test.tsx tests MessageRow in isolation with hand-built meta.
  - verify: grep the harness for 'chunked' and 'chat/active' — none; try a File with size > CHUNK_THRESHOLD_BYTES through attach()+send() and watch the body carry upload_id ''.
- **T-09** [medium/confirmed_by_code] (tests) `orchestrator/tests/test_video_understanding.py:858-874; orchestrator/app/video/api.py:67-116, 154-170; orchestrator/app/uploads.py:217-235, 617-618; frontend/tests/video-attachment.test.tsx:57` — The POST /uploads purpose=video success path, its response shape, the chunked purpose=video complete, the _finalise_video cleanup-on-HTTPException branch and GET /video/{conv}/{upload}/status (including cross-user access) have no tests; the frontend stub's `video:{analysis_id,status}` shape is pinned nowhere, and no frontend code polls status, so progress exists only as chat-stream step events.
  - evidence: test_video_understanding.py:858-874 asserts only 403 and 404; grep of orchestrator/tests for '/video' outside that file → nothing; grep of frontend/components and lib for 'video/.*status|/api/video' → nothing; video/api.py:154 `@router.get("/{conversation_id}/{upload_id}/status")`.
  - verify: Add login_client('alice') POST /uploads purpose=video with settings.video_analysis_enabled=True and a stubbed pipeline.ensure_running; assert the JSON shape; GET status as bob → expect 404.
- **T-10** [medium/confirmed_by_code] (tests) `orchestrator/tests/test_live_generation.py:136-160; orchestrator/app/main.py:1025-1035; frontend/tests/unchanged-edit.test.tsx:159-183` — A second POST /chat for the same conversation cancels the first generation (which then persists nothing), yet the only test hand-simulates the cancel instead of posting twice, and no frontend test double-clicks Send on a real turn; two tabs attaching is covered only on the history dedupe side.
  - evidence: test_live_generation.py:151-158 comment 'Simulate what POST /chat does on a same-conversation resend' then `previous.task.cancel()` by hand; main.py:1025-1035 `previous = _live_generations.get(conv_key_outer) … previous.task.cancel()`; test_live_generation.py:88-93 cancelled → no persist; test_conversation_integrity.py:334-354, 434-464 cover generation_id dedupe only. unchanged-edit.test.tsx:175-179 clicks Send five times only on the 'missing file' error path.
  - verify: POST /chat twice concurrently (TestClient in threads) with a slow fake stream; assert the first response ends without done and history holds exactly one answer.
- **ORCH-05** [medium/confirmed_by_code] (be-chat) `orchestrator/app/main.py:344-346 vs 366-373; 2467-2469 (/chat/attach 404)` — In the detached path the generation is marked done and removed from the registry BEFORE the answer is written to the database, so a reload during the write finds nothing active, attach 404s, and history has no answer — the client renders the bare user turn and never re-checks. The window is the db.add_message round trip, which widens under the known Postgres pool exhaustion.
  - evidence: `await gen.finish()` then `if _live_generations.get(conv_key) is gen: _live_generations.pop(conv_key, None)` then later `stored = await db.run_in_thread(db.add_message, ...)`. `chat_attach`: `if gen is None or gen.done or not _owns(gen, viewer): raise HTTPException(404)`.
  - verify: Monkeypatch db.add_message with a 5 s sleep, finish a detached generation, and hit /chat/active + /history during the sleep.
- **ORCH-08** [medium/confirmed_by_code] (be-chat) `orchestrator/app/video/pipeline.py:237-256 (ensure_running), 359-379 (_run attempt+1, no cap); orchestrator/app/main.py:718-745 (_resolve_video_refs returns every conversation video when no refs), 1657-1660; orchestrator/app/config.py:257 (VIDEO_MAX_CONCURRENT_JOBS=1)` — A 'failed' analysis is re-run from scratch every time a turn references it: ensure_running restarts any row whose status is not 'done', _run only increments `attempt`, and a video-routed follow-up with no explicit refs waits on ALL videos in the conversation. With one job slot, a permanently failing video blocks every other user's analysis for the full stage timeouts on every such turn.
  - evidence: `if row['status'] == 'done': ...return False` then unconditionally `create_task(_run(analysis_id))`. `_resolve_video_refs`: `if not refs: return list(known), None`; run_video_engine: `for row in videos: rows.append(await _wait_for_analysis(row, emit))`.
  - verify: Attach a corrupt MP4 (probe fails), then ask 'what did they say?' three times; watch video_jobs_total and the attempt column climb.
- **F-03** [medium/confirmed_by_code] (be-uploads) `orchestrator/app/uploads.py:126-131, 562-574, 596-608` — Blocking filesystem work runs directly on the asyncio event loop in three upload handlers, stalling every in-flight SSE stream (and the 15 s heartbeat invariant) for the duration: the whole-workspace os.walk + rmtree sweep at the top of every single-shot upload, the per-chunk sync writes in PUT part, and the full-file concatenation + rmtree in POST complete.
  - evidence: create_upload is `async def` and calls enforce_quota_and_ttl() directly (uploads.py:126-131), which os.walk's every upload of every user to sum sizes (repo.py:96-100, 112-118). chunked_part writes with sync out.write inside `async for chunk in request.stream()` (562-574). chunked_complete reads and writes the entire file synchronously (596-606) and rmtree's the parts (607). Contrast attach_upload, which correctly uses asyncio.to_thread for hashing and adoption (api.py:79, 82).
  - verify: py-spy dump the orchestrator (needs SYS_PTRACE) during a /complete of a large file, or watch an open SSE chat stream's heartbeat gaps while another user completes a 1 GB upload.
- **F-04** [medium/confirmed_by_code] (be-uploads) `orchestrator/app/video/api.py:73-78; orchestrator/app/uploads.py:553-570; .env:348; orchestrator/app/config.py:252` — The 4 GB video cap is enforced only after the entire file has been written to disk twice (parts + concatenated original); the per-part rail enforces only UPLOAD_MAX_MB, which is 100 GB in production, so an oversized video consumes 2x its size of disk and minutes of upload before a 413 that then rmtree's everything — and pushes the uploads tree toward the 20 GB quota sweep in F-01.
  - evidence: chunked_part checks `already + written > cap_total` with cap_total = settings.upload_max_mb (uploads.py:553-565; .env:348 sets 102400 MB). attach_upload checks `size > settings.video_max_upload_mb*1024*1024` only after complete has concatenated the file (api.py:73-78); _finalise_video then rmtree's the root on that HTTPException (uploads.py:229-231). chunked_init records only filename and purpose in the marker (531-536) — no declared size to reject early.
  - verify: Init a chunked video upload and stream 4.1 GB of parts: every part returns 200, complete returns 413, and du on /data/workspaces/uploads peaks at ~8.2 GB in the meantime.
- **F-05** [medium/confirmed_by_code] (be-uploads) `orchestrator/app/db.py:5077-5099, 5125-5141; orchestrator/app/main.py:729-744; frontend/components/Composer.tsx:504-508; frontend/components/ChatApp.tsx:1226-1247` — Attaching the same video bytes twice in one conversation silently re-points the single link row at the NEW upload_id, so any earlier upload_id already persisted in a message's meta.attachments stops resolving: a later send that references it fails with 'X is not attached to this conversation — please re-attach it.'
  - evidence: link_video_attachment: `ON CONFLICT (conversation_id, analysis_id) DO UPDATE SET upload_id = EXCLUDED.upload_id` (db.py:5089-5093) on UNIQUE (conversation_id, analysis_id) (db.py:1503-1512). get_video_by_upload matches `l.upload_id = %s` only (db.py:5130-5133). _resolve_video_refs returns the re-attach error when that lookup is None (main.py:739-744). The double upload is a normal path: withEarlyUpload swallows the early error (`.catch(() => null)`, Composer.tsx:508) and the send path re-uploads when `early` is null (ChatApp.tsx:1228-1240); and the client links an id only `if (entry && !entry.id)` (ChatApp.tsx:1244-1247), so a message that already carries the OLD id keeps it.
  - verify: Attach clip.mp4, wait for upload id A to be persisted (meta.attachments[i].id), attach the same clip again (id B), then POST /chat with video_uploads=[{upload_id: A}] — the response is the re-attach error; SELECT upload_id FROM video_attachments for the conversation shows only B.
- **F-06** [medium/confirmed_by_code] (be-uploads) `orchestrator/app/history.py:181-224; orchestrator/app/db.py:3651-3734, 3737-3822; frontend/lib/history.ts:892-951` — PUT /history/conversations/{id}/messages is last-writer-wins for message content and meta (only feedback is carried by position) and deletes+re-inserts every row with new ids; a stale tab whose local copy still carries send_state='uploading' (or lacks a linked attachment id / a generation_id) can overwrite newer server rows of the same length, drop the generation_id dedupe key, and — because add_message takes no row lock — a concurrent detached persist can interleave so the assistant row sorts before the re-inserted user row.
  - evidence: replace_messages locks the conversation FOR UPDATE (db.py:3672-3676), refuses only a SHORTER thread (3677-3684), snapshots feedback by position (3686-3692), DELETEs all rows (3693) and re-inserts the client's copy with the client's meta and generation_id (3694-3722); meta is never merged. The client issues this PUT whenever an already-pushed message's payload diverges, including a meta-only change (history.ts:936-951 'payload diverged: rewrite the thread'), which is exactly what send_state add/remove and attachment-id linking do (ChatApp.tsx:1190, 1246, 1315). add_message does a plain SELECT (no FOR UPDATE) then INSERT (db.py:3752-3778), so under READ COMMITTED its row is neither seen nor de
  - verify: Open the same conversation in two tabs; in tab A attach and send (send_state cleared), in tab B (stale, still holding send_state='uploading') toggle anything that dirties the thread; GET the conversation: the user message carries send_state again. For the race: run a PUT replace while _finalize_gene
- **F-10** [medium/confirmed_by_code] (be-uploads) `orchestrator/app/main.py:2398-2410; orchestrator/app/video/pipeline.py:174, 728-741; orchestrator/app/video/api.py:118-147` — 'Nothing running' from GET /chat/active only means no in-memory LiveGeneration in this process: after an orchestrator restart the list is empty while requeued video analyses run detached, and pipeline._latest is empty so status_payload shows the row's last durable stage with no percent — so a reload cannot distinguish 'no stream in this tab', 'chat generation died with the process', and 'analysis still running'.
  - evidence: /chat/active enumerates `_live_generations` (main.py:2405-2409), a process dict. pipeline.start requeues rows left 'running' and starts a maintenance loop that drains them (pipeline.py:728-741, 753-764) without any LiveGeneration. status_payload overlays `pipeline._latest.get(id)` (api.py:120) which is process memory (pipeline.py:174).
  - verify: Start a video turn, `docker restart` the orchestrator, reload the tab: /chat/active returns [], /video/{conv}/{upload}/status returns status='queued'/'running' with no percent, and the turn shows whatever the frontend infers from an empty active list.
- **F4** [medium/confirmed_by_code] (fe-attach) `frontend/components/ChatApp.tsx:1214-1245,1286-1309` — When one upload in a multi-file turn is refused, siblings keep uploading in the background but any that finishes after the rejection is never linked: its id is lost, its card is removed from history, and for a video the analysis the server started is orphaned.
  - evidence: `const refs = await Promise.all(uploadable.map(async (doc, i) => {...}))` (1214) rejects on the first throw. The catch immediately runs `const landed = userMessage.meta.attachments.filter((a) => a.id); if (landed.length) userMessage.meta.attachments = landed; else delete userMessage.meta.attachments;` (1302-1305) and persists send_state:'failed' (1306-1307). A sibling that resolves later executes `const entry = userMessage.meta?.attachments?.[i]` (1238) against the filtered/deleted array, so `entry` is undefined (or an already-linked entry) and `entry.id = ref.upload_id` never runs. No toast reports the late success; the server has the bytes and (purpose=video) is running its analysis.
  - verify: Attach a valid 100 MB video and a file the server refuses (e.g. exceeding VIDEO_MAX_UPLOAD_MB); Send. After the error toast, wait for the first upload to finish: meta.attachments on the persisted turn has no entry for it, GET /api/uploads/<conv> lists it.
- **F5** [medium/confirmed_by_code] (fe-attach) `frontend/components/ChatApp.tsx:149-158,1302-1307; frontend/components/MessageRow.tsx:867,882-883` — A turn persisted with send_state:'failed' is shown after reload as 'never sent — the page closed while its files were still uploading', the refused file is missing from the message, and (for documents) 'Send now' silently re-asks with fewer files.
  - evidence: unsentTurnState reads `m.meta?.send_state` truthy for both 'uploading' and 'failed' and derives `missing` only from remaining attachments (149-158); the catch path removes the refused attachment from meta before persisting (1302-1305). MessageRow's only two sentences are 'the page closed while its files were still uploading' / 'while X was still uploading' (882-883) — the real cause (413, quota, unsupported, network) is never stated. In-tab the failed row says 'Dataset upload failed — nothing was sent to the model.' for a video (867).
  - verify: Attach two PDFs where one is refused server-side, Send, reload: notice says 'page closed while its files were still uploading' with a Send now button that sends only the surviving pdf_uploads.
- **F6** [medium/confirmed_by_code] (fe-attach) `frontend/components/Composer.tsx:504-508; frontend/lib/uploadDocument.ts:72-103,69,102; frontend/components/ChatApp.tsx:1216-1231` — An early-upload failure is silently converted to null and the whole file is uploaded again from byte 0 at send time under a new upload id; a single transient part failure of a 400 MB chunked video therefore doubles the transfer, leaves the first chunked session's parts on the server, and tells the user nothing.
  - evidence: `uploadDocumentFile(...).catch(() => null)` (Composer.tsx:504-508) with no toast/console. uploadChunked has no retry: each part is `await jsonOrThrow(res, 'part i of n failed')` (uploadDocument.ts:90-95) and any throw abandons the init'd upload_id. send() does `if (!ref) { ... ref = await uploadDocumentFile(src, conversationId, ...) }` (ChatApp.tsx:1218-1231). Additionally uploadSingle/uploadChunked coerce a missing upload_id to '' (`String(body.upload_id ?? '')`, lines 69/102) which then travels in video_uploads.
  - verify: Attach a >90 MB video with the orchestrator stopped for 10 s during part 1, restart it, press Send: a new /api/upload/chunked/init and parts 0..n appear; no error was shown before Send.
- **F7** [medium/confirmed_by_code] (fe-attach) `frontend/components/Composer.tsx:513-524,730-742,850-858; orchestrator/app/video/api.py:66-100` — A video upload (and its server-side analysis) starts before the composer's cap check and cannot be cancelled: a chip that is refused by the MAX_DOCS cap or removed by the user still uploads to completion and queues an analysis.
  - evidence: appendDocument calls `const att = withEarlyUpload(input)` (514) BEFORE the `>= MAX_DOCS` refusal inside setAttachments; the remove button only filters state (`prev.filter((a) => a.clientId !== attachment.clientId)`, 854-858); uploadDocument.ts creates no AbortController. acceptFiles' busy guard (730) covers only send-time uploads. attach_upload runs `pipeline.ensure_running(row['id'])` on every upload (video/api.py:98); content-hash dedupe limits repeats of the same file only.
  - verify: Attach six different mp4s in an existing chat: six /api/upload requests go out although only five chips show; remove all chips — the uploads and analyses continue (server log 'analysis started').
- **F9** [medium/confirmed_by_code] (fe-attach) `frontend/components/ChatApp.tsx:1183-1193; frontend/components/MessageRow.tsx:845-860; frontend/lib/uploadDocument.ts:58-103` — During a large upload send the UI offers neither Stop nor byte progress, so reloading is the only way out — which is exactly what manufactures the unsent turn.
  - evidence: The upload branch sets datasetUpload 'uploading' but not streaming (1183-1193) so the composer shows a disabled Send and no Stop; MessageRow renders an indeterminate Loader 'Uploading video…' with the comment '/api/upload reports no byte progress' (845-860); uploadDocument.ts uses fetch (no upload progress events) and no AbortSignal.
  - verify: Attach a 400 MB video and Send: no Stop button, no percentage, Esc does nothing.
- **F9** [medium/confirmed_by_code] (fe-chat) `frontend/components/MessageRow.tsx:873-892; frontend/components/ChatApp.tsx:1290-1310 (send_state 'failed'), 632-636 (datasetUpload cleared on switch)` — send_state has two values ('uploading' | 'failed') but the notice has one story: 'the page closed while … still uploading'. A refused upload (413 past the Cloudflare edge, quota, 415, a failed chunk part, a 5xx/524 on /complete) becomes 'page closed' as soon as the in-tab datasetUpload marker is gone (switching away and back, or a reload). The in-tab failure indicator itself says 'Dataset upload failed' for videos.
  - evidence: MessageRow renders `unsent.canResend ? 'This message was never sent — the page closed while its files were still uploading.' : \`… the page closed while ${listNames(unsent.missing)} … still uploading …\`` with no branch on send_state; ChatApp writes `send_state: 'failed'` in the catch; `setDatasetUpload((prev) => prev && prev.conversationId === activeId ? prev : null)` drops the indicator on switch. `Dataset upload failed — nothing was sent to the model.` at 867 is shown for any kind.
- **F10** [medium/confirmed_by_code] (fe-chat) `frontend/components/ChatApp.tsx:1064-1130 (isPdf = first.kind, meta gating), 1238 (entry lookup), 1094 (pdfName); frontend/components/Composer.tsx:512-522 (documents append after images)` — `isPdf`/`isDataset` are computed from attachments[0]; the Composer appends documents after existing images, so [image, pdf, pdf] or [image, video] has an image first. needsDocUpload is true, but `meta` is only populated when `isPdf || isDataset || quoted || videoAttachments.length > 0` and `attachments` only when `isPdf || isDataset || videoAttachments.length > 0`, and pdfName only when `isPdf || isDataset || first?.kind === 'video'`. For image+documents: no meta.attachments and no pdfName → upload ids are never linked (`attachments?.[i]` undefined), history shows no chips, unsentTurnState sees nothing missing, and regenerate/Send now re-asks WITHOUT the documents (attachmentsForResend finds neither docs nor pdfName, missing=false). For image+video: the chip is missing (pdfName undefined) though ids link.
  - evidence: `const first = attachments[0] ?? null; const isPdf = first?.kind === 'pdf';` … `pdfName: isPdf || isDataset || first?.kind === 'video' ? first?.name : undefined` … `meta: metaWithBranch(isPdf || isDataset || quoted || videoAttachments.length > 0 ? {…attachments…} : undefined, userBranch)`. Composer.appendDocument: `return [...kept, att];`.
  - verify: Attach a PNG, then two PDFs, send; reload: the turn shows no document chips; press Try again on the answer: the request carries no pdf_uploads.
- **F11** [medium/confirmed_by_code] (fe-chat) `frontend/components/Composer.tsx:499-509 and 479; frontend/components/ChatApp.tsx:2612 (uploadConversationId={activeId}), 1216-1220` — withEarlyUpload captures `uploadConversationId` at attach time and starts the upload (and the video analysis) under that conversation. The Composer never clears attachments when the conversation changes (only after a send), so a video attached in chat A and sent from chat B rides `doc.uploadPromise` (A's upload_id) into B's request: `const early = doc.uploadPromise ? await doc.uploadPromise : null; let ref = early;` is used as-is. B's turn references an upload keyed to A; whether the server rejects it or crosses conversations depends on the orchestrator's lookup.
  - evidence: `if (!uploadConversationId || !att.file || att.base64) return att; const conversationId = uploadConversationId; const uploadPromise = uploadDocumentFile(att.file, conversationId, …).catch(() => null); return { ...att, uploadPromise };` — grep shows no effect keyed on uploadConversationId and `setAttachments([])` only after onSend.
- **F15** [medium/confirmed_by_code] (fe-chat) `frontend/components/ChatApp.tsx:1467-1560 (runRegenerate), 1602-1700 (runEdit), 1776-1810 (retryLastTurn), 1312-1316 (clear); frontend/lib/branching.ts:58-64; frontend/lib/history.ts:889-925` — (a) Only the original send() deletes send_state; runRegenerate (the Send-now path), retryLastTurn and runEdit leave it on the message, and runEdit copies it into the new version via `metaWithBranch(original.meta, version)`. The marker therefore persists on answered turns and re-surfaces whenever the assistant row is removed (markUnreachable on UNAUTHENTICATED deletes the placeholder; truncate). (b) The clear is pushed as a whole-thread replaceMessages; if the orchestrator's own append lands first (fast answer, video already analysed), pushAll gets 409 and adopts server truth without re-applying the local change, so the server keeps 'uploading' on an answered turn; any later situation that leaves that user turn last shows 'never sent' and offers a resend of an already-answered request.
  - evidence: runRegenerate builds startStream from `resendOptionsFor(view[userIdx])` and never touches meta; `metaWithBranch(meta, branch) { if (!branch) return meta; return { ...(meta ?? {}), branch }; }`; pushAll: `if (isConflict(err)) { await loadConversation(conv.id, true); mutateSync(… dirty filter …); return; }`.
- **V4** [medium/confirmed_by_code] (be-video) `orchestrator/app/video/pipeline.py:198-204 (subscribe replays _latest), :215-217 (_latest never cleared per run); orchestrator/app/engines/video.py:258-259 (subscribe then ensure_running), :276-277 (break on the first '_done'), :312 (then blocks in wait_for)` — Re-attaching a video that failed (or was produced under an older pipeline version) earlier in this process's lifetime replays the previous run's terminal '_done' event to the new subscriber before the new run has published anything; the chat engine treats it as the end, forwards no step/status events for the entire re-run, and silently blocks in wait_for until the row is final. The final answer is correct; the live progress is not — consistent with 'one screenshot shows progress, another shows nothing'.
  - evidence: `last = _latest.get(int(analysis_id)); if last is not None: q.put_nowait(last)` with `_latest` written by every `_publish` including `_finish`'s `{"stage": _DONE, "status": status}` (pipeline.py:414). `ensure_running` creates the task without touching `_latest`; `_run` publishes its first event only after acquiring the semaphore (:360-373). In `_wait_for_analysis`: `if stage == "_done": break` then `fresh = await pipeline.wait_for(analysis_id)`, which `await asyncio.shield(task)`s the NEW task.
  - verify: With the process running, attach a corrupt MP4 (probe fails), then attach a valid MP4 with the same... simpler: make transcription fail once (stop whisper), restart whisper, re-attach: the chat turn shows no steps until the final answer. Compare with a fresh process where steps stream.
- **V5** [medium/confirmed_by_code] (be-video) `orchestrator/app/video/pipeline.py:359-373 (first publish only after `async with _semaphore()`); orchestrator/app/engines/video.py:267-272 (30-s timeout branch continues silently while is_running is True)` — A video queued behind another (VIDEO_MAX_CONCURRENT_JOBS=1) emits no event at all until the slot frees; the chat turn waiting on it shows nothing — no 'queued behind another video' step or status — for as long as the other job takes (an hour for a long meeting), kept alive only by SSE heartbeats.
  - evidence: `_run` does `async with _semaphore(): row = ...; _publish(...)`; nothing is published before the acquire. `_wait_for_analysis`: `except asyncio.TimeoutError: fresh = await pipeline.wait_for(analysis_id) if not pipeline.is_running(analysis_id) else None; if fresh is not None: return fresh; continue` — a task parked on the semaphore is 'running', so it just loops.
  - verify: Attach a 40-minute video in conversation A, then a short one in conversation B; B's turn shows no steps until A completes.
- **V6** [medium/confirmed_by_code] (be-video) `orchestrator/app/video/pipeline.py:361, :374-383, :401-404, :406-414 (DB writes outside the per-stage try), :255 (done-callback only pops _tasks), :753-757 (drain lists only 'queued'); orchestrator/app/engines/video.py:270-272, :542-556` — Any exception raised in _run outside a stage body (a DB error in get/update_video_analysis or in _finish) kills the task without a log line, without a '_done' event, and leaves the row at 'running' with no task; nothing re-drives it until the next restart (drain_queue only lists 'queued'). The chat engine's 30-s fallback then returns the stuck row, and on an attach turn run_video_engine builds `parts` only from done/failed rows and streams an EMPTY answer with no error.
  - evidence: `_Runner.stage` catches exceptions only around `_STAGE_FNS[stage](...)`; `await db.run_in_thread(db.update_video_analysis, ...)` at :344 and in `_finish` is outside that try. `task.add_done_callback(lambda t, aid=...: _tasks.pop(aid, None) ...)` never reads `t.exception()`; `wait_for` swallows it (`except Exception: pass`, :263-266). `run_video_engine`: `failed = [... == "failed"]; done = [... == "done"]; if attach_turn and is_placeholder(message): for r in done ...; for r in failed ...; text = "\n\n---\n\n".join(parts)` -> "" for a 'running' row.
  - verify: Run a job with a fault injected into db.update_video_analysis on the second call (raise once); confirm no log entry, row stays 'running', and the attach turn renders an empty assistant message.
- **V7** [medium/confirmed_by_code] (be-video) `orchestrator/app/video/pipeline.py:180-186 (per-process semaphore), :232-234 (per-process is_running), :735 (startup requeue), :779-786 (reap_orphans checks only local is_running before rmtree+delete); orchestrator/app/db.py:5147-5163 (requeue where lease_expires_at IS NULL or expired), :5166-5185 (claim/release lease, no production caller)` — There is no cross-process claim on a run. Commit 845408d (landed during this review) added lease columns and claim_video_lease/release_video_lease and made requeue skip live leases, but pipeline.py never claims, heartbeats or releases a lease, so lease_expires_at is always NULL and the requeue still flips every 'running' row to 'queued' unconditionally. A second orchestrator process against the same database (the candidate-container recipe, or any future --workers) steals a running job on startup; both processes then run _run on the same row, interleave whole-map `stages`/`counts` writes and write the same stage files; reap_orphans in one process can rmtree a directory the other is analysing.
  - evidence: `grep -rn claim_video_lease orchestrator/ --include=*.py | grep -v tests` returns only db.py:5166. Requeue SQL: `WHERE status = 'running' AND (lease_expires_at IS NULL OR lease_expires_at < %s)`. pipeline.py `_semaphore()` builds `asyncio.Semaphore(...)` per loop; `is_running` reads the module-level `_tasks`. Dockerfile:56 runs a single uvicorn process today, so the trigger is a second container, not workers.
  - verify: Start a second orchestrator container against the same Postgres while a job is running; its startup log prints 'requeued 1 video analysis(es)' and its maintenance drain starts the same analysis_id 5 s later.
- **V9** [medium/confirmed_by_code] (be-video) `orchestrator/app/db.py:5188-5213 (link_video_attachment ON CONFLICT (conversation_id, analysis_id) DO UPDATE SET upload_id = EXCLUDED.upload_id); orchestrator/app/main.py:739-742 (_resolve_video_refs by (conversation_id, upload_id))` — Attaching the same file a second time in the same conversation (the natural response to the 'never sent' notice, or an identical file in a two-video turn) overwrites the link's upload_id; the earlier turn's saved reference no longer resolves and 'Send now' on it answers '<name> is not attached to this conversation — please re-attach it.', a misleading message for a file that was uploaded and analysed successfully.
  - evidence: `ON CONFLICT (conversation_id, analysis_id) DO UPDATE SET upload_id = EXCLUDED.upload_id, filename = EXCLUDED.filename` keeps one link per (conversation, analysis); `get_video_by_upload` (db.py) matches `l.upload_id = %s`; `_resolve_video_refs` returns the 'not attached' error when it is None.
  - verify: Attach A.mp4 twice in one conversation; POST /chat with video_uploads=[first upload_id]; observe the 'not attached' reply while GET /video/{conv}/{first_id}/status also 404s.
- **INF-5** [medium/confirmed_by_code] (infra) `orchestrator/app/uploads.py:582-615; orchestrator/app/video/api.py:67-116; orchestrator/app/sse.py:99; docs/AUTH.md:302-311` — `chunked_complete` concatenates all parts synchronously on the asyncio event loop (blocking reads/writes of up to 4 GB), so for the duration every other user's SSE stream, heartbeat, /health probe and upload stalls; the response to `complete` is delayed by assembly plus a full sha256, which on the public path must land inside Cloudflare's ~100 s origin-response window (and undici's 300 s headers timeout in the proxy).
  - evidence: uploads.py:600-612: `with open(raw_path, "wb") as out: for name in parts: with open(...) as fh: while True: chunk = fh.read(_CHUNK) ... out.write(chunk)` — no `await`, no `asyncio.to_thread`. video/api.py:83 `content_hash = await asyncio.to_thread(store.hash_file, raw_path)` (hashing IS offloaded, assembly is not). sse.py:99 `HEARTBEAT_SECONDS = float(os.environ.get("SSE_HEARTBEAT_SECONDS", "15"))` cannot fire while the loop is blocked. docs/AUTH.md:311 relies on the heartbeat to stop Cloudflare timing out long streams. The frontend proxy awaits `upstream.text()` for the whole duration (chunked route.ts:53).
  - verify: Upload a 4 GB video and watch another tab's SSE stream and `docker inspect --format '{{json .State.Health}}'` during `complete`; the heartbeat gap equals the assembly time. Fix: run the assembly in `asyncio.to_thread` (or use `os.sendfile`/`shutil.copyfileobj` in a thread), and answer `complete` wit
- **INF-6** [medium/confirmed_by_code] (infra) `orchestrator/app/video/api.py:38,154-166; frontend/app/api (no video route); scripts/video_smoke.py:19-20; orchestrator/app/main.py:2398-2410` — The only endpoint that reports a running video analysis to a reloaded tab, GET /video/{conversation}/{upload}/status, has no Next.js route-handler proxy, and the orchestrator is not reachable from browsers (not mapped by the tunnel, CORS-restricted). After a reload with the uploads finished but no chat request ever sent, /chat/active is empty (it lists LiveGenerations only) and the analysis status is unreachable, so the UI cannot show 'analysis in progress' — one screenshot shows progress (a LiveGeneration exists) and the other the notice (none exists).
  - evidence: video/api.py:38 `router = APIRouter(prefix="/video", ...)`; 154-166 `@router.get("/{conversation_id}/{upload_id}/status")` owner-checked. `ls frontend/app/api` = admin audio auth chat conversations debug history public reports upload uploads — no `video`. video_smoke.py:19-20: 'The status route has no proxy, so that step is skipped there.' main.py:2398-2410 `chat_active` returns keys of `_live_generations` only. compose.cloudflare.yaml:179-184: only frontend:3000 is mapped.
  - verify: From a browser session, fetch /api/video/<conv>/<upload>/status: 404 from Next. Fix: add the proxy route and have the reload path query it for turns whose attachments carry a video analysis_id.
- **INF-7** [medium/confirmed_by_code] (infra) `scripts/deploy.sh:118-122,313; git status (untracked BURN-IN-REPORT.md, scripts/burnin.sh)` — `land()` runs `git clean -qfd -e .env -e .runtime`, which deletes every untracked, non-ignored file and directory in the production checkout — which is also the owner's working directory — while the dirty-tree gate only inspects tracked files, so untracked work is silently destroyed rather than refused. Right now BURN-IN-REPORT.md and scripts/burnin.sh would be deleted by the next deploy.
  - evidence: deploy.sh:118 `DIRTY="$(git -C "$ROOT" status --porcelain --untracked-files=no)"`; 313 `git -C "$ROOT" clean -qfd -e .env -e .runtime || true`. Header line 27-28 promises 'A dirty production tree aborts the deploy rather than discarding someone's work in progress.' Current `git status` shows `?? BURN-IN-REPORT.md` and `?? scripts/burnin.sh`.
  - verify: `git -C <root> clean -nd -e .env -e .runtime` lists what the next deploy will remove. Fix: drop `-d`/the clean entirely, or include untracked files in the refusal (status --porcelain without --untracked-files=no) with an explicit override.
- **INF-8** [medium/confirmed_by_code] (infra) `orchestrator/app/uploads.py:511-543,562-580,591-598; orchestrator/app/video/api.py:76-82; .env (UPLOAD_MAX_MB=102400)` — The chunked rail accepts truncated parts and cannot detect a short file: init records neither total size nor part count, a client disconnect mid-part raises ClientDisconnect and leaves the partial part file in place, and `complete` validates only that part names are contiguous. Separately, the 4 GB video cap is applied only after the whole file is assembled, while the per-upload cap on the chunked rail is the live UPLOAD_MAX_MB=100 GB (bounded only by _MAX_PARTS x 90 MiB = 11.25 GB), so oversize videos are fully written to /data and assembled before being refused.
  - evidence: uploads.py:531-538 marker holds only `{"filename", "purpose"}`. 563-580: `with open(dest, "wb") as out: async for chunk in request.stream(): ... out.write(chunk)` — Starlette raises ClientDisconnect out of `stream()` on `http.disconnect`, the `with` closes the file, and no `os.unlink(dest)` runs on that path (only on the 413 path, 568-570). 591-598: `expected = [f"{i:05d}" for i in range(len(parts))]; if parts != expected: raise 400` — size never checked. 557 `cap_total = settings.upload_max_mb * 1024 * 1024` with live UPLOAD_MAX_MB=102400. video/api.py:77-82 `if size > cap: raise HTTPException(413 ...)` runs after assembly (uploads.py:600-613).
  - verify: Abort a part PUT mid-body and call complete: 200 and a corrupt file reaches ffprobe. Fix: record declared size/part size at init, require each part's length to match, refuse `complete` unless the sum equals the declared size, unlink the partial part on ClientDisconnect, and enforce VIDEO_MAX_UPLOAD_
- **INF-10** [medium/confirmed_by_code] (infra) `launcher/techsara_cli/cli.py:1189-1198; frontend (no build-id handshake found by grep); .runtime/logs/deploy-20260909-224832.log:3,431; git log 1ded7fd` — There is no version handshake between the JavaScript bundle a tab loaded and the backend it talks to, and the launcher recreates the orchestrator before the frontend; so for the recreate+health window every open tab runs the previous bundle against the new backend, and tabs opened before a deploy keep the old bundle indefinitely. Commit 1ded7fd (meta.send_state + 'never sent' notice) reached production at 22:52 IST on 2026-09-09 after three earlier deploys that evening; any tab loaded before that renders the new server rows with code that does not know send_state.
  - evidence: cli.py:1190-1197 orchestrator up + wait_service before frontend up. grep for buildId/BUILD_ID/version-mismatch/reload-the-page across frontend/lib, frontend/app, frontend/components returned no handshake. Deploy log 22:48:32 `target =1ded7fd... fix(chat): a turn saved while its files upload can no longer pose as sent` ... 22:52:26 `DEPLOYED 1ded7fd...`; earlier deploys at 20:48:24, 21:31:02, 22:04:27 the same evening.
  - verify: Open a tab, deploy, and inspect the loaded chunk hashes vs `.next/BUILD_ID` in the new container; the tab keeps the old bundle until a hard reload. Fix: expose the build id via /api/auth/me or a response header and prompt a reload on mismatch.
- **S-02** [medium/plausible_risk] (rest) `orchestrator/app/config.py:257 (VIDEO_MAX_CONCURRENT_JOBS=1), :266 (VIDEO_STAGE_TIMEOUT_S=7200); orchestrator/app/video/api.py:154 (only route is GET .../status); docs/video-understanding/DECISIONS.md:40-56` — There is no user-facing way to cancel a detached video analysis (the only /video route is status; chat Stop deliberately does not stop the job), and the deployment runs one job at a time with a 2 h per-stage timeout, so one large or mistaken upload can hold every other user's video work for hours with no operator-visible lever.
  - evidence: config.py:257 `video_max_concurrent_jobs = _int('VIDEO_MAX_CONCURRENT_JOBS', 1)`; :266 `video_stage_timeout_s = 2*3600`; grep of video/api.py decorators shows a single `@router.get("/{conversation_id}/{upload_id}/status")`; DECISIONS.md §2: 'A new message in the same conversation cancels the running generation... So video/pipeline.py runs stages in its own task, started at UPLOAD time'.
  - verify: Video reader: confirm video/api.py has no DELETE/cancel route and pipeline has no admin abort; then upload a 4 h file from user A and a 12 s clip from user B and observe B's step events stay queued.
- **T-11** [medium/plausible_risk] (tests) `orchestrator/tests/test_conversation_integrity.py:68-96; frontend/tests/upload-id-persistence.test.ts:209-225; frontend/tests/history-server.test.ts:204-221` — A same-length PUT /messages is accepted unconditionally and the client re-pushes on any meta change, so a second tab whose snapshot still carries send_state:'uploading' can overwrite the server copy after the sending tab cleared it — nothing tests a stale-snapshot replace.
  - evidence: test_conversation_integrity.py:68-83 same-length replace → 200 with no version/etag check; upload-id-persistence.test.ts:209-225 'any meta change on a pushed message syncs' via replace; history-server.test.ts:204-221 diverged tail → rebuild. No test seeds two stores against one fake server with divergent meta.
  - verify: Two createServerHistoryStore instances on one makeServer: A saves turn with send_state, flushes; B loads; A deletes send_state and flushes; B re-saves its copy with a meta tweak and flushes → inspect server meta.send_state.
- **ORCH-06** [medium/plausible_risk] (be-chat) `orchestrator/app/main.py:2212-2229 (finally with suppress(Exception) around asyncio.shield), 2380-2388 (/chat/stop), 1030-1034 (replace), 105-111 (busy probe); orchestrator/app/video/pipeline.py:83-113 (pace)` — A second task.cancel() delivered while the worker's finally is awaiting the shielded usage record (from /chat/stop pressed again, or a new send in the same conversation — both allowed while gen.done is False) raises CancelledError at that await; `contextlib.suppress(Exception)` does not catch BaseException, so `_finalize_generation` never runs: the generation stays 'active' forever, /chat/attach replays its buffer and heartbeats indefinitely (a 'live progress' screenshot that never completes), and the busy probe reports chatting so every video pipeline unit paces up to VIDEO_PACE_MAX_WAIT_S (20 s) — until the next send to that conversation overwrites the registry entry.
  - evidence: `with contextlib.suppress(Exception): await asyncio.shield(_record_usage_event(...))` then `with contextlib.suppress(Exception): await asyncio.shield(_finalize_generation(conv_key_outer, gen))`. /chat/stop: `if gen is None or gen.done or gen.task is None: return ...; gen.task.cancel()` — no guard against re-cancel. Busy probe: `any(not g.done and not getattr(g, 'waiting_on_video', False) for g in list(_live_generations.values()))`. Also the pre-try lines 1132-1146 (`set_identity(...)`) run outside the try/finally, so an exception there leaves the same orphaned entry.
  - verify: Monkeypatch usage.record_async to sleep 2 s; POST /chat, POST /chat/stop twice 500 ms apart; observe /chat/active still listing the conversation and /chat/attach streaming only keep-alives.
- **ORCH-07** [medium/plausible_risk] (be-chat) `orchestrator/app/engines/video.py:249-313 (_wait_for_analysis), 504-539 (run_video_engine); orchestrator/app/video/pipeline.py:259-266 (wait_for), 359-373 (_run)` — If the pipeline task dies without reaching _finish (an exception in _run before/around the row update, e.g. a DB error at pipeline.py:361/373), no `_done` event is published; after the 30 s queue timeout the engine takes the still-'running' row as final, sees neither done nor failed, and the attach turn streams an empty answer with a normal meta/done — so gen.answer is '' and the server persists nothing, and the row stays 'running' until the next restart or referencing turn.
  - evidence: `fresh = await pipeline.wait_for(analysis_id) if not pipeline.is_running(analysis_id) else None; if fresh is not None: return fresh` — wait_for returns `db.get_video_analysis` regardless of status. In run_video_engine: `failed=[...=='failed']; done=[...=='done']`; with both empty and `attach_turn and is_placeholder(message)`, `parts=[]` → `text=''` → `_chunks('',400)==['']` → emits token '' and meta. _finalize_generation requires `gen.answer` truthy (main.py:352).
  - verify: Make db.update_video_analysis raise once inside _run; attach a video; the chat turn returns an empty assistant message after ~30 s.
- **ORCH-09** [medium/plausible_risk] (be-chat) `orchestrator/app/main.py:442 (mode default 'salesforce'), 1782-1794 (SF Intelligence gate), 1837-1848 (sf_intel.run), 1892 (video branch), 1921 (document branch)` — In Salesforce mode — the wire default — an attach turn with text (including the frontend's 'Analyze the attached video.' placeholder) is handed to the Salesforce planner before the video/document branch, because the gate excludes only inline pdf_data/image_data, not video_uploads/pdf_uploads; if the planner 'handles' it (answers from CRM or asks a clarification) the video engine never runs and the analysis is never reported on.
  - evidence: Gate: `request.mode == 'salesforce' and clarification_available and request.text and not (request.pdf_data or request.image_data) and not request.sf_live and github_ref is None and not repo_followup and not url_list and not dataset_ready and '(Clarified:' not in text`. Dispatch: `if sf_outcome is not None and sf_outcome.handled: answer = sf_outcome.answer elif request.video_uploads or video_followup: ...`.
  - verify: POST /chat with mode='salesforce', message='Analyze the attached video.', video_uploads=[...] while SALESFORCE_INTELLIGENCE_ENABLED or CLARIFY_BEFORE_ANSWERING is on; check meta.route.
- **ORCH-10** [medium/plausible_risk] (be-chat) `orchestrator/app/engines/video.py:73-82 (_ABOUT_VIDEO_RE), 146-163 (is_about_video); orchestrator/app/main.py:1657-1660, 1892` — Once any video is attached to a conversation, every later text turn containing common words (call, session, host, explain, application, the app, this one, said, show, slide, screen, summary...) is routed to the video engine, which re-waits every attached video (ORCH-08) and answers only from video evidence under a prompt that forbids anything else — unrelated questions get 'The video doesn't cover that.'
  - evidence: `if _ABOUT_VIDEO_RE.search(message or ''): return True` before the embedding-distance test; regex includes `\b(...|call|...|session|...|host|...|explain(?:s|ed)?|application|the app\b|...|this one)\b`. main.py: `video_followup = await video_engine.is_about_video(request.text, conversation_videos)` then `elif request.video_uploads or video_followup:`.
  - verify: In a conversation with one video, ask 'explain how a Python decorator works'; meta.route == 'video'.
- **F-07** [medium/plausible_risk] (be-uploads) `orchestrator/app/history.py:79-82, 159-174; orchestrator/app/db.py:3737-3778` — Appended messages have no client-supplied idempotency key (only assistant rows with meta.generation_id dedupe), so a retried POST after a network error that actually landed, or two tabs syncing the same turn, produce duplicate user rows; the client's next same-length PUT is then refused with 409 and the client adopts the duplicated server thread as truth.
  - evidence: MessageIn has role/content/meta only (history.py:79-82); add_message dedupes solely on (conversation_id, generation_id) (db.py:3765-3799), which user turns never carry. replace_messages raises MessageCountWouldShrink when the client's count is lower (db.py:3683-3684) -> 409 (history.py:207-215) -> history.ts:904-911 loadConversation(conv.id, true).
  - verify: POST the same user message twice to /history/conversations/{id}/messages; both return 200 with different ids. Check production for conversations with two consecutive identical user rows.
- **F-08** [medium/plausible_risk] (be-uploads) `orchestrator/app/uploads.py:544-575, 585-592; frontend/lib/uploadDocument.ts:84-95` — A part write is not atomic and has no expected size or checksum: a client disconnect or short write mid-part leaves a truncated file named as a valid index, concurrent PUTs of the same index interleave into one file, and complete accepts any contiguous index set — the client never compares `received` to the slice size and sends no part count — so a corrupt or short file can be assembled without any error.
  - evidence: chunked_part opens dest with 'wb' and writes as chunks arrive (uploads.py:562-574); only the 413 branch unlinks (565-566) — a ClientDisconnect raised by request.stream() propagates with the partial file left in place. chunked_complete's only validation is `parts == [f'{i:05d}' for i in range(len(parts))]` (585-592). The marker stores no size/part count (531-536). The client ignores the `received` value (uploadDocument.ts:93) and complete carries no body (97-101).
  - verify: PUT part/0 with a Content-Length larger than the bytes sent and drop the connection; observe _parts/00000 remains; then PUT part/1 and POST complete -> 200 with a short file. Also PUT the same index from two connections concurrently and diff the result.
- **F-09** [medium/plausible_risk] (be-uploads) `orchestrator/app/main.py:358-405; orchestrator/app/db.py:3737-3822` — The server persists the final answer only when NO subscriber was attached at finish; when the browser received the terminal events but closed/lost the tab before its history sync pushed the assistant message, the answer exists nowhere but the browser's local cache (if that write happened).
  - evidence: _finalize_generation stores via db.add_message only if `gen.subscribers == 0` (main.py:363-370); otherwise it only logs at debug (395-405). The dedupe key exists (db.py:146-147, 3765-3799) so an always-persist would be safe, but is not done.
  - verify: Attach a tab, let an answer stream to completion, kill the tab process (not a graceful close) within the sync window, then GET the conversation from another device: the assistant row is missing. Requires the main.py/frontend readers to confirm when subscribers is decremented relative to finish().
- **F8** [medium/plausible_risk] (fe-attach) `frontend/components/ChatApp.tsx:1238-1243,1313-1318; frontend/lib/history.ts:926-990,1072-1079,904-910` — The marker removal is a queued whole-thread replace behind earlier per-file replaces while the chat request is already out; a reload before the queue flushes, or a 409 on that replace, leaves send_state:'uploading' on a turn that WAS sent, producing the red notice on a conversation that is (or was) generating.
  - evidence: Each `entry.id = ...; persist(...)` (1238-1243) and `delete userMessage.meta.send_state; persist(...)` (1314-1315) enqueue syncConversation; because syncKey hashes meta, a same-length thread with changed payload goes through pushAll -> api.replaceMessages (history.ts:945-955). enqueue chains per conversation (1072-1079) so the marker-clearing replace waits for prior network round trips, while `void startStream(...)` (1318) is not gated on it. In pushAll, `if (isConflict(err)) { await loadConversation(conv.id, true); s.dirty = s.dirty.filter(...); return; }` (904-910) adopts the server copy — which still carries the marker — and drops the local deletion. After reload the notice shows until at
  - verify: Throttle the network, send a two-video turn, reload within ~1 s of the 'Uploaded 2 files' toast; server history shows the user turn with send_state:'uploading' and /chat/active lists the conversation.
- **F12** [medium/plausible_risk] (fe-chat) `frontend/components/Composer.tsx:504-508; frontend/components/ChatApp.tsx:1216-1236; frontend/lib/uploadDocument.ts:72-103` — An early upload that fails resolves to null and send() re-uploads the whole file. If the failure was on the final `complete` call after the server assembled the parts (a proxy 502, a Cloudflare 524 on a slow assembly), the server already holds the video and has started its analysis; the resend uploads the 400 MB again and starts a second analysis. The client sends no content hash or part count that would let the server dedupe.
  - evidence: `.catch(() => null)`; `if (!ref) { … ref = await uploadDocumentFile(src, conversationId, …) }`; uploadChunked posts init/parts/complete with only conversation_id, filename, purpose.
- **F13** [medium/plausible_risk] (fe-chat) `frontend/lib/streams.ts:487-491 (consume), 286-296 (finalize)` — If the SSE body closes cleanly without 'done'/'error' (a proxy or edge closing the response, a graceful orchestrator restart), consume() calls finalize({status:'done'}) and the partial text is appended to history as a completed assistant message with no server generation_id. The detached generation may still be running; when it finishes with no subscriber the server persists the full answer too, yielding a partial 'done' row followed by the real one.
  - evidence: `if (!sawTerminal) { // Stream ended without a terminal event — treat as complete. settleReasoningClock(s); finalize(s, { status: 'done' }); }`.
- **F14** [medium/plausible_risk] (fe-chat) `frontend/lib/streams.ts:627-664; frontend/components/ChatApp.tsx:1467-1472; orchestrator/app/main.py:1025-1032` — POST /api/chat carries no idempotency key (only clarification answers have one). A double click on Send now, or Send now in a second tab, produces a second POST that the orchestrator honours by cancelling the first generation; the first tab's stream ends and persists a partial/error row.
  - evidence: Body fields: messages, current_text, session_id, conversation_id, mode, … no request id; server: `previous.task.cancel()` for the owner's newer request.
- **F16** [medium/plausible_risk] (fe-chat) `frontend/components/ChatApp.tsx:1238-1243; frontend/components/Composer.tsx:499-509` — The per-file id link is written to the cache synchronously but reaches the server through an async whole-thread push; a reload in that window leaves the server with no id for a file it holds (and has analysed), so the notice tells the user to attach it again and a re-attach uploads and analyses it a second time. An early upload that completes before Send exists only in Composer state — a reload before Send orphans the upload and its analysis with nothing in history pointing at it.
  - evidence: `entry.id = ref.upload_id; persist(conversationId, turns);` → saveMessages → enqueue(syncConversation) → replaceMessages (network). withEarlyUpload stores the reference only on the Attachment object.
- **F18** [medium/plausible_risk] (fe-chat) `frontend/components/ChatApp.tsx:632-636, 1180-1200, 1318-1340; frontend/lib/streams.ts:552-575 (register)` — Switching conversations during an upload clears datasetUpload (composer no longer busy) and, on return, the row shows the never-sent notice for an upload that is still running. If the user types another turn meanwhile, the upload's completion calls startStream({turns}) with the array captured at send time; register() sets s.messages = [...turns, placeholder] and notifies, so the newer turn disappears from the screen and finalize saves that list over the cache (then 409s against the server, adopting whatever it has).
  - evidence: `setDatasetUpload((prev) => prev && prev.conversationId === activeId ? prev : null)`; `const turns = [...messagesRef.current, userMessage];` captured before the await; `s.messages = [...turns, {…placeholder}]; streams.set(conversationId, s); notifyNow(conversationId);` and finalize `saveMessages(s.conversationId, s.messages)`.
- **V10** [medium/plausible_risk] (be-video) `orchestrator/app/video/media.py:316-334 (select filter, no count cap); orchestrator/app/video/pipeline.py:536-566 (all candidates extracted before select/unlink), :552 (timeout scales with duration); orchestrator/app/video/frames.py:157 (hash every candidate in one thread)` — For videos under VIDEO_KEYFRAMES_ONLY_AFTER_S (20 min) every decoded frame is tested against gt(scene,0.30) with no ceiling on selected frames; a screen recording with constant scrolling/flicker or a fast-cut clip can select thousands of 1280-px JPEGs (hundreds of MB) that all land on disk and are all pHashed before the cap of 400 is applied. There is no per-analysis or per-directory byte budget in store.py or the pipeline (VIDEO_DATA_DIR is outside the workspace sweep by design).
  - evidence: `select='gt(scene,T)+isnan(prev_selected_t)+gte(t-prev_selected_t,FLOOR)'` and `-f image2 pattern` with `-fps_mode vfr`; `extracted = await media.extract_frames(...)` then `kept, report = await asyncio.to_thread(fr.select, extracted, ...)` then unlink of the non-kept. `frame_cap` bounds only what is KEPT (frames.py:121-126).
  - verify: Attach a 15-minute 30-fps screen recording of continuous scrolling; watch `du -sh /data/video/<hash>/frames` during the frames stage and the extracted count in the stage detail.
- **V21** [medium/plausible_risk] (be-video) `orchestrator/app/video/pipeline.py:743-749 (stop cancels every job task), :326-330 (CancelledError leaves the row 'running'), :735 (requeue at next start); orchestrator/app/main.py:100-115 (lifespan)` — Every orchestrator restart (routine deploys restart it) cancels running analyses; the job itself resumes at the interrupted stage after startup, but the in-memory chat turn that was subscribed to it dies with the old process, so from the user's side the answer never arrives and the progress it was showing stops without a terminal event — the analysis then completes with nobody listening.
  - evidence: `for task in list(_tasks.values()): task.cancel()`; `_wait_for_analysis` and LiveGeneration live in the dying process; V29 (845408d) added chat_requests 'interrupted' bookkeeping in db.py but nothing in main.py or the pipeline consumes it yet (grep for claim_video_lease/lease_owner in main.py and pipeline.py: none).
  - verify: Attach a 10-minute video, send the turn, `docker compose restart orchestrator` during 'Transcribing'; the row later reads done while the conversation holds no assistant answer.
- **INF-9** [medium/plausible_risk] (infra) `orchestrator/app/uploads.py:498-509,613-614; frontend/app/api/upload/chunked/[...path]/route.ts:37-63` — If the proxy or edge gives up on a long `complete` (Cloudflare ~100 s, undici 300 s, or a frontend recreate) while the orchestrator is still assembling, the server finishes, deletes the parts and marker, and starts the analysis — but the client sees 502/524 and a retry of `complete` returns 404 'upload not found', so the UI reports a failed upload for a video whose analysis is actually running (and a re-upload of the same bytes creates a second attachment).
  - evidence: uploads.py:613-614 `shutil.rmtree(parts_dir, ignore_errors=True); os.unlink(os.path.join(root, _MARKER))` after assembly; 498-509 `_chunk_state` raises 404 when the marker is missing. The proxy (route.ts:53-63) buffers `upstream.text()` and answers 502 on any thrown error; nothing in the orchestrator keys `complete` by an idempotent token or records 'completed' state for a late retry.
  - verify: Add an artificial 130 s sleep in chunked_complete and run scripts/video_smoke.py --via-frontend through the public hostname; observe 524/502 on the client and 'analysis started' in orchestrator logs. Fix: make `complete` idempotent (keep a `_completed.json` with the upload result and return it on re
- **S-04** [low/confirmed_by_code] (rest) `orchestrator/app/sharing.py:259-261` — The 'an image someone uploaded is private whatever the answer says' guard keys on `m.get('has_attachment')`, a field no producer in the repo sets, so it never fires; image-turn privacy rests entirely on the user message's meta.attachments key being persisted by the history PUT.
  - evidence: sharing.py:259-261 `if m.get('has_attachment'): private_kinds.append('uploaded files')`; `grep -rn has_attachment orchestrator/ frontend/` matches only this line. 'vision' is in PUBLIC_SAFE_ROUTES (sharing.py:118).
  - verify: History reader: check what key the frontend/history PUT persists for image chips; if it is not `attachments`, share a conversation with an image and see the vision answer go public.
- **S-06** [low/confirmed_by_code] (rest) `orchestrator/app/engines/report.py:127-135 (_run_pandoc); orchestrator/app/core/report_render.py:134-140 (consulted); orchestrator/app/main.py (grep: no wall_clock)` — pandoc/weasyprint is spawned with create_subprocess_exec + communicate() and no timeout in both report paths, and main.py applies no wall clock to the whole generation task (only llm.py:385 bounds the model stream), so a wedged renderer keeps the SSE stream open indefinitely (heartbeats every 15 s keep proxies from cutting it) and the turn never reaches done/error.
  - evidence: report.py:127-130 `proc = await asyncio.create_subprocess_exec(*cmd, ...)`; `_, stderr = await proc.communicate()` with no asyncio.wait_for; report_render.py:134-140 same shape; `grep -n wall_clock orchestrator/app/main.py` returns nothing; sse.py:185 HEARTBEAT_SECONDS=15.
  - verify: Replace pandoc in the orchestrator image with a script that sleeps forever and request a report; the stream stays open past GEN_WALL_CLOCK_S with only ': keep-alive' frames.
- **S-07** [low/confirmed_by_code] (rest) `orchestrator/app/metrics.py:31-32,124-137; orchestrator/app/video/pipeline.py:338 (consulted); docs/video-understanding/README.md:239` — video_stage_seconds is recorded into the shared histogram whose largest bucket is 30 s, while measured video stages take 282-453 s, so every long stage lands in +Inf and per-stage latency quantiles/alerts for the video pipeline are meaningless.
  - evidence: metrics.py:32 `_BUCKETS = (0.005, ..., 10.0, 30.0)` with the comment 'Tuned for retrieval and small fetches, not for model generation'; pipeline.py:338 `metrics.observe('video_stage_seconds', ms / 1000.0, ..., stage=stage)`; README.md:239 'transcription 453 s ... OCR 282 s'.
  - verify: curl the orchestrator /metrics after one long video: all video_stage_seconds_bucket{le<=30} counts are 0 for transcript/ocr and only le=+Inf increments.
- **T-12** [low/confirmed_by_code] (tests) `orchestrator/tests/test_video_understanding.py:640-644; orchestrator/tests/test_live_generation.py; orchestrator/app/main.py:1035` — Restart recovery is covered only at the DB helper level (`requeue_interrupted_video_analyses`); no test proves the lifespan requeues/resumes analyses, and `_live_generations` is an in-memory dict so after an orchestrator restart /chat/active is empty and a reloaded tab reads the turn as unsent/stale — untested.
  - evidence: test_video_understanding.py:642-644 `db.update_video_analysis(row['id'], status='running'); assert db.requeue_interrupted_video_analyses() == 1`; no test enters `TestClient(app)` lifespan and asserts a queued row is picked up; main.py:1035 `_live_generations[conv_key_outer] = gen` (module dict).
  - verify: Seed a 'running' video_analyses row, enter TestClient(app), assert status → queued and pipeline.ensure_running was called.
- **T-15** [low/confirmed_by_code] (tests) `.github/workflows/pipeline.yml:184-323; orchestrator/tests/test_video_understanding.py:157-158; frontend/app/api/upload/route.ts; frontend/app/api/upload/chunked/[...path]/route.ts` — CI runs the orchestrator suite on Python 3.11 with a digest-pinned PostgreSQL 18.4 service and no ffmpeg (so the one ffmpeg-dependent video test skips), and the frontend suite on Node 20 with `vitest run` (node env, jsdom opt-in); the Next upload/chunked proxies that the public 100 MB-edge path depends on have no tests, and nothing exercises an old frontend against the new backend.
  - evidence: pipeline.yml:252 `python-version: "3.11"`, 238 `image: postgres@sha256:9a8afca…`, 275-276 `TEST_DATABASE_URL: …/techsara_orchestrator_test` + `python -m pytest tests -q`; 303-310 `node-version: "20"` + `npm test` (package.json test = `vitest run`); no apt-get ffmpeg step; test_video_understanding.py:157-158 `if not media.tools_available(): pytest.skip("ffmpeg is not installed here")`; grep of frontend/tests for 'app/api/upload' → only upload-retrieval.test.ts (file proxy).
  - verify: Inspect the orchestrator job steps in pipeline.yml; run `grep -l 'app/api/upload' frontend/tests/*`.
- **T-16** [low/confirmed_by_code] (tests) `orchestrator/tests/test_authn_idor.py:96-107, 153-169; orchestrator/tests/test_document_uploads.py:152-164; orchestrator/app/video/api.py` — Cross-user access is covered for stop/attach/active, single-shot uploads and chunked init/part/complete, but not for /uploads/{conv}/{id}/file, /uploads/{conv}/document, or /video/{conv}/{upload}/status; there is no video cancel endpoint, so Stop on a video turn cancels only the chat generation while the analysis continues — untested.
  - evidence: test_authn_idor.py:96-107 covers POST /uploads and GET /uploads/{conv}; test_document_uploads.py:152-164 covers chunked routes as bob; grep of video/api.py routes shows only the status route (154) and artifact helpers — no cancel; grep of tests for '/uploads/.*/file' as another user → none.
  - verify: As bob, GET /uploads/conv-docs/{alice_upload}/file and GET /video/conv-docs/{upload}/status → expect 404 (currently unasserted).
- **ORCH-11** [low/confirmed_by_code] (be-chat) `orchestrator/app/main.py:1912 (gen.waiting_on_video = True, never cleared), 105-111` — waiting_on_video is set for the whole video turn and never reset after the analysis completes, so while the engine is decoding the answer on the main model the busy probe reports 'not chatting' and other video jobs run ASR/OCR at full speed during that decode.
  - evidence: `gen.waiting_on_video = True  # see video_pipeline.set_busy_probe` followed directly by `await video_engine.run_video_engine(...)`; no assignment to False anywhere in main.py or video.py.
- **ORCH-12** [low/confirmed_by_code] (be-chat) `orchestrator/app/engines/video.py:60 (_STEP_IDS fixed 1-9), 504-506 (sequential waits); orchestrator/app/video/pipeline.py:198-204 (subscribe replays only _latest)` — Two videos in one turn are waited on one after the other and emit the same step ids 1-9, so the second video's steps overwrite the first's in the timeline, and progress the second job publishes while the first is being waited on is dropped except its last event.
  - evidence: `_STEP_IDS = {name: i + 1 for i, name in enumerate(STAGES)}`; `for row in videos: rows.append(await _wait_for_analysis(row, emit))`; subscribe: `last = _latest.get(...); if last is not None: q.put_nowait(last)`.
- **ORCH-13** [low/confirmed_by_code] (be-chat) `orchestrator/app/main.py:736-745 (_resolve_video_refs), 1903-1907` — A chat request that references an upload whose analysis link does not exist yet (the reloaded tab aborted the 400 MB upload, or 'Send now' fired before the link was written) streams 'X is not attached to this conversation — please re-attach it.' as a normal token+meta+done answer, which is persisted as the assistant's reply; when the original upload later completes and sends, a second answer follows.
  - evidence: `row = await db.run_in_thread(db.get_video_by_upload, conversation_id, upload_id); if row is None: return [], f"{name} is not attached to this conversation — please re-attach it."` then `await emit('token', {'text': video_err}); await emit('meta', {'route': 'video'}); answer = video_err`.
- **ORCH-14** [low/confirmed_by_code] (be-chat) `orchestrator/app/video/api.py:66-109 (attach_upload keyed by content_hash, `reused`), orchestrator/app/main.py:2373-2388 (/chat/stop)` — Analyses are shared across tenants by content hash (by design) and the upload response's `reused: true` is a cross-tenant hash oracle; there is no endpoint to cancel a running analysis — /chat/stop only cancels the chat worker and the job keeps consuming the single GPU slot.
  - evidence: `row = await db.run_in_thread(db.upsert_video_analysis, content_hash, size, media_type, filename)` ... `'reused': not row.get('created', False) and status == 'done'`. chat_stop: `gen.task.cancel(); return {'stopped': True}` — pipeline untouched.
- **F-11** [low/confirmed_by_code] (be-uploads) `orchestrator/app/uploads.py:496-505, 557-559, 585, 607-608` — Between the marker check and the file operations, a concurrent complete or the sweep can remove the marker or _parts, turning the request into an unhandled FileNotFoundError (500) rather than a 404/409; the client then reports an opaque 'upload could not be assembled'/'part N failed'.
  - evidence: _chunk_state does isfile then open (uploads.py:503-505); chunked_part os.scandir(parts_dir) (557-559) and chunked_complete os.scandir + os.unlink(marker) (585, 608) have no try/except.
  - verify: Issue two POST /complete concurrently for one upload id; one returns 500.
- **F-12** [low/confirmed_by_code] (be-uploads) `orchestrator/app/uploads.py:511-520, 544-551, 578-584` — Only chunked_init checks dataset_uploads_enabled, require_attachments and require_video; part and complete check only ownership, so an account whose attachment/video capability is revoked mid-upload can still finish and start an analysis.
  - evidence: chunked_init has `_attachments: None = Depends(require_attachments)` and the purpose/video gates (511-526); chunked_part and chunked_complete depend only on require_user and call _own (544-552, 578-584).
  - verify: Init as a user with attachments on, disable the feature for that user, then PUT part and POST complete: both succeed.
- **F-14** [low/confirmed_by_code] (be-uploads) `orchestrator/app/uploads.py:436-451; orchestrator/app/video/store.py:96-119` — After the workspace sweep, GET /uploads/{conv} reports a video upload as 'expired' although its analysis source survives under /data/video via the hard link, so any UI that trusts that status tells the user to re-attach a video the server can still answer about.
  - evidence: list_uploads flips status to 'expired' when bytes_available(conversation_id, id) is false (uploads.py:447-449), which only looks at the workspace copy (45-48); adopt_source hard-links the bytes into analysis_dir (store.py:112-118) and _run uses store.source_path (pipeline.py:369-372).
  - verify: Delete /data/workspaces/uploads/<conv>/<upload> for a video and GET /uploads/<conv>: status 'expired'; /video/.../status still reports source_present=true.
- **F11** [low/confirmed_by_code] (fe-attach) `frontend/components/ChatApp.tsx:1469-1550` — runRegenerate (the 'Send now' path) never removes send_state, so the marker stays in server history and the notice reappears whenever the answer row is discarded.
  - evidence: grep for send_state in ChatApp.tsx hits only 1190, 1306, 1313-1314, 2512; runRegenerate builds startStream from resendOptionsFor without touching meta.
  - verify: Send now on an unsent doc turn, then confirm-regenerate that answer (truncate): the notice returns.
- **F14** [low/confirmed_by_code] (fe-attach) `frontend/components/ChatApp.tsx:1106-1112; frontend/lib/attachments.ts:300-330` — rememberAttachmentFiles keeps only [first] for pdf/dataset turns and only images otherwise (nothing for a video-only turn), while cards are rendered from meta.attachments positionally, so multi-document or video turns cannot open their cards in-tab and indices can point at the wrong file.
  - evidence: `rememberAttachmentFiles(userMessage.id, (isPdf || isDataset ? (first ? [first] : []) : images).map(...))` (ChatApp.tsx:1106-1112) versus meta.attachments = [...docs, ...videos]; attachmentFile(messageId, index) is positional (attachments.ts:322-327).
  - verify: Send two PDFs and open the second card in the same tab: the preview falls to the server tier or 'unavailable'.
- **V8** [low/confirmed_by_code] (be-video) `orchestrator/app/video/pipeline.py:237-256` — ensure_running checks is_running, then awaits a DB read, then creates the task; two concurrent callers (attach_upload from an upload plus drain_queue, or two conversations attaching the same bytes at once, or the chat turn's _wait_for_analysis racing an upload) both create a task for the same analysis. With the 1-slot semaphore the second run usually finds the row 'done' and exits, but if the first run failed the second silently re-runs it, and with VIDEO_MAX_CONCURRENT_JOBS>1 both run concurrently on the same files and row.
  - evidence: `if is_running(analysis_id): return True` at :240, `row = await db.run_in_thread(...)` at :242 (a yield point), `task = ...create_task(_run(...))` at :253, `_tasks[analysis_id] = task` at :254 overwrites; `_run` re-reads the row and proceeds for any status other than current 'done' (:361-367).
  - verify: Call ensure_running twice concurrently in a test (asyncio.gather) with a slow get_video_analysis stub and assert two _run invocations.
- **V11** [low/confirmed_by_code] (be-video) `orchestrator/app/video/api.py:77-81 (413 when size > VIDEO_MAX_UPLOAD_MB after merge); orchestrator/app/uploads.py:557 (chunked cap_total = UPLOAD_MAX_MB), :232-233 (upload root deleted on HTTPException)` — The video-specific 4096 MB ceiling is checked only in attach_upload, after the whole file has been uploaded (through Cloudflare in 90 MB parts, since the chunked rail's own cap is UPLOAD_MAX_MB, set to 102400 in the deployed env) and merged; an over-limit video costs the full upload and is then refused and deleted.
  - evidence: `cap = settings.video_max_upload_mb * 1024 * 1024; if size > cap: raise HTTPException(413, ...)` runs on `complete`; chunked_part uses `cap_total = settings.upload_max_mb * 1024 * 1024`; env grep: UPLOAD_MAX_MB=102400.
  - verify: Declare a 5 GB video on the chunked init route: no 413 until complete.
- **V13** [low/confirmed_by_code] (be-video) `orchestrator/app/db.py:5060-5096 (upsert leaves an existing row's filename untouched); orchestrator/app/video/pipeline.py:655 (fusion prompt File: header), :691 (artifact titles)` — Because analyses are content-addressed, the FIRST uploader's filename is baked into the fusion prompt and into transcript.txt / summary.md for every later tenant who attaches the same bytes; a second user's downloadable artifacts carry another user's filename.
  - evidence: `_stage_fusion` passes `filename=str(ctx.row.get("filename") or "video")`; `_stage_artifacts` uses `title = str(ctx.row.get("filename") or "video")` for `transcript_txt(..., title=title)` and `summary_md(u, title=title, ...)`; only `status_payload` uses the per-link `display_name` (api.py:168).
  - verify: Two users upload the same bytes as 'boardpack-Q3.mp4' and 'x.mp4'; download the second user's summary.md.
- **V15** [low/confirmed_by_code] (be-video) `orchestrator/app/video/pipeline.py:753-764 (drain_queue limit=4), :791-801 (5 s then every VIDEO_MAINTENANCE_INTERVAL_S=1800)` — After a restart that requeued more than four rows, rows 5+ are not started until the next 30-minute maintenance pass even when the single slot frees within minutes; only a fresh attach (ensure_running) short-circuits it.
  - evidence: `rows = await db.run_in_thread(db.list_video_analyses, "queued", limit)` with `limit: int = 4`; `_maintenance_loop` sleeps `max(60.0, settings.video_maintenance_interval_s)` between passes.
  - verify: Queue six videos, restart the orchestrator, observe the fifth's started_at.
- **V16** [low/confirmed_by_code] (be-video) `orchestrator/app/video/pipeline.py:391-404 (branches run to completion before _finish), :346-350; orchestrator/app/video/api.py (status route only — no cancel endpoint)` — A fatal transcript failure is published as 'Transcribing: failed' while the frames/OCR/vision branch keeps running for minutes; the job's terminal '_done failed' comes only after that branch ends, so the UI shows a failed step followed by continuing progress. There is no cancel endpoint, so an operator cannot stop a 4-hour job holding the only slot except by restarting the orchestrator.
  - evidence: `failures = await asyncio.gather(*(runner.chain(branch) for branch in _BRANCHES)); failure = next((f for f in failures if f), None)` then `_finish`. api.py exposes GET status only.
  - verify: Stop whisper and attach a 20-minute slide video; watch the step list.
- **V17** [low/confirmed_by_code] (be-video) `compose/whisper/server.py:508 (await file.read() through Starlette UploadFile), :224-236 (_decode subprocess timeout=120 raises TimeoutExpired), :514 (run outside the HTTPException mapping); orchestrator/app/asr.py:465-473 (5xx -> stand down 20 s)` — The engine's 'nothing is written to disk' claim is false for clips over 1 MB (Starlette spools multipart uploads above 1 MB to a temp file before file.read()); the engine has no upload size cap of its own; and a decode that exceeds 120 s surfaces as a bare 500, which the orchestrator treats as the ENGINE being unavailable and stands it down for 20 s on the strength of one bad clip.
  - evidence: `payload = await file.read()` on an `UploadFile = File(...)` parameter; `subprocess.run([... 'pipe:0' ...], input=payload, timeout=120)` raises subprocess.TimeoutExpired (not HTTPException) from `loop.run_in_executor(None, _decode, payload)` which is outside the try at :517-530. RoutedProvider: `except ASRUnavailable as exc: ... self._down_until[index] = time.monotonic() + self._COOLDOWN_S`.
  - verify: POST a 2 MB clip and `ls /tmp` inside the whisper container during the request; POST a crafted stream that stalls ffmpeg and read the orchestrator's 'standing it down' warning.
- **V19** [low/confirmed_by_code] (be-video) `orchestrator/app/video/pipeline.py:374-383 (only stages/counts reset), :406-414 (_finish on failure keeps summary/understanding/artifacts/indexed_at)` — When a row finished under an older PIPELINE_VERSION is re-run and fails early (e.g. source missing or probe error), the row keeps the previous summary, understanding, artifacts and indexed_at, so status_payload advertises downloadable artifacts from the superseded run beside status 'failed'.
  - evidence: `**({"stages": {}, "counts": {}} if stale else {})` is the only reset; `_finish` sets status/error/finished_at/stage only.
  - verify: Bump PIPELINE_VERSION in a test, delete the source file, call ensure_running, read status_payload.
- **INF-11** [low/confirmed_by_code] (infra) `frontend/app/api/upload/route.ts:41-46; frontend/app/api/upload/chunked/[...path]/route.ts:61-63; frontend/app/api/chat/active/route.ts:26-28; orchestrator/app/metrics.py:20-100; monitoring/prometheus/rules/alerts.yml; monitoring/prometheus/prometheus.yml:183-197` — Upload-path proxy failures are swallowed without a server log line (unlike /api/chat, which calls logProxyError), the orchestrator exports no upload counters (only video stage and ASR metrics), and no alert covers video/ASR/upload failures or planned container recreates; the blackbox probe only checks /health and /. An upload that dies mid-flight therefore leaves no trace anywhere an operator looks.
  - evidence: upload route.ts:41-46 `catch { return Response.json({ detail: 'The orchestrator is unreachable.' }, { status: 502 }); }`; chunked route.ts:61-63 same shape; active route.ts:26-28 `catch { return Response.json({ active: [] }); }`. chat/route.ts:189-218 by contrast logs via logProxyError. metrics.py `_ALLOWED` labels cover stage/language/rule/result — no upload family; grep 'upload' in metrics.py returns nothing. alerts.yml groups: availability, gpu-health, host-health, vllm-health, containers (ContainerRestartLoop >3 in 30m), databases — nothing on video_stage_total{result="fail"} or asr_errors_total.
  - verify: Stop the orchestrator and POST /api/upload: the frontend log shows nothing. Fix: log the cause in the three catches, add upload_requests_total{result}, and alert on video_stage_total{result="fail"}.
- **S-05** [low/plausible_risk] (rest) `orchestrator/app/authn/sessions.py:237-246,267-286; orchestrator/app/authn/store.py:466-478; orchestrator/app/authn/api.py:200-224; orchestrator/app/config.py:1112-1116` — An unremembered session has an absolute ceiling of AUTH_SESSION_UNREMEMBERED_HOURS (24 h) that rolling renewal cannot extend (roll_session uses LEAST(now+lifetime, absolute_expires_at)), and /auth/me actively clears an expired cookie; a long chunked upload or a multi-hour analysis wait that crosses that ceiling gets 401 on the next chunk PUT / /chat, which the notice layer can only report as 'never sent'.
  - evidence: sessions.py:243-246 `absolute_lifetime_for(remember=False) = timedelta(hours=settings.auth_session_unremembered_hours)`; store.py:475 `expires_at = LEAST(%s, absolute_expires_at)`; api.py:218-224 sets a cookie-clearing header on END_SESSION_EXPIRED. Remembered sessions (default remember=True, LoginRequest.remember) get 30/90 days.
  - verify: Log in with remember=false, set AUTH_SESSION_UNREMEMBERED_HOURS=1 in a test env, start a chunked upload spanning the boundary, and observe the PUT part response and what the composer shows.
- **S-08** [low/plausible_risk] (rest) `orchestrator/app/authn/store.py:842-849 (bind_report ON CONFLICT DO NOTHING); orchestrator/app/engines/report.py:255-256,283-285; orchestrator/app/engines/dataset_report.py:329-330; orchestrator/app/core/report_render.py timestamped_base` — Non-video report filenames are <slug>-<YYYYmmdd-HHMMSS> with no user or random component; report_files.filename is a global PK bound to the first user and never rebound, and pandoc -o overwrites, so two users generating same-titled reports in the same second leave the second user 404'd on their own file and the first user able to download the second user's content.
  - evidence: report.py:255-256 `stamp = time.strftime('%Y%m%d-%H%M%S'); base_name = f"{slugify(plan['title'])}-{stamp}"`; store.py:845-847 `INSERT INTO report_files ... ON CONFLICT (filename) DO NOTHING`; engines/video.py:449 shows the per-user `-u{user_id}` scheme was added only for video artifacts.
  - verify: Run two dataset-report turns as two users within one second with identical titles (e.g. same filename) and compare report_files.user_id with the file contents.
- **T-13** [low/observation] (tests) `orchestrator/tests/test_history.py:32-36; orchestrator/tests/test_history_v3.py:316-321; orchestrator/tests/test_history_search.py:73; orchestrator/tests/conftest.py:319-350` — 'needs no credentials' tests assert anonymous 200s that pass only because the conftest shim resolves a cookie-less request to the 'local' member; they no longer prove what their names and docstrings claim (login returned 2026-09-01) and cannot catch an anonymous-access regression.
  - evidence: test_history.py:33 docstring 'Login was removed — history is open to whoever can reach the port'; conftest.py:343-346 `if not state['ambient_enabled']: return None; … state['principal'] = _principal_for('local','member')`; test_authn_idor.py:234-239 shows the real rule (anonymous /chat → 401 under anonymous_mode).
  - verify: Run test_history_needs_no_credentials with the anonymous_mode fixture → 401.
- **T-14** [low/observation] (tests) `frontend/tests/dataset-chat-request.test.tsx:590; orchestrator/tests/test_live_generation.py:52; orchestrator/tests/test_video_understanding.py:672-694; frontend/tests/_wireHarness.tsx:171,182; frontend/tests/dataset-upload-feedback.test.tsx:116-126; frontend/tests/conversation-switch-loading.test.tsx:59-72; orchestrator/tests/test_conversation_integrity.py:447` — Several mission-adjacent assertions rest on sleeps/timeouts, while barrier-style primitives already exist in the codebase and could be lifted into _wireHarness: a per-URL deferred registry (hold/release/fail per file), a settable /chat/active list, and a remount helper backed by the stored messages.
  - evidence: dataset-chat-request.test.tsx:590 `await new Promise((r) => setTimeout(r, 50)); expect(chatBodies).toEqual([])`; test_live_generation.py:52 `await asyncio.sleep(0.01)  # reader drains the replayed buffer`; _wireHarness send() waits with `{ timeout: 4000 }`. Barrier patterns: dataset-upload-feedback deferredUpload {settle, fail}; conversation-switch-loading deferLoad/land; streaming-frame-coalescing manual rAF queue ('nothing here sleeps or races'); test_conversation_integrity `threading.Barrier(8)`.
  - verify: n/a — recommendation
- **F-13** [low/plausible_risk] (be-uploads) `orchestrator/app/uploads.py:473-493; orchestrator/app/history.py:114-128; frontend/lib/history.ts:892-897` — An early upload into a not-yet-created chat creates the conversation server-side as 'New chat' before the client's own create (409, tolerated); if the chat request never goes out (the incident shape) the server keeps an empty 'New chat' row that other devices list, and the client's title is never pushed because pushAll only re-applies flags.
  - evidence: _own inserts `db.create_conversation(user_id, conversation_id, "New chat")` when unowned (uploads.py:482-490); create_conversation returns 409 on IntegrityError (history.py:126-128); pushAll swallows the 409 and afterwards pushes only pinned/archived (history.ts:892-897, 918-921).
  - verify: Attach a file in a brand-new chat without sending; list conversations from another device.
- **F10** [low/plausible_risk] (fe-attach) `frontend/lib/uploadDocument.ts:77-100; orchestrator/app/uploads.py:546-580,591-598` — The client never tells the server the total size or part count, so complete() can only check contiguity; a part accepted with fewer bytes than sent, or a trailing part that never existed, assembles silently. A client disconnect mid-part leaves a partial file that counts toward cap_total.
  - evidence: init form carries only conversation_id, filename, purpose (77-80); complete is a bare POST (97-100). Server: `parts = sorted(...)`, `expected = [f'{i:05d}' for i in range(len(parts))]`, `if parts != expected: 400` (591-598) — no size check. chunked_part writes inside `async for chunk in request.stream()` with no except/unlink on disconnect (563-579) and `already = sum(e.stat().st_size ...)` counts leftovers (559-561). Exposure is narrow because the client only calls complete after every part returned 2xx.
  - verify: Abort a part PUT mid-body and inspect the upload's _parts dir; PUT a short body for part k then complete — assembled size is wrong with 200 OK.
- **F17** [low/observation] (fe-chat) `frontend/components/Composer.tsx:499-509 (no AbortController), removal/switch paths` — Removing a video chip, leaving the conversation or never pressing Send does not abort the in-flight early upload nor tell the server to stop the analysis it started at upload time; server-side GPU work continues for turns that will never be sent.
  - evidence: uploadDocumentFile is called without a signal and the promise is only awaited or dropped.
- **F19** [low/plausible_risk] (fe-chat) `frontend/lib/history.ts:889-925 (pushAll), 1250-1262 (setMessageFeedback markDirty), 1385-1392 (refresh re-pushes dirty)` — replaceMessages is refused only when it would shrink the thread; an equal-length replace from a tab whose cache predates the upload-id links (marked dirty by a thumb that could not reach the server) rewrites the server's user turn without the ids, making the turn un-resendable and severing the video references.
  - evidence: `await api.replaceMessages(conv.id, conv.messages.map(toServerMessage))` guarded only by `isConflict`; historyApi comment: 'The server REFUSES (409) when the incoming thread is shorter'.
- **F20** [low/plausible_risk] (fe-chat) `frontend/lib/uploadDocument.ts:16-17, 97-102` — CHUNK_THRESHOLD_BYTES=90 MB / CHUNK_PART_BYTES=64 MB keep every request under the 100 MB edge wall, but the final POST …/complete is one request whose response waits for server-side assembly; on the public path a slow assembly of a multi-GB file can exceed Cloudflare's origin response timeout (524), the client throws 'upload could not be assembled', the turn is marked failed and later described as 'page closed', while the server may have assembled the file and started the analysis (orphan). No retry or status poll exists on the client.
  - evidence: `const done = await fetch(…/complete, { method: 'POST' }); const body = await jsonOrThrow<UploadBody>(done, 'upload could not be assembled');`
- **V12** [low/plausible_risk] (be-video) `orchestrator/app/video/store.py:74-83 (source_path matches any 'source.*' file), :95-97 (adopt_source returns an existing match early), :105-109 (hardlink else copy to source.<ext>.part); orchestrator/app/video/pipeline.py:369-371, :753-757` — source_path treats 'source.mp4.part' as the source. When adoption falls back to a copy (upload root and VIDEO_DATA_DIR on different filesystems), a concurrent attach of the same bytes returns the half-written .part as 'existing' and abandons its own copy, and a maintenance drain or that second attach's ensure_running can probe the partial file ('moov atom not found') and fail the row. Inert when the hard link succeeds (same volume), which appears to be the normal case.
  - evidence: `if entry.is_file() and entry.name.startswith("source."): return entry.path`; `existing = source_path(content_hash); if existing: return existing`; `tmp = dest + ".part"; try: os.link(path, tmp) except OSError: shutil.copyfile(path, tmp); os.replace(tmp, dest)`.
  - verify: Mount VIDEO_DATA_DIR on a different filesystem from the workspace, attach the same 1 GB file from two sessions within a few seconds, and inspect the second row's error.
- **V14** [low/plausible_risk] (be-video) `orchestrator/app/video/pipeline.py:574-584 (_kept_frames drops frames whose file is missing), :605-620 (texts positional over the filtered list), :621-624 (vision pads only when shorter); orchestrator/app/video/screen.py:251 (build_spans zips frames/texts/captions)` — ocr.json texts are positional over the frames that EXISTED at OCR time; _stage_vision recomputes the list and only pads when it is shorter. If a frame file disappears between a cached OCR stage and a re-run vision stage (resume after a restart or a failed vision attempt), texts shift onto the wrong frames and the spans cite the wrong timestamps.
  - evidence: `texts = [by_index.get(k.index, "") for k in kept]` where `kept` is the filtered list; later `texts = list(ocr_data.get("texts") or []); if len(texts) < len(kept): texts += ["" ...]` then `zip(frames, ocr_texts, captions)`.
  - verify: After OCR completes, delete frames/f_000003.jpg, mark vision failed on the row, re-attach; compare screen.json spans' frame names to ocr.json order.
- **INF-12** [low/observation] (infra) `compose.yaml:152-254,291-315; .env.example:933-941` — No memory, pids or CPU limits exist on the orchestrator, frontend or sync-worker, and the orchestrator runs ffmpeg (VIDEO_FFMPEG_THREADS=8), OCR (4-wide) and caption (3-wide) fan-out in the same process that serves chat, on a unified-memory node shared with the vLLM engines; the only guard is the UnifiedMemoryPressure alert at 92%.
  - evidence: compose.yaml orchestrator service (152-254) and frontend (291-315) contain no `mem_limit`, `pids_limit`, `cpus` or `deploy.resources.limits`; the `deploy:` block on the DGX overlay reserves GPUs only (compose.dgx-spark.yaml:9-15,31). alerts.yml:102-113 UnifiedMemoryPressure > 92% for 5m.
  - verify: `docker inspect sf-local-ai-orchestrator-1 --format '{{.HostConfig.Memory}} {{.HostConfig.PidsLimit}}'` returns 0/0.
- **INF-13** [low/observation] (infra) `scripts/deploy-drain.sh:142-231; scripts/deploy.sh:439-442; .runtime/logs/deploy-20260909-224832.log:308-315` — The drain `wait` counts ESTABLISHED connections with `--max 0`; the Next proxy's undici keep-alive pool, sidebar polling of /chat/active and Prometheus scrapes keep that count non-zero, so the wait spends the whole 90 s deadline per service and then proceeds — it adds three minutes to every deploy and never actually finds a gap.
  - evidence: deploy.sh:439-442 loops orchestrator/frontend/sync-worker with `--deadline 90 --quiet-for 5` and `|| true`. Log 22:48 run: `drain wait: orchestrator still has 2 established connection(s) after 90s.` then `drain wait: frontend still has 1 established connection(s) after 90s.`; the 21:58 run shows the same.
  - verify: Run `scripts/deploy-drain.sh wait orchestrator --deadline 30` on an idle box and watch the count stay at >=1 from the frontend's keep-alive connection.
- **INF-14** [low/plausible_risk] (infra) `frontend/Dockerfile:22-32; frontend/next.config.mjs; frontend/lib/uploadDocument.ts:16-17; orchestrator/app/uploads.py:462-467` — Nothing overrides the Node HTTP server's default request timeouts in the standalone Next server (Node's `server.requestTimeout` is 300 s for receiving the entire request), so a 64 MiB part on a remote link slower than roughly 220 KB/s would be dropped with 408 regardless of the orchestrator's limits; part sizes (64 MiB client, 90 MiB server cap) are otherwise correctly under the 100 MB edge wall, and the orchestrator itself imposes no body limit (Starlette streams; caps are enforced live in the handlers).
  - evidence: Dockerfile:32 `CMD ["node", "server.js"]` with only PORT/HOSTNAME env; next.config.mjs sets headers only. uploadDocument.ts:16-17 `CHUNK_THRESHOLD_BYTES = 90 * 1024 * 1024; CHUNK_PART_BYTES = 64 * 1024 * 1024`; uploads.py:463 `_PART_CAP = 90 * 1024 * 1024`. Next's start-server internals could not be read (node_modules excluded), so the timeout behavior is asserted from Node defaults.
  - verify: Throttle a browser to ~150 KB/s and upload a 200 MB file through the public hostname; watch for 408s on part PUTs in the frontend container log.
- **S-09** [info/confirmed_by_code] (rest) `orchestrator/app/authn/api.py:61-76; orchestrator/app/config.py:239; compose.yaml:213; launcher/techsara_cli/environment.py (generated keys list)` — Whether the composer offers video at all is decided per deployment by VIDEO_ANALYSIS_ENABLED folded into /auth/me.features; the default is false, the launcher's generated.env never sets it, and nothing surfaces the flag in /health, so a localhost stack and the tunnelled stack can legitimately show different composers for the same account and the difference is invisible to monitoring.
  - evidence: api.py:73-75 `if key in features and not settings.video_analysis_enabled: features[key] = False`; config.py:239 `_bool('VIDEO_ANALYSIS_ENABLED', False)`; compose.yaml:213 `${VIDEO_ANALYSIS_ENABLED:-false}`; environment.py generated-key grep contains no VIDEO_*, UPLOAD_MAX_MB, SSE_HEARTBEAT_SECONDS, LLM_REQUEST_TIMEOUT or GEN_WALL_CLOCK_S; health.py grep has no video probe.
  - verify: Compare `/auth/me` features.video_analysis between http://127.0.0.1:3000 and https://ai.techsarasolutions.com for the same user.
- **S-10** [info/confirmed_by_code] (rest) `orchestrator/app/engines/dataset.py:205-211,235-248` — The dataset engine answers from every upload row in the conversation, not from the uploads the current turn referenced, so a dataset attached to a turn that was never sent (or to an earlier turn) is silently included in later dataset answers in that conversation.
  - evidence: dataset.py:205 `uploads = await db.run_in_thread(db.get_uploads, conversation_id)` and :239-247 lists all of them in meta.datasets; no per-turn filter.
  - verify: Upload two CSVs in one conversation, reload before sending the second turn, then ask a dataset question: meta.datasets lists both.
- **S-11** [info/confirmed_by_code] (rest) `orchestrator/app/sse.py:133,165-172` — step status is a closed set {running, done, failed}; there is no queued/cancelled state, so a video analysis waiting behind another job (VIDEO_MAX_CONCURRENT_JOBS=1) can only be shown as 'running' with a detail string, which reads the same as live progress in a reloaded timeline.
  - evidence: sse.py:133 `STEP_STATUSES = ('running', 'done', 'failed')`; :167-168 raises ValueError for anything else.
  - verify: Start two video uploads from two users and inspect the step frames of the second: status is 'running' before any work starts.
- **F-15** [info/confirmed_by_code] (be-uploads) `orchestrator/app/uploads.py:51-71, 96-99` — Because the file arrives as a FastAPI File(...) form field, Starlette has already spooled the full multipart body to a temp file before the handler runs; the 413 in _stream_to_disk is applied after receipt, so the docstring's 'enforcing the size cap live' is not true and a 100 GB UPLOAD_MAX_MB means the request body is accepted in full first.
  - evidence: create_upload signature `file: UploadFile = File(...)` (uploads.py:96-99); the loop reads from that UploadFile (57-70).
  - verify: POST a body larger than the cap and watch disk usage of the container's tmp before the 413.
- **ORCH-15** [info/observation] (be-chat) `orchestrator/app/main.py:753-772 (/health), 1034, 2385, 2210, 306-341 (_record_usage_event)` — No metric or health field exposes the number of live generations, replaced/cancelled generations, or the video queue depth; the only failure detail (`str(exc)`) goes to the browser as the error payload and nowhere else; usage_events records status but no reason.
  - evidence: /health returns status/checks/context/web_index only; `_metrics.inc('chat_route_total', ...)` fires only on meta; `usage.record_async(... status=status, meta={model_calls, context_tokens, context_window})`.
- **F-16** [info/observation] (be-uploads) `orchestrator/app/video/api.py (no cancel route); orchestrator/app/main.py:2380-2395; orchestrator/app/config.py:257` — There is no endpoint to cancel a video analysis and /chat/stop cancels only the chat generation; with VIDEO_MAX_CONCURRENT_JOBS=1 a single long video serialises every other user's analysis behind it, and a chat turn waiting on it can exceed its wall clock while the analysis keeps running (visible after reload only via /video status).
  - evidence: video/api.py exposes only the status GET (150-164); chat_stop cancels gen.task (main.py:2393-2394) and _run holds a process-wide semaphore (pipeline.py:180-186, 360).
  - verify: Queue two videos from two accounts; the second's status stays 'queued' until the first finishes.
- **F-17** [info/observation] (be-uploads) `orchestrator/app/db.py:1456-1512, 4977-5012; orchestrator/app/video/api.py:81` — Analyses are content-addressed and shared across accounts by design: a second user uploading identical bytes receives the first user's finished summary/understanding/artifacts immediately ('reused'); access requires possessing the bytes, so this is an intentional cache, not a leak, but video_attachments.user_id is nullable and the uploads table has no user_id, so ownership is always derived from conversations.user_id at request time.
  - evidence: upsert_video_analysis keys on content_hash (db.py:4988-5010); attach_upload returns reused=True when the row was not created and is done (api.py:107-113); uploads DDL has no user_id or FK (db.py:187-197).
  - verify: Upload the same file from two accounts; the second attach returns video.reused=true.
- **F12** [info/observation] (fe-attach) `frontend/lib/streams.ts:161-180; frontend/components/Composer.tsx:187; orchestrator/app/uploads.py:557-558; orchestrator/app/config.py:641; .env:348` — fetchServerActive returns [] on any non-401 failure, so a transient failure of /chat/active is indistinguishable from 'nothing running' for the notice's !isStreaming half; the composer's 4 GB video cap is enforced server-side against UPLOAD_MAX_MB (default 200) on every chunked part — fine here (.env sets 102400) but a drift trap for other deployments.
  - evidence: `if (!res.ok) return []; ... catch { return []; }` (streams.ts:171-179). chunked_part: `cap_total = settings.upload_max_mb * 1024 * 1024` applied to video parts (uploads.py:557-558); MAX_VIDEO_BYTES = 4 GiB (Composer.tsx:187); .env:348 UPLOAD_MAX_MB=102400.
  - verify: Run with UPLOAD_MAX_MB unset: a 400 MB video fails at part 3 with 'That file is larger than 200 MB.' after transferring 200 MB.
- **F13** [info/observation] (fe-attach) `frontend/components/Composer.tsx:499-509; frontend/components/ChatApp.tsx:1238-1243; frontend/app/api/uploads/[conversation]/route.ts` — Between attach and Send the only holder of the upload id is the in-memory uploadPromise; a tab closed in that window orphans a stored (and for video, analysed) upload that no turn references, and the composer never reconciles via GET /api/uploads/[conversation].
  - evidence: withEarlyUpload stores the ref only on `att.uploadPromise` (Composer.tsx:508-509); the id is written to meta.attachments only inside send()'s loop (ChatApp.tsx:1238-1243); the list proxy exists 'so a reloaded tab can tell which references still resolve' but has no caller in Composer.
  - verify: Attach an mp4 in an existing chat, close the tab before sending, reopen: GET /api/uploads/<conv> lists it; nothing in the UI does.
- **F21** [info/observation] (fe-chat) `frontend/components/AgentTimeline.tsx:45-62; frontend/lib/streams.ts:702-750` — Step progress for a video answer rides the chat stream only; after a reload the replayed steps show 'Working…' with a client clock that starts at attach time, so the elapsed figure understates. No step carries a server timestamp and no video-analysis job status is queryable outside an attached chat stream, so a tab that is not attached (F3/F4/F5) has no way to show that analysis is happening.
  - evidence: `if (running && startedAt.current === null) startedAt.current = Date.now();` — steps carry no timestamps (sse.ts step schema: id/title/status/detail).
- **V18** [info/observation] (be-video) `orchestrator/app/video/pipeline.py:116 (_OPTIONAL = ocr, vision, index), :636-639 (vision 'failed' when 0 captions), :678-686 (index optional); orchestrator/app/video/index.py:286-289, :312-316 (retrieve returns [] on any failure)` — A job whose vision or index stage failed still finishes 'done'; questions are then answered without frame descriptions or without retrieval evidence and the user is never told which half is missing (only status_payload's stages jsonb records it).
  - evidence: `if outcome.status == "failed" and stage not in _OPTIONAL: return failure` — optional failures return None; `run_video_engine` never inspects `row['stages']`; `retrieve` is documented 'never a gate'.
  - verify: Stop the router sidecar, attach a slide video, ask 'what was on the slide at 2:00'.
- **V20** [info/observation] (be-video) `orchestrator/app/video/pipeline.py:83-110 (pace waits up to VIDEO_PACE_MAX_WAIT_S=20 s per unit); orchestrator/app/main.py:106-110 (busy = any live generation not flagged waiting_on_video), :1911 (flag set once, never cleared)` — While anyone else in the workspace is generating, every ASR window, OCR batch and caption waits up to 20 s before running, so a job's progress advances in 20-s stalls (a 100-window transcript can stretch by ~17 min) — a legitimate policy, but it is part of why the same job looks 'live' one moment and frozen the next. Separately, waiting_on_video stays True for the generation's own answer phase, so a second video job is not paced while a video answer streams.
  - evidence: `while _busy_probe is not None and limit > 0 and waited < limit: ... await asyncio.sleep(1.0); waited += 1.0`; probe is `any(not g.done and not getattr(g, "waiting_on_video", False) for g in ...)`.
  - verify: Run a long chat answer while a transcript is in progress and watch video_pace_seconds in /metrics.
- **INF-15** [info/observation] (infra) `.github/workflows/pipeline.yml:812-905 (verify job); scripts/deploy.sh:451-492 (health); scripts/video_smoke.py` — Neither deploy.sh's health gate nor the pipeline's verify job exercises an upload or a video analysis; both stop at /health, /login, /v1/models and one 8-token completion, so a regression in the upload rail or the video hand-off ships green. A ready-made probe exists (scripts/video_smoke.py --via-frontend) but is not wired in.
  - evidence: pipeline.yml verify steps: 'Every endpoint answers' (frontend /login, orchestrator /health, model /v1/models, grafana, prometheus), 'The model actually generates', 'A rolling deploy did not restart the main model'. deploy.sh health(): /health, frontend /, chat completion, cluster-status. video_smoke.py docstring lines 16-20 describes the browser-equivalent path 'which is what a deploy is verified with' — not referenced by pipeline.yml or deploy.sh.
  - verify: grep -n video_smoke .github/workflows/pipeline.yml scripts/deploy.sh returns nothing.

## Completeness critic

- uncovered: `/home/techsphere/Documents/project/personal-LLM-Chabot/frontend/lib/idbCache.ts (+ frontend/lib/history.ts:308-355 persist debounce)` — Area F/D local half. The IndexedDB persister is the ONLY copy of an answer between the client's `done` and its server push (ORCH-04/F6). history.ts:308 PERSIST_DEBOUNCE_MS=300 and :354 `setTimeout(flushNow, 300)`; grep for pagehide/beforeunload/visibilitychange across frontend/lib and frontend/components returns nothing, so a reload inside that 300 ms window loses the local copy too. history.ts:343-345 comments 'Persistence is best-effort; the server holds the durable copy' — false precisely when a subscriber is attached. No reader cited either file.
- uncovered: `/home/techsphere/Documents/project/personal-LLM-Chabot/frontend/lib/proxy.ts and frontend/app/api/history/[...path]/route.ts` — Area B/F server-push path. Every send_state write, upload-id link and final answer push goes through proxyToOrchestrator (proxy.ts:22-78): it buffers `await req.text()`, sets no timeout, does not retry, and maps any fetch throw to 502 — which history.ts enqueue's catch turns into markDirty. Nobody read the proxy the persistence chain depends on (fe-attach F8 and be-uploads F-06 stopped at history.ts).
- uncovered: `/home/techsphere/Documents/project/personal-LLM-Chabot/frontend/lib/orchestrator.ts` — Idempotency. This is the translator that puts video_uploads/pdf_uploads on the wire (:190-229) and the only place the word 'idempotency key' appears (:70, the clarification contract). Combined with `grep idempot|request_id|client_request_id` returning nothing in orchestrator/app/main.py, it is the primary evidence for fe-chat F14 / be-uploads F-07, yet no reader cited it.
- uncovered: `/home/techsphere/Documents/project/personal-LLM-Chabot/orchestrator/app/core/upload_paths.py and frontend/app/api/uploads/[conversation]/[upload]/file/route.ts` — Tenant isolation on the READ side (restoration D). The reload path resolves attachment cards via /api/uploads/{conv}/{id}/file; T-16 only notes the orchestrator route is untested. Read: sound — uploads.py:335-343 strict owner check (`owner is None` refused) and row scoped by both ids, upload_paths.py:57-91 32-hex regex + resolve() + is_relative_to, Next route validates ids before fetching (:38-41) and forwards the cookie.
- uncovered: `/home/techsphere/Documents/project/personal-LLM-Chabot/orchestrator/app/health.py` — Observability. 571 lines behind GET /health; `grep -n 'video|asr|upload|live_gen|whisper'` returns zero hits, so the health surface cannot see the video pipeline, the ASR engine, upload rail or live generations. INF-15/ORCH-15 cited main.py:753-772 and metrics.py but never opened the module that composes the probes.
- uncovered: `/home/techsphere/Documents/project/personal-LLM-Chabot/compose/compose.dgx-spark.yaml (orchestrator block :18-31), compose/compose.cluster-dgx-spark.yaml (:108-110), compose/compose.cloudflare.yaml` — Area G deployment drift. The overlays the launcher actually stacks in production (environment.py:78, cluster.py:45). Read: they override only image/GPU/bind-mount/extra_hosts and NEVER set stop_grace_period, so compose.yaml's values (orchestrator 2m, frontend unset) hold in every profile — INF-1 applies to all of them. cloudflare overlay maps only frontend:3000 (comment :9-14), confirming INF-6's 'orchestrator unreachable from browsers'. No reader cited any compose/ overlay.
- uncovered: `/home/techsphere/Documents/project/personal-LLM-Chabot/launcher/techsara_cli/compose.py:143-153` — Area G. `up_service` runs `up -d --no-deps [--force-recreate] <svc>` with a 900 s timeout; its own comment (:148-152) says recreation blocks until the old container stops and names sync-worker's 10m grace — it knows the grace mechanism and nothing gives the frontend one. cli.py:1189-1198 (recreate order) was cited; the module that performs the recreate was not.
- uncovered: `/home/techsphere/Documents/project/personal-LLM-Chabot/scripts/deploy-rollback.sh:284-310 and scripts/deploy-smoke.sh` — Area G. Rollback runs the same best-effort drain (`|| true` at :286) then `up -d --no-deps --force-recreate` on orchestrator+frontend (:309) — the identical SIGKILL-at-10 s exposure as INF-1, on the path used when a deploy is bad. deploy-smoke.sh has no upload/video probe (only /v1/chat/completions at :150), so a rollback that breaks the upload rail is also green. Neither cited.
- uncovered: `/home/techsphere/Documents/project/personal-LLM-Chabot/scripts/recovery-tests/ and scripts/tests/` — Restoration D. Both directories contain only __pycache__ and `git ls-files` returns nothing for either — the 'recovery tests' the layout advertises do not exist. The tests reader (74 files) did not report this absence.
- uncovered: `/home/techsphere/Documents/project/personal-LLM-Chabot/scripts/video_edge_cases.sh` — Deployment verification. A ready-made runner of corrupt/silent/long/webm inputs through the real upload+chat routes (via video_smoke.py), not wired into deploy.sh, deploy-smoke.sh or pipeline.yml. INF-15 mentions video_smoke.py only.
- uncovered: `/home/techsphere/Documents/project/personal-LLM-Chabot/frontend/lib/sse.ts` — Area E client half (chunk boundaries). The hand-rolled incremental SSE parser (events split mid-line, CRLF split across chunks :39-48). Not cited by anyone; header states it is unit-tested in tests/sse.test.ts. Sound on parsing — the observed truncation risks (F13, ORCH-02) come from a MISSING terminal event, not from parsing.
- uncovered: `/home/techsphere/Documents/project/personal-LLM-Chabot/frontend/components/AttachmentPreview.tsx:311-316 (+ frontend/lib/attachments.ts:539-560)` — Restoration D. This is the concrete UI be-uploads F-14 alludes to ('any UI that trusts that status'): on a 410 from the file route it renders 'This upload has expired and is no longer stored. Attach the file again' — for a video whose analysis source survives under /data/video via hard link. Not cited by name.
- uncovered: `/home/techsphere/Documents/project/personal-LLM-Chabot/orchestrator/app/video/artifacts.py, fusion.py, vad.py, types.py` — Four of the thirteen video modules were never cited. artifacts.py is the artifact download/bundle path (tenant isolation for cross-tenant content-addressed artifacts, cf. V13); the others are analysis content, not the reliability path — lower priority.
- uncovered: `/home/techsphere/Documents/project/personal-LLM-Chabot/frontend/app/api/chat/stop/route.ts` — Cancellation auth, frontend half. Read: forwards the cookie (:20-23) so the orchestrator's owner check applies; returns {stopped:false} 502 on unreachable. Sound; not cited.
- verdict fe-chat F1 (critical) — poll force-load replaces the uploading turn: **confirmed** — ChatApp.tsx:1183-1191 sets send_state:'uploading' and persists BEFORE the upload; the 8 s poll at :606-634 fires the force load on exactly `id && !isStreaming(id) && !active.includes(id) && last.role==='user'` with no datasetUpload guard, and setMessages(conv.messages) at :623-626; history.ts:1027-1
- verdict fe-attach F1 (high) — every video-only turn classified `missing`: **confirmed** — attachments.ts:185 `docs = attachments.filter(kind==='pdf')` is [] for a video turn, so :199-203 `else if (message.pdfName)` runs; pdfName IS set for video turns at ChatApp.tsx:1087-1088 (`first?.kind === 'video' ? first?.name`) and on reload by history.ts:1041-1046 (attachments[0].name); remembered
- verdict fe-chat F3 (high) — attachStream false on any non-OK treated as 'finished': **confirmed** — streams.ts:731-735 `if (!res.ok || !res.body) { streams.delete; return false }` and :745-749 catch returns false for every non-Abort error; attach route.ts:62-66 maps an unreachable orchestrator to 502 and :67-73 every non-404/401 upstream status to 502; ChatApp.tsx:515-527 and :1866-1878 both react
- verdict ORCH-04 / fe-chat F6 (high) — answer persisted by whichever client is attached; done published before any durable write: **confirmed** — main.py:2183 `await gen.publish('done', ...)` precedes the finally's _finalize_generation (:2226-2229); :358-372 persists only when `gen.subscribers == 0`, and follow() holds subscribers>0 (:257) until the generator drains (:274), so with a tab attached the server never writes. Client side: streams.
- verdict be-uploads F-01 / INF-4 (high) — workspace sweep rmtree's the entire uploads tree: **confirmed** — core/repo.py:94-118 iterates `os.listdir(settings.workspace_dir)` and rmtree's each TOP-LEVEL directory older than TTL (:107-108) or until under quota (:114-118); uploads.py:42-44 `upload_root` = `<workspace>/uploads/<conv>/<id>` — one top-level entry for every user's uploads; uploads.py:126-131 cal
- verdict ORCH-02 (high) — any second POST /chat cancels the running generation; stream ends without done/error; nothing logged: **confirmed** — main.py:1025-1032 `previous.task.cancel()` whenever `previous.user_id == viewer` and not done; worker :2206-2207 `except asyncio.CancelledError: gen.cancelled = True` — no publish, no log; follow() at :271-272 breaks on `self.done` with the buffer drained, so the first subscriber's body simply ends;
- verdict ORCH-03 / INF-3 (high) — restart loses every in-flight generation while the video job is requeued and finishes anyway: **confirmed** — main.py:280 `_live_generations: dict = {}` is in-process; lifespan :96-118 calls video_pipeline.start() (pipeline.py:728-741 `requeue_interrupted_video_analyses`) but the `finally` at :114-117 only stops the pipeline/web worker/pool — nothing persists a LiveGeneration; /chat/active :2398-2410 iterat
- verdict INF-1 (high) — frontend has no stop_grace_period; deploy.sh knows and continues; happened four times on 2026-09-09: **confirmed** — compose.yaml:291-315 frontend block declares no stop_grace_period (grep over compose.yaml and compose/*.yaml finds it only at compose.yaml:80, :229, :288 and the two cluster vllm blocks); deploy.sh:427-428 `deploy-drain.sh check ... || say '  drain: a service being recreated is on Docker's 10s defau
- verdict be-video V1 (high) — gather without return_exceptions leaves sibling ASR windows running and retry hits ASRBusy: **confirmed** — transcribe.py:256 `await asyncio.gather(*(one(i, w) for ...))` with no return_exceptions and no cancellation of siblings on the first raise; one() at :233-240 catches only `asr.ASRRejected` and re-raises past the failure threshold (:238-239) — ASRBusy/ASRUnavailable propagate immediately; asr.py:543
- area Lettered areas A–G — none is silent at the area level: sound — Every lettered area carries reader findings (A early upload: fe-attach F6/F7/F13, fe-chat F11/F17, T-07; B send_state persistence: fe-attach F1/F5/F11, fe-chat F15, F-06, T-02/T-11; C parallel completion: fe-attach F4, fe-chat F8a/F8b, T-03; D restoration: fe-chat F1/F3/F4/F5, INF-2/INF-6, F-10; E c
- area Cancellation authorization (chat stop / attach / replace-by-newer-send): sound — main.py:2370-2378 `_owns` = `gen.user_id == viewer`; /chat/stop :2392 refuses non-owners indistinguishably from absent; /chat/attach :2477 404s unless `_owns`; replacement at :1029-1031 requires `previous.user_id == viewer`; frontend stop route forwards the session cookie (chat/stop/route.ts:20-23).
- area Tenant isolation — upload bytes and video status reads: sound — uploads.py:335-343 `owner is None or owner != user.id` → 404 before any row lookup, then the row is scoped by both ids; core/upload_paths.py:57-91 enforces 32-hex upload ids, basename-only filenames, resolve() and `is_relative_to(root)`; video/api.py:161-166 owner check before get_video_by_upload. C
- area E client half — SSE frame parsing across chunk boundaries: sound — frontend/lib/sse.ts:26-48 keeps a buffer, `pendingCR` and a scan offset so events split mid-line or across a CRLF pair are completed only when whole; header :9 states it is unit-tested (tests/sse.test.ts). No reader cited it; the terminal-event gaps (F13, ORCH-02) are upstream of the parser.
- area F/D local half — IndexedDB persister and unload flushing: unexamined — Nobody opened frontend/lib/idbCache.ts or history.ts:308-355. PERSIST_DEBOUNCE_MS=300 (:308, :354), persist failures are swallowed with 'the server holds the durable copy' (:343-345), and grep for pagehide/beforeunload/visibilitychange across frontend/lib and frontend/components returns nothing — so
- area G — compose overlays and the launcher's recreate primitive: sound — compose/compose.dgx-spark.yaml:18-31 and compose/compose.cluster-dgx-spark.yaml:108-110 override image/GPU/mounts/extra_hosts only and never set stop_grace_period, so compose.yaml's values are what every profile runs (no inter-profile drift); launcher compose.py:143-153 recreates with `up -d --no-de
- area G — rollback path: unexamined — scripts/deploy-rollback.sh:284-310 repeats the best-effort drain (`|| true`, :286) and `up -d --no-deps --force-recreate` on orchestrator+frontend (:309); nobody read it, and it inherits INF-1's 10 s SIGKILL on the frontend at the moment a bad deploy is being undone.
- area D — recovery test suite: unexamined — scripts/recovery-tests/ and scripts/tests/ hold only __pycache__ and `git ls-files` returns nothing for either — there is no recovery test to examine; the tests reader did not report the directories are empty.
- area Observability — /health composition: unexamined — orchestrator/app/health.py (571 lines) has zero references to video, asr, upload, whisper or live generations (grep empty); ORCH-15/INF-15 reasoned from main.py:753-772 and metrics.py without opening the module that builds the probe set.
