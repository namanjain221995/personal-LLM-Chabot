# Local test/dev requirements (host Python 3.12; containers pin 3.11 — accepted difference).
# Identical to requirements.txt EXCEPT: no transformers, no weasyprint (heavy /
# native; both are lazy at runtime), plus pytest. The whole test suite runs
# OFFLINE — no vLLM servers, no GPU, no torch.
fastapi>=0.115
# FastAPI raises at ROUTE-DEFINITION time when a route declares File()/Form()
# without this, so `import app.uploads` fails and 16 test modules cannot be
# collected. Its absence only ever worked because developers had the full
# requirements.txt installed; a clean checkout could not run the suite at all.
python-multipart>=0.0.9
uvicorn[standard]>=0.30
langgraph>=0.2
openai>=1.50
httpx>=0.27
duckdb>=1.0
lancedb==0.37.1  # exact, same as requirements.txt (tests must see the production format)
pandas>=2.2
pyarrow==25.0.1  # exact, same as requirements.txt
openpyxl>=3.1
matplotlib>=3.8
pydantic>=2.7
# V2 auth (V2-DESIGN §3c): argon2 password hashing + signed session cookies.
argon2-cffi>=23.1
itsdangerous>=2.1
# App state: PostgreSQL (see requirements.txt for why [binary,pool]).
psycopg[binary,pool]>=3.2,<4
# The crawler's link harvest (core/extract.py) is real frontier logic, and a
# missing lxml degrades it to "zero links found" rather than an error — a
# failure mode CI must be able to see. lxml alone is a small manylinux wheel;
# trafilatura (the heavy tree above it) stays out, and text extraction keeps
# its _strip_tags fallback.
lxml>=5
pytest>=8.0
