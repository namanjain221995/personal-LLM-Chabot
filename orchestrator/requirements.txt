# Container requirements (Python 3.11 in-container).
# NOTE: torch is provided by the nvcr.io/nvidia/pytorch base image — do NOT pin torch here.
fastapi>=0.115
# Phase 4: multipart dataset uploads (streamed, never base64 in the chat body)
python-multipart>=0.0.9
uvicorn[standard]>=0.30
langgraph>=0.2
openai>=1.50
httpx>=0.27
duckdb>=1.0
# LanceDB + Arrow are EXACT pins, identical in sync-worker/requirements.txt,
# and tests/test_dependency_pins.py fails if the two files ever disagree.
# Both containers open the same Lance files (/data/lancedb is written by
# the sync-worker and read here; /data/lancedb-web is written here), so
# the on-disk format and the Arrow ABI must be one version, not a range:
# on 2026-09-03 the ranges had resolved to lancedb 0.37.1 in both images
# but pyarrow 25.0.1 here and 21.0.0 in the worker. 25.0.1 is what the
# production orchestrator (the request path) runs today; the worker moves.
lancedb==0.37.1
pandas>=2.2
pyarrow==25.0.1
# Declared explicitly because app/recall.py's batched cosine uses it directly.
# It was already present, but only as a transitive dependency of pyarrow and
# pandas — so an upgrade that dropped it would have silently moved semantic
# recall onto its pure-python fallback (measured ~20x slower, on the event
# loop) with nothing failing to say so. A floor, not a pin: pyarrow's own
# requirement is the binding constraint and must stay free to resolve.
numpy>=1.26
openpyxl>=3.1
matplotlib>=3.8
pydantic>=2.7
# V2 auth (V2-DESIGN §3c): argon2 password hashing + signed session cookies.
argon2-cffi>=23.1
itsdangerous>=2.1
# App state (2026-08-10): PostgreSQL replaces stdlib sqlite3.
# [binary] ships libpq in the wheel — no libpq-dev in the image, and binary
# result mode is not optional: psycopg3 in TEXT mode is slower than the driver
# gap it is meant to close. [pool] is psycopg_pool, which is what actually
# deletes the old per-operation connect() cost.
psycopg[binary,pool]>=3.2,<4
# Reranker (Qwen3-Reranker-0.6B). Imported LAZILY inside the rag engine only —
# the app and the test suite run without it. Disable at runtime with RERANK_ENABLED=false.
transformers>=4.51
# PDF engine for pandoc (pandoc --pdf-engine=weasyprint) AND, since the
# Artifact Studio, driven in-process by app/artifacts/render/pdf.py with a
# custom URL fetcher. PINNED: the fetcher contract changed between 69 and 70
# (URLFetcherResponse required; `_fail_on_errors` read without a default),
# and an unpinned rebuild on 2026-09-11 broke every deck PDF in the container
# while the venv passed. Move the pin deliberately, with the renderer suites.
weasyprint==70.0
pypdfium2>=4.30
pillow>=10.0
trafilatura>=1.12
# Video understanding (2026-09-09). Voice-activity detection cuts a recording
# into transcription windows at pauses. `webrtcvad-wheels` is the maintained
# build of Google's detector WITH manylinux wheels for aarch64 and cp311/cp312
# — plain `webrtcvad` is an sdist that needs a compiler the CPU image does not
# have. Frame decoding is ffmpeg (apt, see the Dockerfiles), not a wheel:
# PyAV/OpenCV in-process would run on the event loop's cores; a child process
# is bounded by `-threads` and killed on a deadline.
webrtcvad-wheels>=2.0.14
# Artifact Studio (2026-09-11): the Office writers behind app/artifacts/render.
# EXACT pins, identical in requirements-dev.txt, because the files a person
# downloads are these libraries' output and a silent minor bump changes the
# bytes. Both are pure-Python wheels (py3-none-any) over lxml, which is
# already here; python-pptx also pulls XlsxWriter (pure Python) for the
# workbook embedded in a native chart. Verified downloadable for
# manylinux2014_aarch64 on cp311 and cp312. Imported lazily only.
python-pptx==1.0.2
python-docx==1.1.2
# XlsxWriter is python-pptx's transitive dependency (the workbook inside a
# native chart); pinned so that workbook's bytes do not change under a
# silent minor bump either. Pure Python.
XlsxWriter==3.2.9
