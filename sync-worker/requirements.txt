# Runtime dependencies for the Salesforce sync worker (Python 3.11 in the container).
# Ranged pins: latest compatible patch/minor is fine; majors are capped.
httpx>=0.27,<1
PyJWT>=2.8,<3
# Floor 50.0.0 (2026-09-15 security scan): <47 resolved 46.0.7, which pip-audit
# flags for GHSA-537c-gmf6-5ccf (bundled OpenSSL, fixed 48.0.1),
# GHSA-jwv3-5hgf-82ww and GHSA-m2h6-j472-rp4c (fixed 49.0.0) and
# GHSA-g6cj-pr64-35w5 (fixed 50.0.0). PyJWT uses it for the RS256 signature
# of the Salesforce JWT bearer grant (syncworker/sf_auth.py).
cryptography>=50.0.0,<51
duckdb>=1.0,<2
pyarrow==25.0.1
pandas>=2.2,<3
PyYAML>=6.0,<7
# EXACT pins, identical to orchestrator/requirements.txt (see the note there;
# orchestrator/tests/test_dependency_pins.py enforces the match). The
# orchestrator reads the Lance files this worker writes, so the two must
# share one LanceDB and one Arrow version.
lancedb==0.37.1
